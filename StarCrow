local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer
local RunService = game:GetService("RunService")
local StarterGui = game:GetService("StarterGui")
local UserInputService = game:GetService("UserInputService")
if not game:IsLoaded() then
    game.Loaded:Wait()
end
repeat task.wait(0.1) until LocalPlayer.Character
    and LocalPlayer.Character:FindFirstChild("HumanoidRootPart")
    and LocalPlayer.Character:FindFirstChild("Humanoid")
    and LocalPlayer.Character.Humanoid.Health > 0
    and LocalPlayer:FindFirstChildOfClass("PlayerGui")
    and workspace.CurrentCamera
    and workspace.CurrentCamera.CameraType ~= nil
local Camera = workspace.CurrentCamera
local mouse = LocalPlayer:GetMouse()
local function createFloatSizeSlider(panelRef, S_ref, UIS_ref)
    local MIN_FLOAT_SIZE = 30
    local MAX_FLOAT_SIZE = 130
    local TS2 = game:GetService("TweenService")
    local curVal = math.clamp(S_ref.floatBtnSize, MIN_FLOAT_SIZE, MAX_FLOAT_SIZE)
    local sizeLabelRow = Instance.new("Frame")
    sizeLabelRow.BackgroundTransparency = 1
    sizeLabelRow.Size   = UDim2.new(1, 0, 0, 16)
    sizeLabelRow.Parent = panelRef
    local sizeLbl = Instance.new("TextLabel")
    sizeLbl.Size                   = UDim2.new(0.5, 0, 1, 0)
    sizeLbl.BackgroundTransparency = 1
    sizeLbl.Text                   = "Float Size"
    sizeLbl.TextColor3             = Color3.fromRGB(180, 180, 180)
    sizeLbl.Font                   = Enum.Font.Gotham
    sizeLbl.TextSize               = 12
    sizeLbl.TextXAlignment         = Enum.TextXAlignment.Left
    sizeLbl.Parent                 = sizeLabelRow
    local sizeValueLabel = Instance.new("TextLabel")
    sizeValueLabel.Size                   = UDim2.new(0.5, 0, 1, 0)
    sizeValueLabel.Position               = UDim2.new(0.5, 0, 0, 0)
    sizeValueLabel.BackgroundTransparency = 1
    sizeValueLabel.Text                   = tostring(S_ref.floatBtnSize)
    sizeValueLabel.TextColor3             = Color3.fromRGB(200, 200, 200)
    sizeValueLabel.Font                   = Enum.Font.GothamBold
    sizeValueLabel.TextSize               = 12
    sizeValueLabel.TextXAlignment         = Enum.TextXAlignment.Right
    sizeValueLabel.Parent                 = sizeLabelRow
    local sliderWrapper = Instance.new("Frame")
    sliderWrapper.Size                   = UDim2.new(1, 0, 0, 20)
    sliderWrapper.BackgroundTransparency = 1
    sliderWrapper.BorderSizePixel        = 0
    sliderWrapper.Active                 = true
    sliderWrapper.Parent                 = panelRef
    local trackBg = Instance.new("Frame")
    trackBg.Size                   = UDim2.new(1, 0, 0, 6)
    trackBg.Position               = UDim2.new(0, 0, 0.5, -3)
    trackBg.BackgroundColor3       = Color3.fromRGB(55, 55, 60)
    trackBg.BackgroundTransparency = 0
    trackBg.BorderSizePixel        = 0
    trackBg.Active                 = true
    trackBg.Parent                 = sliderWrapper
    Instance.new("UICorner", trackBg).CornerRadius = UDim.new(1, 0)
    local trackGrad = Instance.new("UIGradient")
    trackGrad.Color = ColorSequence.new({
        ColorSequenceKeypoint.new(0, Color3.fromRGB(35, 35, 38)),
        ColorSequenceKeypoint.new(1, Color3.fromRGB(70, 70, 75)),
    })
    trackGrad.Rotation = 0
    trackGrad.Parent   = trackBg
    local fill = Instance.new("Frame")
    fill.Size                   = UDim2.new((curVal - MIN_FLOAT_SIZE) / (MAX_FLOAT_SIZE - MIN_FLOAT_SIZE), 0, 1, 0)
    fill.BackgroundColor3       = Color3.fromRGB(160, 160, 170)
    fill.BackgroundTransparency = 0
    fill.BorderSizePixel        = 0
    fill.ZIndex                 = 2
    fill.Parent                 = trackBg
    Instance.new("UICorner", fill).CornerRadius = UDim.new(1, 0)
    local thumb = Instance.new("Frame")
    thumb.Size                   = UDim2.new(0, 14, 0, 14)
    thumb.AnchorPoint            = Vector2.new(0.5, 0.5)
    thumb.Position               = UDim2.new((curVal - MIN_FLOAT_SIZE) / (MAX_FLOAT_SIZE - MIN_FLOAT_SIZE), 0, 0.5, 0)
    thumb.BackgroundColor3       = Color3.fromRGB(255, 255, 255)
    thumb.BackgroundTransparency = 0
    thumb.BorderSizePixel        = 0
    thumb.ZIndex                 = 10
    thumb.Active                 = true
    thumb.Parent                 = trackBg
    Instance.new("UICorner", thumb).CornerRadius = UDim.new(1, 0)
    local sDragging = false
    local sDragInput = nil
    local function applySlider(inputX)
        local abs = trackBg.AbsolutePosition
        local sz  = trackBg.AbsoluteSize
        local rel = math.clamp((inputX - abs.X) / sz.X, 0, 1)
        curVal = math.clamp(math.floor(MIN_FLOAT_SIZE + rel * (MAX_FLOAT_SIZE - MIN_FLOAT_SIZE) + 0.5), MIN_FLOAT_SIZE, MAX_FLOAT_SIZE)
        S_ref.floatBtnSize = curVal
        sizeValueLabel.Text = tostring(S_ref.floatBtnSize)
        local fillRel = (curVal - MIN_FLOAT_SIZE) / (MAX_FLOAT_SIZE - MIN_FLOAT_SIZE)
        fill.Size      = UDim2.new(fillRel, 0, 1, 0)
        thumb.Position = UDim2.new(fillRel, 0, 0.5, 0)
        if S_ref.auraFloatBtn     then S_ref.auraFloatBtn.Size     = UDim2.new(0, S_ref.floatBtnSize, 0, S_ref.floatBtnSize) end
        if S_ref.dropFloatBtn     then S_ref.dropFloatBtn.Size     = UDim2.new(0, S_ref.floatBtnSize, 0, S_ref.floatBtnSize) end
        if S_ref.equipAllFloatBtn then S_ref.equipAllFloatBtn.Size = UDim2.new(0, S_ref.floatBtnSize, 0, S_ref.floatBtnSize) end
        if S_ref.useFloatBtn      then S_ref.useFloatBtn.Size      = UDim2.new(0, S_ref.floatBtnSize, 0, S_ref.floatBtnSize) end
        if S_ref.invisFloatBtn    then S_ref.invisFloatBtn.Size    = UDim2.new(0, S_ref.floatBtnSize, 0, S_ref.floatBtnSize) end
        if S_ref.flyFloatBtn      then S_ref.flyFloatBtn.Size      = UDim2.new(0, S_ref.floatBtnSize, 0, S_ref.floatBtnSize) end
        if S_ref.noclipFloatBtn   then S_ref.noclipFloatBtn.Size   = UDim2.new(0, S_ref.floatBtnSize, 0, S_ref.floatBtnSize) end
    end
    S_ref.updateFloatSizeVisuals = function(newSize)
        curVal = math.clamp(newSize, MIN_FLOAT_SIZE, MAX_FLOAT_SIZE)
        S_ref.floatBtnSize = curVal
        sizeValueLabel.Text = tostring(S_ref.floatBtnSize)
        local fillRel = (curVal - MIN_FLOAT_SIZE) / (MAX_FLOAT_SIZE - MIN_FLOAT_SIZE)
        fill.Size      = UDim2.new(fillRel, 0, 1, 0)
        thumb.Position = UDim2.new(fillRel, 0, 0.5, 0)
        if S_ref.auraFloatBtn     then S_ref.auraFloatBtn.Size     = UDim2.new(0, S_ref.floatBtnSize, 0, S_ref.floatBtnSize) end
        if S_ref.dropFloatBtn     then S_ref.dropFloatBtn.Size     = UDim2.new(0, S_ref.floatBtnSize, 0, S_ref.floatBtnSize) end
        if S_ref.equipAllFloatBtn then S_ref.equipAllFloatBtn.Size = UDim2.new(0, S_ref.floatBtnSize, 0, S_ref.floatBtnSize) end
        if S_ref.useFloatBtn      then S_ref.useFloatBtn.Size      = UDim2.new(0, S_ref.floatBtnSize, 0, S_ref.floatBtnSize) end
        if S_ref.invisFloatBtn    then S_ref.invisFloatBtn.Size    = UDim2.new(0, S_ref.floatBtnSize, 0, S_ref.floatBtnSize) end
        if S_ref.flyFloatBtn      then S_ref.flyFloatBtn.Size      = UDim2.new(0, S_ref.floatBtnSize, 0, S_ref.floatBtnSize) end
        if S_ref.noclipFloatBtn   then S_ref.noclipFloatBtn.Size   = UDim2.new(0, S_ref.floatBtnSize, 0, S_ref.floatBtnSize) end
    end
    sliderWrapper.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1
        or input.UserInputType == Enum.UserInputType.Touch then
            if sDragging then return end
            sDragging = true
            sDragInput = input
            applySlider(input.Position.X)
            TS2:Create(thumb, TweenInfo.new(0.08), { Size = UDim2.new(0, 17, 0, 17) }):Play()
        end
    end)
    trackBg.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1
        or input.UserInputType == Enum.UserInputType.Touch then
            if sDragging then return end
            sDragging = true
            sDragInput = input
            applySlider(input.Position.X)
            TS2:Create(thumb, TweenInfo.new(0.08), { Size = UDim2.new(0, 17, 0, 17) }):Play()
        end
    end)
    UIS_ref.InputChanged:Connect(function(input)
        if not sDragging then return end
        if input.UserInputType == Enum.UserInputType.MouseMovement then
            applySlider(input.Position.X)
        elseif input.UserInputType == Enum.UserInputType.Touch and input == sDragInput then
            applySlider(input.Position.X)
        end
    end)
    UIS_ref.InputEnded:Connect(function(input)
        if not sDragging then return end
        if input.UserInputType == Enum.UserInputType.MouseButton1
        or (input.UserInputType == Enum.UserInputType.Touch and input == sDragInput) then
            sDragging = false
            sDragInput = nil
            TS2:Create(thumb, TweenInfo.new(0.08), { Size = UDim2.new(0, 14, 0, 14) }):Play()
        end
    end)
end
local S = {
    chamsEnabled = false,
    SpectatingPlayer=nil, spectating=false, specConnection=nil,
    selectedSpecButton=nil, specButtons={}, specContainer=nil,
    flyEnabled=false, flySpeed=2, flyConnection=nil, noclipConnection=nil,
    infJumpEnabled=false, infJumpConnection=nil,
    autoTPEnabled=false, autoTPConnection=nil,
    ctrlClickTPEnabled=false,
    noclipEnabled=false, noclipLoop=nil,
    freezeBodyEnabled=false, freezeBodyConnection=nil, freezeBodyPrevSpeed=16, freezeBodyPrevJump=50,
    cameraNoclipEnabled=false, cameraNoclipConnection=nil, cameraNoclipPrevType=nil,
    jumpPowerEnabled=false, jumpPowerConnection=nil,
    floatEnabled=false, floatJumpConn=nil, floatStepConn=nil, floatHovering=false, floatLandConn=nil,
    noclipFloatEnabled=false, noclipFloatBtn=nil, noclipFloatStroke=nil, noclipFloatPos=nil,
    autoHitboxEnabled=false, autoBodyHitboxEnabled=false, bodyHitboxChars={},
    nameESPEnabled=false, boxESPEnabled=false, espNameAllState=false, tracerESPEnabled=false,
    espShowName=false, espShowDist=false, espShowHp=false,
    selectedPlayers={}, selectedTPPlayers={}, boxESPObjects={}, nameESPObjects={}, tracerESPObjects={},
    processedChars={}, playerConnections={}, manualPositionCFrame=nil,
    isInvisible=false, invisButton=nil, invisSound=nil,
    auraListening=false,
    auraFloatEnabled=false, dropFloatEnabled=false,
    auraFloatBtn=nil, dropFloatBtn=nil, equipAllFloatEnabled=false, equipAllFloatBtn=nil,
    auraFloatDragConn=nil, dropFloatDragConn=nil, floatBtnSize=46,
    FlingActive=false, BangActive=false,
    SelectedFlingTargets={}, FlingPlayerCheckboxes={},
    walkflinging=false, walkFlingHumanoidDiedConn=nil, speaker=nil,
    antiFlingEnabled=false, antiFlingConnection=nil, antiFlingAutoEnabledByWalkFling=false,
    loopBringActive=false, loopBringStates={},
    antiAfkEnabled=false,
    autoClickEnabled=false, autoClickConnection=nil,
    feRefreshBusy=false, lastFeToolSignature="", feAutoScanRunning=false,
    useCooling=false, useFloatEnabled=false, useFloatBtn=nil, feUseInput=nil,
    statusLabel=nil, sizeInput=nil,
    flyToggle=nil, loopspeedInput=nil, loopspeedToggle=nil,
    walkFlingBtn=nil, flingStatusLabel=nil, flingPlayerList=nil,
    autoTPBtn=nil, tpPlayerList=nil, playerList=nil,
    feAuraBtn=nil, feKBBtn=nil,
    invisFloatEnabled=false, invisFloatBtn=nil, invisFloatStroke=nil,
    flyFloatEnabled=false, flyFloatBtn=nil, flyFloatStroke=nil,
    invisFloatPos=nil, flyFloatPos=nil, auraFloatPos=nil, dropFloatPos=nil, equipAllFloatPos=nil, useFloatPos=nil,
    flingStartBtn=nil, bangBtn=nil, loopBringBtn=nil,
    aimSettings=nil,
    keybindsEnabled=true,
    invisKeyCode=Enum.KeyCode.C,
    flyKeyCode=Enum.KeyCode.F,
    noclipKeyCode=Enum.KeyCode.J,
    waitingForKeybind=nil,
    menuKeybind=Enum.KeyCode.RightControl,
    isChangingMenuKey=false,
    btnRefs={},
}
S.speaker = LocalPlayer
function SpectatePlayer(player)
    S.SpectatingPlayer = player
    S.spectating = true
    if S.specConnection then
        S.specConnection:Disconnect()
        S.specConnection = nil
    end
    local function setCamera(character)
        if not S.spectating then return end
        local humanoid = character:WaitForChild("Humanoid")
        Camera.CameraSubject = humanoid
    end
    if player.Character then
        setCamera(player.Character)
    end
    S.specConnection = player.CharacterAdded:Connect(function(char)
        if S.spectating then
            setCamera(char)
        end
    end)
end
function StopSpectate()
    S.spectating = false
    S.SpectatingPlayer = nil
    if S.specConnection then
        S.specConnection:Disconnect()
        S.specConnection = nil
    end
    if S.selectedSpecButton then
        S.selectedSpecButton.BackgroundColor3 = Color3.fromRGB(70,70,70)
        S.selectedSpecButton = nil
    end
    if LocalPlayer.Character then
        local humanoid = LocalPlayer.Character:FindFirstChildOfClass("Humanoid")
        if humanoid then
            Camera.CameraSubject = humanoid
        end
    end
end
S.DEFAULT_HITBOX_SIZE = 15
S.ESP_COLOR          = Color3.fromRGB(255, 255, 255)
S.ESP_TRACER_COLOR   = Color3.fromRGB(255, 255, 255)
S.ESP_TRANSPARENCY   = 0.7
S.TEXT_ESP_COLOR     = Color3.fromRGB(255, 255, 255)
S.INVIS_CONFIG = {
    TOGGLE_KEY = Enum.KeyCode.C,
    SOUND_ID = "rbxassetid://139804904213958",
    INVISIBILITY_POSITION = Vector3.new(-25.95, 84, 3537.55),
    NOTIFICATION_DURATION = 3,
    BACKGROUND_COLOR = Color3.fromRGB(25, 25, 25),
    PRIMARY_COLOR = Color3.fromRGB(0, 170, 255),
    SUCCESS_COLOR = Color3.fromRGB(46, 204, 113),
    TEXT_COLOR = Color3.fromRGB(255, 255, 255),
}
getgenv().OldPos = nil
getgenv().FPDH = workspace.FallenPartsDestroyHeight
do
    local COOLDOWN = 10
    local now = tick()
    local function _earlyNotif(msg)
        pcall(function()
            local TweenService = game:GetService("TweenService")
            local parent
            pcall(function() parent = game:GetService("CoreGui") end)
            if not parent then parent = game:GetService("Players").LocalPlayer:FindFirstChildOfClass("PlayerGui") end
            if not parent then return end
            local ng = parent:FindFirstChild("SCNotifGui_StarCrow")
            if not ng then
                ng = Instance.new("ScreenGui")
                ng.Name = "SCNotifGui_StarCrow"
                ng.ResetOnSpawn = false
                ng.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
                ng.DisplayOrder = 999998
                ng.IgnoreGuiInset = true
                local ok2 = pcall(function() ng.Parent = game:GetService("CoreGui") end)
                if not ok2 or not ng.Parent then ng.Parent = game:GetService("Players").LocalPlayer:FindFirstChildOfClass("PlayerGui") end
            end
            if not ng or not ng.Parent then return end
            local container = ng:FindFirstChild("NotifContainer")
            if not container then
                container = Instance.new("Frame")
                container.Name = "NotifContainer"
                container.Size = UDim2.new(0, 260, 1, 0)
                container.Position = UDim2.new(1, -270, 0, 0)
                container.BackgroundTransparency = 1
                container.BorderSizePixel = 0
                container.Parent = ng
                local layout = Instance.new("UIListLayout")
                layout.VerticalAlignment = Enum.VerticalAlignment.Bottom
                layout.SortOrder = Enum.SortOrder.LayoutOrder
                layout.Padding = UDim.new(0, 8)
                layout.Parent = container
                local pad = Instance.new("UIPadding")
                pad.PaddingBottom = UDim.new(0, 24)
                pad.Parent = container
            end
            local nf = Instance.new("Frame")
            nf.Name = "NotifCard"
            nf.Size = UDim2.new(1, 0, 0, 68)
            nf.BackgroundColor3 = Color3.fromRGB(18, 18, 24)
            nf.BackgroundTransparency = 0.35
            nf.BorderSizePixel = 0
            nf.ClipsDescendants = true
            nf.Position = UDim2.new(2, 0, 0, 0)
            nf.Parent = container
            local corner = Instance.new("UICorner")
            corner.CornerRadius = UDim.new(0, 8)
            corner.Parent = nf
            local blur = Instance.new("UIGradient")
            blur.Color = ColorSequence.new({
                ColorSequenceKeypoint.new(0, Color3.fromRGB(255,255,255)),
                ColorSequenceKeypoint.new(1, Color3.fromRGB(200,200,255))
            })
            blur.Transparency = NumberSequence.new({
                NumberSequenceKeypoint.new(0, 0.9),
                NumberSequenceKeypoint.new(1, 1)
            })
            blur.Rotation = 90
            blur.Parent = nf
            local stroke = Instance.new("UIStroke")
            stroke.Thickness = 2
            stroke.Transparency = 0
            stroke.ApplyStrokeMode = Enum.ApplyStrokeMode.Border
            stroke.Color = Color3.fromRGB(255, 255, 255)
            stroke.Parent = nf
            local gradient = Instance.new("UIGradient")
            gradient.Color = ColorSequence.new({
                ColorSequenceKeypoint.new(0, Color3.fromRGB(170, 70, 255)),
                ColorSequenceKeypoint.new(0.5, Color3.fromRGB(0, 180, 255)),
                ColorSequenceKeypoint.new(1, Color3.fromRGB(170, 70, 255)),
            })
            gradient.Rotation = 0
            gradient.Parent = stroke
            task.spawn(function()
                TweenService:Create(gradient, TweenInfo.new(4, Enum.EasingStyle.Linear, Enum.EasingDirection.InOut, -1), { Rotation = 360 }):Play()
            end)
            local icon = Instance.new("ImageLabel")
            icon.Size = UDim2.new(0, 28, 0, 28)
            icon.Position = UDim2.new(0, 10, 0, 6)
            icon.BackgroundTransparency = 1
            icon.Image = "rbxassetid://5276992751"
            icon.ZIndex = 2
            icon.Parent = nf
            local title = Instance.new("TextLabel")
            title.Size = UDim2.new(1, -44, 0, 24)
            title.Position = UDim2.new(0, 40, 0, 7)
            title.BackgroundTransparency = 1
            title.Text = "StarCrow Says:"
            title.TextXAlignment = Enum.TextXAlignment.Left
            title.TextColor3 = Color3.fromRGB(255, 255, 255)
            title.Font = Enum.Font.GothamBold
            title.TextSize = 15
            title.ZIndex = 2
            title.Parent = nf
            local titleRainbow = Instance.new("UIGradient")
            titleRainbow.Color = ColorSequence.new({
                ColorSequenceKeypoint.new(0, Color3.fromRGB(210, 60, 255)),
                ColorSequenceKeypoint.new(1, Color3.fromRGB(0, 210, 255)),
            })
            titleRainbow.Rotation = 0
            titleRainbow.Parent = title
            task.spawn(function()
                TweenService:Create(titleRainbow, TweenInfo.new(3, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), { Rotation = 90 }):Play()
            end)
            local body = Instance.new("TextLabel")
            body.Size = UDim2.new(1, -20, 0, 28)
            body.Position = UDim2.new(0, 14, 0, 36)
            body.BackgroundTransparency = 1
            body.Text = msg
            body.TextXAlignment = Enum.TextXAlignment.Left
            body.TextColor3 = Color3.fromRGB(220, 220, 220)
            body.Font = Enum.Font.GothamSemibold
            body.TextSize = 13
            body.TextWrapped = true
            body.ZIndex = 2
            body.Parent = nf
            nf.Position = UDim2.new(2, 0, 0, 0)
            TweenService:Create(nf, TweenInfo.new(0.35, Enum.EasingStyle.Back, Enum.EasingDirection.Out), {
                Position = UDim2.new(0, 0, 0, 0)
            }):Play()
            task.delay(3, function()
                if not nf or not nf.Parent then return end
                local tw = TweenService:Create(nf, TweenInfo.new(0.28, Enum.EasingStyle.Quad, Enum.EasingDirection.In), {
                    Position = UDim2.new(2, 0, 0, 0)
                })
                tw:Play()
                tw.Completed:Connect(function()
                    if nf and nf.Parent then nf:Destroy() end
                end)
            end)
        end)
    end
    if getgenv()._StarCrowLoaded then
        _earlyNotif("Script is already loaded!")
        return
    end
    if getgenv()._StarCrowLastExe then
        local elapsed = now - getgenv()._StarCrowLastExe
        local remaining = math.ceil(COOLDOWN - elapsed)
        if remaining > 0 then
            _earlyNotif("Slow down executing, try again (" .. remaining .. "s)")
            return
        end
    end
    getgenv()._StarCrowLastExe = now
    getgenv()._StarCrowLoaded = true
end
local function _playClick()
    local s = Instance.new("Sound")
    s.SoundId = "rbxassetid://116394299154002"
    s.Volume = 0.5
    s.Parent = game:GetService("SoundService")
    s:Play()
    game:GetService("Debris"):AddItem(s, 2)
end
local function tableCount(t)
    local n = 0
    for _ in t do n = n + 1 end
    return n
end
local function getTeamColor(player)
    if player.Team and player.Team.TeamColor then
        return player.Team.TeamColor.Color
    end
    return S.ESP_COLOR
end
local function getESPTeamColor(player)
    if LocalPlayer.Team ~= nil and player.Team == LocalPlayer.Team then
        return ESP_FRIENDLY_COLOR
    end
    return getTeamColor(player)
end
local function createNotification(title, text)
    pcall(function()
        StarterGui:SetCore("SendNotification", {
            Title = title,
            Text = text,
            Duration = S.INVIS_CONFIG.NOTIFICATION_DURATION,
        })
    end)
end
local function setCharacterTransparency(character, transparency)
    if not character then return end
    for _, d in character:GetDescendants() do
        if d:FindFirstAncestorOfClass("Accessory") then
            continue
        end
        if d:IsA("BasePart") then
            if d.Name == "HumanoidRootPart" then
                d.Transparency = 1
            elseif d.Name == "Head" then
                d.Transparency = 0
            else
                d.Transparency = transparency
            end
        elseif d:IsA("Decal") then
            if d.Name == "face" and d.Parent and d.Parent.Name == "Head" then
                d.Transparency = 0
            else
                d.Transparency = transparency
            end
        end
    end
end
local function getHumanoidRootPart()
    local character = LocalPlayer.Character
    if not character then return nil end
    return character:FindFirstChild("HumanoidRootPart")
end
local function isAlive(character)
    if not character then return false end
    local humanoid = character:FindFirstChildOfClass("Humanoid")
    return humanoid and humanoid.Health > 0
end
local function setFloatStrokeActive(stroke, isActive)
    if not stroke then return end
    local TweenService = game:GetService("TweenService")
    local old = stroke:FindFirstChildOfClass("UIGradient")
    if old then old:Destroy() end
    if isActive then
        stroke.Color = Color3.fromRGB(255, 255, 255)
        stroke.Thickness = 2.2
        local grad = Instance.new("UIGradient")
        grad.Color = ColorSequence.new({
            ColorSequenceKeypoint.new(0,   Color3.fromRGB(170, 70, 255)),
            ColorSequenceKeypoint.new(0.5, Color3.fromRGB(0, 180, 255)),
            ColorSequenceKeypoint.new(1,   Color3.fromRGB(170, 70, 255)),
        })
        grad.Rotation = 0
        grad.Parent = stroke
        task.spawn(function()
            TweenService:Create(grad, TweenInfo.new(3, Enum.EasingStyle.Linear, Enum.EasingDirection.InOut, -1), { Rotation = 360 }):Play()
        end)
    else
        stroke.Color = Color3.fromRGB(255, 255, 255)
        stroke.Thickness = 1.6
        local grad = Instance.new("UIGradient")
        grad.Color = ColorSequence.new({
            ColorSequenceKeypoint.new(0,   Color3.fromRGB(60, 0, 120)),
            ColorSequenceKeypoint.new(0.5, Color3.fromRGB(0, 100, 200)),
            ColorSequenceKeypoint.new(1,   Color3.fromRGB(60, 0, 120)),
        })
        grad.Rotation = 0
        grad.Parent = stroke
        task.spawn(function()
            TweenService:Create(grad, TweenInfo.new(3, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), { Rotation = 180 }):Play()
        end)
    end
end
local function setToggleState(btn, isOn)
    if not btn then return end
    local track = btn:FindFirstChild("ToggleTrack")
    if track then
        track.BackgroundColor3 = isOn and Color3.fromRGB(50, 200, 120) or Color3.fromRGB(38, 38, 38)
        track.BackgroundTransparency = isOn and 0 or 0.35
    end
end
local toggleInvisibility
local disableInvisibility
do
    local INVIS_Y_OFFSET = 1000
    local invisPlatform = nil
    local fakeCharacter = nil
    local motor6DMap = {}
    local invisHeartbeatConn = nil
    local invisSteppedConn = nil
    local invisJumpConn = nil

    local function createFakeCharacter(realChar)
        local ok, clone = pcall(function()
            realChar.Archivable = true
            local c = realChar:Clone()
            realChar.Archivable = false
            return c
        end)
        if not ok or not clone then return nil end

        for _, descendant in ipairs(clone:GetDescendants()) do
            if descendant:IsA("Script") or descendant:IsA("LocalScript") or descendant:IsA("Sound") or descendant:IsA("Highlight") then
                descendant:Destroy()
            elseif descendant:IsA("BasePart") or descendant:IsA("Decal") then
                if descendant:IsA("BasePart") then
                    descendant.Anchored = false
                end
                local realPart = realChar:FindFirstChild(descendant.Name, true)
                if descendant.Name == "HumanoidRootPart" or (realPart and realPart:IsA("BasePart") and realPart.Transparency >= 0.99) then
                    descendant.Transparency = 1
                else
                    descendant.Transparency = 0.5
                end
            end
        end

        motor6DMap = {}
        for _, descendant in ipairs(realChar:GetDescendants()) do
            if descendant:IsA("Motor6D") then
                local current = descendant
                local path = {}
                while current and current ~= realChar do
                    table.insert(path, 1, current.Name)
                    current = current.Parent
                end
                local cloneCurrent = clone
                for _, name in ipairs(path) do
                    cloneCurrent = cloneCurrent and cloneCurrent:FindFirstChild(name)
                end
                if cloneCurrent and cloneCurrent:IsA("Motor6D") then
                    motor6DMap[descendant] = cloneCurrent
                end
            end
        end

        clone.Parent = workspace
        return clone
    end

    disableInvisibility = function()
        if invisHeartbeatConn then
            invisHeartbeatConn:Disconnect()
            invisHeartbeatConn = nil
        end
        if invisSteppedConn then
            invisSteppedConn:Disconnect()
            invisSteppedConn = nil
        end
        if invisJumpConn then
            invisJumpConn:Disconnect()
            invisJumpConn = nil
        end

        local character = LocalPlayer.Character
        if character then
            local humanoidRootPart = character:FindFirstChild("HumanoidRootPart")
            local humanoid = character:FindFirstChildOfClass("Humanoid")
            local fakeHRP = fakeCharacter and fakeCharacter:FindFirstChild("HumanoidRootPart")
            local returnCFrame = fakeHRP and fakeHRP.CFrame

            if fakeCharacter then
                fakeCharacter:Destroy()
                fakeCharacter = nil
            end

            if humanoidRootPart and returnCFrame then
                humanoidRootPart.CFrame = returnCFrame
                humanoidRootPart.AssemblyLinearVelocity = Vector3.new(0, 0, 0)
            end

            local camera = workspace.CurrentCamera
            if camera and humanoid then
                camera.CameraSubject = humanoid
            end
        end

        if invisPlatform then
            invisPlatform:Destroy()
            invisPlatform = nil
        end

        motor6DMap = {}
        S.isInvisible = false
        if S.invisButton then
            setToggleState(S.invisButton, false)
            S.invisButton.Text = "Invisible: OFF"
        end
        if S.invisFloatStroke then
            setFloatStrokeActive(S.invisFloatStroke, false)
        end
    end

    local function enableInvisibility()
        local character = LocalPlayer.Character
        if not character then return end

        local humanoidRootPart = character:FindFirstChild("HumanoidRootPart")
        local humanoid = character:FindFirstChildOfClass("Humanoid")
        if not humanoidRootPart or not humanoid then return end

        local hipHeightOffset = (humanoid.RigType ~= Enum.HumanoidRigType.R6) and (humanoid.HipHeight + humanoidRootPart.Size.Y / 2) or 3
        local originalPos = humanoidRootPart.Position
        local groundY = originalPos.Y - hipHeightOffset

        fakeCharacter = createFakeCharacter(character)
        if not fakeCharacter then return end

        local fakeHRP = fakeCharacter:FindFirstChild("HumanoidRootPart")
        local fakeHumanoid = fakeCharacter:FindFirstChildOfClass("Humanoid")
        if not fakeHRP or not fakeHumanoid then
            if fakeCharacter then fakeCharacter:Destroy() fakeCharacter = nil end
            return
        end

        invisPlatform = Instance.new("Part")
        invisPlatform.Name = "InvisPlatform"
        invisPlatform.Size = Vector3.new(12, 1, 12)
        invisPlatform.Anchored = true
        invisPlatform.CanCollide = true
        invisPlatform.Transparency = 1
        invisPlatform.CastShadow = false
        invisPlatform.Parent = workspace
        invisPlatform.CFrame = CFrame.new(originalPos.X, groundY + INVIS_Y_OFFSET - 0.5, originalPos.Z)

        fakeHumanoid.WalkSpeed = humanoid.WalkSpeed
        fakeHumanoid.JumpPower = humanoid.JumpPower
        fakeHumanoid.JumpHeight = humanoid.JumpHeight
        fakeHumanoid.UseJumpPower = humanoid.UseJumpPower

        local camera = workspace.CurrentCamera
        if camera then
            camera.CameraSubject = fakeHumanoid
        end

        humanoidRootPart.CFrame = humanoidRootPart.CFrame + Vector3.new(0, INVIS_Y_OFFSET, 0)
        humanoidRootPart.AssemblyLinearVelocity = Vector3.new(0, 0, 0)

        invisSteppedConn = RunService.Stepped:Connect(function()
            if fakeCharacter and character then
                for realMotor, fakeMotor in pairs(motor6DMap) do
                    if realMotor and realMotor.Parent and realMotor.Parent.Parent then
                        fakeMotor.Transform = realMotor.Transform
                    end
                end
            end
        end)

        invisJumpConn = UserInputService.JumpRequest:Connect(function()
            if fakeHumanoid then
                fakeHumanoid.Jump = true
            end
        end)

        invisHeartbeatConn = RunService.Heartbeat:Connect(function()
            if not character or not character.Parent or humanoid.Health <= 0 then
                if S.isInvisible then
                    disableInvisibility()
                end
                return
            end

            if humanoidRootPart and fakeCharacter and fakeCharacter.Parent and fakeHRP and fakeHRP.Parent and fakeHumanoid and invisPlatform then
                local state = fakeHumanoid:GetState()
                if (state == Enum.HumanoidStateType.Climbing or state == Enum.HumanoidStateType.Swimming or state == Enum.HumanoidStateType.Seated) and state ~= humanoid:GetState() then
                    humanoid:ChangeState(state)
                end

                local moveDirection = humanoid.MoveDirection
                if state == Enum.HumanoidStateType.Climbing then
                    if camera then
                        local lookVector = camera.CFrame.LookVector
                        local unit = Vector3.new(lookVector.X, 0, lookVector.Z).Unit
                        local dot = humanoid.MoveDirection:Dot(unit)
                        if dot > 0.1 then
                            moveDirection = Vector3.new(0, 1, 0)
                        elseif dot < -0.1 then
                            moveDirection = Vector3.new(0, -1, 0)
                        else
                            moveDirection = Vector3.new(0, 0, 0)
                        end
                    end
                elseif state == Enum.HumanoidStateType.Swimming then
                    local jumpY = (UserInputService:IsKeyDown(Enum.KeyCode.Space) or humanoid.Jump) and 1 or 0
                    moveDirection = Vector3.new(moveDirection.X, jumpY, moveDirection.Z)
                end

                fakeHumanoid:Move(moveDirection, false)
                fakeHumanoid.Jump = humanoid.Jump
                fakeHumanoid.WalkSpeed = humanoid.WalkSpeed
                fakeHumanoid.JumpPower = humanoid.JumpPower
                fakeHumanoid.JumpHeight = humanoid.JumpHeight
                fakeHumanoid.UseJumpPower = humanoid.UseJumpPower

                local raycastParams = RaycastParams.new()
                raycastParams.FilterType = Enum.RaycastFilterType.Exclude
                raycastParams.FilterDescendantsInstances = {character, invisPlatform, fakeCharacter}

                local raycastResult = workspace:Raycast(fakeHRP.Position, Vector3.new(0, -500, 0), raycastParams)
                if raycastResult then
                    groundY = raycastResult.Position.Y
                else
                    groundY = fakeHRP.Position.Y - hipHeightOffset
                end

                humanoidRootPart.CFrame = fakeHRP.CFrame + Vector3.new(0, INVIS_Y_OFFSET, 0)
                humanoidRootPart.AssemblyLinearVelocity = fakeHRP.AssemblyLinearVelocity
                humanoidRootPart.AssemblyAngularVelocity = fakeHRP.AssemblyAngularVelocity
                invisPlatform.CFrame = CFrame.new(humanoidRootPart.Position.X, groundY + INVIS_Y_OFFSET - 0.5, humanoidRootPart.Position.Z)

                if humanoidRootPart.Position.Y < groundY + INVIS_Y_OFFSET - 15 then
                    humanoidRootPart.CFrame = CFrame.new(humanoidRootPart.Position.X, groundY + INVIS_Y_OFFSET + hipHeightOffset + 2, humanoidRootPart.Position.Z)
                    humanoidRootPart.AssemblyLinearVelocity = Vector3.new(0, 0, 0)
                end
            end
        end)

        S.isInvisible = true
        if S.invisButton then
            setToggleState(S.invisButton, true)
            S.invisButton.Text = "Invisible: ON"
        end
        if S.invisFloatStroke then
            setFloatStrokeActive(S.invisFloatStroke, true)
        end
    end

    toggleInvisibility = function()
        local character = LocalPlayer.Character
        if not character or not character:FindFirstChild("HumanoidRootPart") then return end
        if S.isInvisible then
            disableInvisibility()
        else
            enableInvisibility()
        end
    end
end
local function removeBoxESP(character)
    if not character then return end
    if S.boxESPObjects[character] then
        pcall(function() S.boxESPObjects[character]:Destroy() end)
        S.boxESPObjects[character] = nil
    end
    local head = character:FindFirstChild("Head")
    if head then
        for _, c in head:GetChildren() do
            if c:IsA("BoxHandleAdornment") and c.Name == "BoxChamESP" then
                c:Destroy()
            end
        end
    end
end
local function addBoxESP(character, color, size)
    if not character or not isAlive(character) then return end
    removeBoxESP(character)
    local head = character:FindFirstChild("Head")
    if not head or not head:IsA("BasePart") then return end
    local box = Instance.new("BoxHandleAdornment")
    box.Name = "BoxChamESP"
    box.Adornee = head
    box.Size = Vector3.new(size, size, size)
    box.Color3 = color or ESP_BOX_COLOR
    box.Transparency = S.ESP_TRANSPARENCY
    box.ZIndex = 10
    box.AlwaysOnTop = true
    box.Parent = head
    S.boxESPObjects[character] = box
end
local function removeChams(character)
    if not character then return end
    local highlight = character:FindFirstChild("StarCrowChams")
    if highlight then
        highlight:Destroy()
    end
end
local function removeOutlineChams(character)
    if not character then return end
    local h = character:FindFirstChild("StarCrowOutline")
    if h then h:Destroy() end
end
local function addOutlineChams(character)
    if not character or not isAlive(character) then return end
    removeOutlineChams(character)
    local h = Instance.new("Highlight")
    h.Name = "StarCrowOutline"
    h.Adornee = character
    h.FillTransparency = 1
    h.OutlineColor = Color3.fromRGB(255, 80, 80)
    h.OutlineTransparency = 0
    h.DepthMode = Enum.HighlightDepthMode.AlwaysOnTop
    h.Parent = character
end
local function addChams(character, color)
    if not character or not isAlive(character) then return end
    removeChams(character)
    local chamColor = color or Color3.fromRGB(50, 200, 120)
    local highlight = Instance.new("Highlight")
    highlight.Name = "StarCrowChams"
    highlight.Adornee = character
    highlight.FillColor = chamColor
    highlight.FillTransparency = 0.3
    highlight.OutlineColor = chamColor
    highlight.OutlineTransparency = 0
    highlight.DepthMode = Enum.HighlightDepthMode.AlwaysOnTop
    highlight.Parent = character
end
local function removeTracerESP(player)
    if S.tracerESPObjects[player] then
        pcall(function() S.tracerESPObjects[player]:Remove() end)
        S.tracerESPObjects[player] = nil
    end
end
local function createTracerESP(player)
    if player == LocalPlayer then return end
    removeTracerESP(player)
    local line = Drawing.new("Line")
    line.Visible = false
    line.Thickness = 1.8
    line.Transparency = 1
    local conn
    conn = RunService.RenderStepped:Connect(function()
        if not S.tracerESPEnabled then
            line.Visible = false
            return
        end
        local myChar = LocalPlayer.Character
        local char = player.Character
        if not myChar or not char or not isAlive(char) then
            line.Visible = false
            return
        end
        local hrp = char:FindFirstChild("HumanoidRootPart")
        if not hrp then
            line.Visible = false
            return
        end
        local pos, onScreen = Camera:WorldToViewportPoint(hrp.Position)
        if not onScreen then
            line.Visible = false
            return
        end
        line.From = Vector2.new(Camera.ViewportSize.X / 2, Camera.ViewportSize.Y - 8)
        line.To = Vector2.new(pos.X, pos.Y)
        line.Color = getTeamColor(player) ~= S.ESP_COLOR and getTeamColor(player) or S.ESP_TRACER_COLOR
        line.Visible = true
    end)
    S.tracerESPObjects[player] = {
        Line = line,
        Connection = conn
    }
end
local function removeHeadHitbox(character)
    if not character then return end
    S.processedChars[character] = nil
    removeOutlineChams(character)
    local head = character:FindFirstChild("Head")
    if not head then return end
    local origSize = head:FindFirstChild("OriginalSize")
    if origSize then
        head.Size = origSize.Value
        origSize:Destroy()
    end
    local origColor = head:FindFirstChild("OriginalColor")
    if origColor then
        head.Color = origColor.Value
        origColor:Destroy()
    end
    local meshBackup = head:FindFirstChild("__MeshBackup")
    if meshBackup then
        local realMesh = meshBackup:FindFirstChildOfClass("SpecialMesh") or meshBackup:FindFirstChildOfClass("BlockMesh")
        if realMesh then
            realMesh:Clone().Parent = head
        end
        meshBackup:Destroy()
    end
    head.Transparency = 0
    head.CanCollide = false
    head.Material = Enum.Material.Plastic
    head.Massless = false
end
local function applyHeadHitbox(player, character, size)
    if not player or not character then return end
    local humanoid = character:FindFirstChildOfClass("Humanoid")
    if not humanoid or humanoid.Health <= 0 then
        if S.processedChars[character] then
            removeHeadHitbox(character)
        end
        return
    end
    local head = character:FindFirstChild("Head")
    if not head then return end
    local teamColor = getTeamColor(player)
    local targetSize = Vector3.new(size, size, size)
    if humanoid.Health <= 0 then
        if S.processedChars[character] then
            removeHeadHitbox(character)
        end
        return
    end
    if not head:FindFirstChild("OriginalSize") then
        local sv = Instance.new("Vector3Value")
        sv.Name = "OriginalSize"
        sv.Value = head.Size
        sv.Parent = head
    end
    if not head:FindFirstChild("OriginalColor") then
        local cv = Instance.new("Color3Value")
        cv.Name = "OriginalColor"
        cv.Value = head.Color
        cv.Parent = head
    end
    local meshBackup = head:FindFirstChild("__MeshBackup")
    if not meshBackup then
        local existingMesh = head:FindFirstChildOfClass("SpecialMesh") or head:FindFirstChildOfClass("BlockMesh")
        if existingMesh then
            local folder = Instance.new("Folder")
            folder.Name = "__MeshBackup"
            folder.Parent = head
            existingMesh:Clone().Parent = folder
            existingMesh:Destroy()
        end
    else
        local strayMesh = head:FindFirstChildOfClass("SpecialMesh") or head:FindFirstChildOfClass("BlockMesh")
        if strayMesh then
            strayMesh:Destroy()
        end
    end
    if humanoid.Health <= 0 then
        removeHeadHitbox(character)
        return
    end
    local needsRefresh =
        head.Size ~= targetSize
        or head.Transparency < 0.45 or head.Transparency > 0.55
        or head.Material ~= Enum.Material.ForceField
        or head.CanCollide ~= false
        or head.Massless ~= true
        or head.Color ~= teamColor
    if needsRefresh then
        head.Massless = true
        head.Size = targetSize
        head.CanCollide = false
        head.Transparency = 0.5
        head.Material = Enum.Material.ForceField
        head.Color = teamColor
    end
    if not character:FindFirstChild("StarCrowOutline") then
        addOutlineChams(character)
    end
    if not character:FindFirstChild("__HeadHitboxDeathHook") then
        local tag = Instance.new("BoolValue")
        tag.Name = "__HeadHitboxDeathHook"
        tag.Parent = character
        humanoid.Died:Connect(function()
            if character and character.Parent then
                removeHeadHitbox(character)
            end
        end)
        humanoid:GetPropertyChangedSignal("Health"):Connect(function()
            if humanoid.Health <= 0 then
                if character and character.Parent then
                    removeHeadHitbox(character)
                end
            end
        end)
        task.spawn(function()
            while true do
                task.wait(0.5)
                if not character or not character.Parent then
                    task.wait(2)
                    if not character or not character.Parent then break end
                end
                if not S.autoHitboxEnabled then break end
                if not S.processedChars[character] then break end
                local currentHumanoid = character:FindFirstChildOfClass("Humanoid")
                if not currentHumanoid or currentHumanoid.Health <= 0 then break end
                if not character:FindFirstChild("StarCrowOutline") then
                    addOutlineChams(character)
                end
            end
        end)
    end
    S.processedChars[character] = true
end
local function applyBodyHitbox(character, size)
    if not character then return end
    local humanoid = character:FindFirstChildOfClass("Humanoid")
    if not humanoid or humanoid.Health <= 0 then return end
    local hrp = character:FindFirstChild("HumanoidRootPart")
    if not hrp then return end
    pcall(function()
        hrp.Size = Vector3.new(size, size, size)
        hrp.CanCollide = false
        hrp.Transparency = 0.5
        hrp.Material = Enum.Material.ForceField
        for _, part in pairs(character:GetDescendants()) do
            if part:IsA("BasePart") and part ~= hrp then
                part.CanCollide = false
            end
        end
    end)
    S.bodyHitboxChars[character] = true
    addOutlineChams(character)
end
local function resetBodyHitbox(character)
    if not character then return end
    local hrp = character:FindFirstChild("HumanoidRootPart")
    if not hrp then return end
    pcall(function()
        hrp.Size = Vector3.new(2, 2, 1)
        hrp.Transparency = 1
        hrp.CanCollide = false
        hrp.Material = Enum.Material.Plastic
    end)
    S.bodyHitboxChars[character] = nil
    removeOutlineChams(character)
end
local function removeNameESP(player)
    if S.nameESPObjects[player] then
        pcall(function() S.nameESPObjects[player]:Destroy() end)
        S.nameESPObjects[player] = nil
    end
    pcall(function()
        local char = player.Character
        if char then
            local hum = char:FindFirstChildOfClass("Humanoid")
            if hum then
                hum.DisplayDistanceType = Enum.HumanoidDisplayDistanceType.Automatic
                hum.HealthDisplayType   = Enum.HumanoidHealthDisplayType.DisplayWhenDamaged
            end
        end
    end)
end
local function createNameESP(player, char)
    if player == LocalPlayer then return end
    if not char then return end
    local head = char:FindFirstChild("Head")
    if not head then return end
    removeNameESP(player)
    local billboard = Instance.new("BillboardGui")
    billboard.Name = "NameESP"
    billboard.Adornee = head
    billboard.Size = UDim2.new(0, 240, 0, 40)
    billboard.StudsOffset = Vector3.new(0, 2.5, 0)
    billboard.AlwaysOnTop = true
    billboard.MaxDistance = math.huge
    billboard.Parent = head
    pcall(function()
        local hum = char:FindFirstChildOfClass("Humanoid")
        if hum then
            hum.DisplayDistanceType = Enum.HumanoidDisplayDistanceType.None
            hum.HealthDisplayType   = Enum.HumanoidHealthDisplayType.AlwaysOff
        end
    end)
    local infoLabel = Instance.new("TextLabel")
    infoLabel.Size = UDim2.new(1, 0, 1, 0)
    infoLabel.Position = UDim2.new(0, 0, 0, 0)
    infoLabel.BackgroundTransparency = 1
    infoLabel.RichText = true
    infoLabel.Text = ""
    infoLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
    infoLabel.TextStrokeTransparency = 0
   infoLabel.TextStrokeColor3 = Color3.fromRGB(0, 0, 0)
    infoLabel.Font = Enum.Font.GothamBold
    infoLabel.TextSize = 13
    infoLabel.TextXAlignment = Enum.TextXAlignment.Center
    infoLabel.Parent = billboard
    local conn
    conn = RunService.Heartbeat:Connect(function()
        if not billboard or not billboard.Parent then
            conn:Disconnect(); return
        end
        if not isAlive(char) then
            billboard.Enabled = false; return
        end
        billboard.Enabled = true
        if LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("Head") and head and head.Parent then
            local dist = math.floor((head.Position - LocalPlayer.Character.Head.Position).Magnitude)
            local humanoid = char:FindFirstChildOfClass("Humanoid")
            local hpPercent = 100
            local hpReal = 100
            local hpColor = "#00ff00"
            if humanoid then
                hpReal = math.floor(humanoid.Health)
                hpPercent = humanoid.MaxHealth > 0 and (humanoid.Health / humanoid.MaxHealth) * 100 or 0
                if hpPercent > 60 then
                    hpColor = "#00ff00"
                elseif hpPercent > 30 then
                    hpColor = "#ffaa00"
                else
                    hpColor = "#ff3300"
                end
            end
            local nc = getTeamColor(player)
            local nameHex = string.format("#%02x%02x%02x",
                math.clamp(math.floor(nc.R * 255), 0, 255),
                math.clamp(math.floor(nc.G * 255), 0, 255),
                math.clamp(math.floor(nc.B * 255), 0, 255)
            )
            local parts = {}
            local nameStr = ""
            if S.espShowName then
                nameStr = string.format('<font color="%s">%s</font>', nameHex, player.Name)
            end
            if S.espShowDist then
                if nameStr ~= "" then
                    nameStr = nameStr .. string.format(' <font color="rgb(180,180,180)" size="11">[%dm]</font>', dist)
                else
                    nameStr = string.format('<font color="rgb(180,180,180)" size="11">[%dm]</font>', dist)
                end
            end
            if nameStr ~= "" then
                parts[#parts + 1] = nameStr
            end
            if S.espShowHp then
                parts[#parts + 1] = string.format('<font color="%s">%d HP</font>', hpColor, hpReal)
            end
            infoLabel.Text = table.concat(parts, "\n")
        end
    end)
    S.nameESPObjects[player] = billboard
end
local function cleanupPlayerConnections(player)
    if S.playerConnections[player] then
        for _, conn in ipairs(S.playerConnections[player]) do
            pcall(function() conn:Disconnect() end)
        end
        S.playerConnections[player] = nil
    end
end
local function onCharacterReady(player, character)
    if not character then return end
    local humanoid = character:WaitForChild("Humanoid", 10)
    local head     = character:WaitForChild("Head", 10)
    local hrp      = character:WaitForChild("HumanoidRootPart", 10)
    if not humanoid or not head or not hrp then return end
    repeat task.wait(0.1) until humanoid.Health > 0 or not character.Parent
    if not character.Parent or humanoid.Health <= 0 then return end
    local size = tonumber(S.sizeInput and S.sizeInput.Text) or S.DEFAULT_HITBOX_SIZE
    if S.autoHitboxEnabled    then pcall(applyHeadHitbox,  player, character, size)          end
    if S.autoBodyHitboxEnabled then pcall(applyBodyHitbox, character, size)                  end
    if S.boxESPEnabled         then pcall(addBoxESP, character, getTeamColor(player), size)  end
    if S.espShowName or S.espShowDist or S.espShowHp or (S.nameESPEnabled and S.selectedPlayers[player]) then
        pcall(createNameESP, player, character)
    end
    if S.chamsEnabled then pcall(addChams, character, getTeamColor(player)) end
    if S.tracerESPEnabled then pcall(createTracerESP, player) end
    humanoid.Died:Connect(function()
        S.processedChars[character] = nil
        removeHeadHitbox(character)
        removeOutlineChams(character)
        resetBodyHitbox(character)
        removeBoxESP(character)
        removeNameESP(player)
        removeChams(character)
        removeTracerESP(player)
        task.spawn(function()
            while character and character.Parent do
                pcall(resetBodyHitbox, character)
                task.wait(0.5)
            end
        end)
    end)
    character.AncestryChanged:Connect(function(_, parent)
        if not parent then
            S.processedChars[character] = nil
            removeHeadHitbox(character)
            removeOutlineChams(character)
            resetBodyHitbox(character)
            removeBoxESP(character)
            removeChams(character)
        end
    end)
end
local function registerPlayer(player)
    if player == LocalPlayer then return end
    if S.playerConnections[player] then return end
    S.playerConnections[player] = {}
    task.spawn(function()
        local lastChar = nil
        while player.Parent do
            local char = player.Character
            if char and char ~= lastChar then
                lastChar = char
                S.processedChars[char] = nil
                task.spawn(onCharacterReady, player, char)
            end
            task.wait(0.5)
        end
    end)
end
local function unregisterPlayer(player)
    cleanupPlayerConnections(player)
    if player.Character then
        removeHeadHitbox(player.Character)
        removeBoxESP(player.Character)
        removeChams(player.Character)
    end
    removeNameESP(player)
    removeTracerESP(player)
    S.selectedPlayers[player] = nil
end
function createTransparentButton(text, color, parent)
    local isToggle = text:match(": OFF$") or text:match(": ON$")
                  or text:match(" : OFF$") or text:match(" : ON$")
    local btn = Instance.new("TextButton")
    btn.Size = UDim2.new(1, 0, 0, 36)
    btn.BackgroundColor3 = color
    btn.BackgroundTransparency = 0.35
    btn.BorderSizePixel = 0
    btn.Font = Enum.Font.Gotham
    btn.TextSize = 14
    btn.Parent = parent
    local c = Instance.new("UICorner")
    c.CornerRadius = UDim.new(0, 8)
    c.Parent = btn
    local bgGrad = Instance.new("UIGradient")
    bgGrad.Color = ColorSequence.new({
        ColorSequenceKeypoint.new(0, Color3.fromRGB(255,255,255)),
        ColorSequenceKeypoint.new(1, Color3.fromRGB(200,200,255)),
    })
    bgGrad.Transparency = NumberSequence.new({
        NumberSequenceKeypoint.new(0, 0.1),
        NumberSequenceKeypoint.new(1, 0.25),
    })
    bgGrad.Rotation = 90
    bgGrad.Parent = btn
    if isToggle then
        btn.Text = text
        btn.TextTransparency = 1
        local cleanName = text:gsub("%s*:?%s*O[NF][F]?%s*$", "")
        local nameLabel = Instance.new("TextLabel")
        nameLabel.Size = UDim2.new(1, -58, 1, 0)
        nameLabel.Position = UDim2.new(0, 12, 0, 0)
        nameLabel.BackgroundTransparency = 1
        nameLabel.Text = cleanName
        nameLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
        nameLabel.Font = Enum.Font.GothamSemibold
        nameLabel.TextSize = 13
        nameLabel.TextXAlignment = Enum.TextXAlignment.Left
        nameLabel.TextYAlignment = Enum.TextYAlignment.Center
        nameLabel.ZIndex = btn.ZIndex + 2
        nameLabel.Parent = btn
        local track = Instance.new("Frame")
        track.Name = "ToggleTrack"
        track.Size = UDim2.new(0, 42, 0, 22)
        track.Position = UDim2.new(1, -50, 0.5, -11)
        track.BackgroundColor3 = Color3.fromRGB(38, 38, 38)
        track.BackgroundTransparency = 0.35
        track.BorderSizePixel = 0
        track.ZIndex = btn.ZIndex + 2
        track.Parent = btn
        local tc = Instance.new("UICorner")
        tc.CornerRadius = UDim.new(1, 0)
        tc.Parent = track
        local knob = Instance.new("Frame")
        knob.Name = "ToggleKnob"
        knob.Size = UDim2.new(0, 18, 0, 18)
        knob.Position = UDim2.new(0, 2, 0.5, -9)
        knob.BackgroundColor3 = Color3.fromRGB(190, 190, 190)
        knob.BorderSizePixel = 0
        knob.ZIndex = btn.ZIndex + 3
        knob.Parent = track
        local kc = Instance.new("UICorner")
        kc.CornerRadius = UDim.new(1, 0)
        kc.Parent = knob
        local TS = game:GetService("TweenService")
        local GREEN = Color3.fromRGB(50, 200, 120)
        local BTN_DEFAULT_COLOR = color
        local BTN_DEFAULT_TRANS = 0.35
        local _ignoreColorChange = false
        local function syncToggle()
            local isOn = btn.Text:find(": ON") ~= nil
            TS:Create(track, TweenInfo.new(0.15, Enum.EasingStyle.Quad), {
                BackgroundColor3 = isOn and GREEN or Color3.fromRGB(38, 38, 38),
                BackgroundTransparency = isOn and 0 or 0.35,
            }):Play()
            TS:Create(knob, TweenInfo.new(0.15, Enum.EasingStyle.Quad), {
                Position = isOn and UDim2.new(0, 22, 0.5, -9) or UDim2.new(0, 2, 0.5, -9),
                BackgroundColor3 = isOn and Color3.fromRGB(255, 255, 255) or Color3.fromRGB(190, 190, 190),
            }):Play()
            nameLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
        end
        btn:GetPropertyChangedSignal("BackgroundColor3"):Connect(function()
            if _ignoreColorChange then return end
            local c = btn.BackgroundColor3
            if c == GREEN or (c.R > 0.15 and c.G > 0.7 and c.B < 0.6) then
                _ignoreColorChange = true
                btn.BackgroundColor3 = BTN_DEFAULT_COLOR
                btn.BackgroundTransparency = BTN_DEFAULT_TRANS
                _ignoreColorChange = false
            end
        end)
        btn:GetPropertyChangedSignal("Text"):Connect(function()
            local t = btn.Text
            local n = t:gsub("%s*:?%s*O[NF][F]?%s*$", "")
            if n ~= "" then nameLabel.Text = n end
            syncToggle()
        end)
        syncToggle()
    else
        btn.Text = ""
        local nameLabel = Instance.new("TextLabel")
        nameLabel.Size = UDim2.new(1, 0, 1, 0)
        nameLabel.BackgroundTransparency = 1
        nameLabel.Text = text
        nameLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
        nameLabel.Font = Enum.Font.GothamSemibold
        nameLabel.TextSize = 13
        nameLabel.TextXAlignment = Enum.TextXAlignment.Center
        nameLabel.ZIndex = btn.ZIndex + 2
        nameLabel.Parent = btn
    end
    btn.MouseButton1Click:Connect(function()
        local s = Instance.new("Sound")
        s.SoundId = "rbxassetid://116394299154002"
        s.Volume = 0.5
        s.Parent = game:GetService("SoundService")
        s:Play()
        game:GetService("Debris"):AddItem(s, 2)
    end)
    return btn
end
function createTransparentInput(placeholder, text, parent)
    local input = Instance.new("TextBox")
    input.Size = UDim2.new(1, 0, 0, 32)
    input.PlaceholderText = placeholder
    input.Text = text
    input.BackgroundColor3 = Color3.fromRGB(60, 60, 60)
    input.BackgroundTransparency = 0.7
    input.TextColor3 = Color3.fromRGB(255, 255, 255)
    input.PlaceholderColor3 = Color3.fromRGB(180, 180, 180)
    input.Font = Enum.Font.GothamSemibold
    input.TextSize = 14
    input.BorderSizePixel = 0
    input.Parent = parent
    local c = Instance.new("UICorner")
    c.CornerRadius = UDim.new(0, 4)
    c.Parent = input
    return input
end
function createTransparentLabel(text, parent)
    local label = Instance.new("TextLabel")
    label.Size = UDim2.new(1, 0, 0, 22)
    label.BackgroundTransparency = 1
    label.Text = text
    label.TextColor3 = Color3.fromRGB(200, 200, 200)
    label.Font = Enum.Font.Gotham
    label.TextSize = 12
    label.TextXAlignment = Enum.TextXAlignment.Left
    label.Parent = parent
    return label
end
local ContextActionService = game:GetService("ContextActionService")
local function getMobileJumpButton()
    local pg = LocalPlayer:FindFirstChild("PlayerGui")
    if not pg then return nil end
    local touchGui = pg:FindFirstChild("TouchGui")
    if not touchGui then return nil end
    local frame = touchGui:FindFirstChild("TouchControlFrame")
    if not frame then return nil end
    return frame:FindFirstChild("JumpButton")
end
local function startFly()
    local char = LocalPlayer.Character
    if not char then return end
    local hrp = char:FindFirstChild("HumanoidRootPart")
    local humanoid = char:FindFirstChildOfClass("Humanoid")
    if not hrp or not humanoid then return end
    humanoid.PlatformStand = true
    local bv = Instance.new("BodyVelocity")
    bv.MaxForce = Vector3.new(9e9,9e9,9e9)
    bv.Velocity = Vector3.new()
    bv.Parent = hrp
    local bg = Instance.new("BodyGyro")
    bg.MaxTorque = Vector3.new(9e9,9e9,9e9)
    bg.P = 1e7
    bg.D = 12000
    bg.CFrame = hrp.CFrame
    bg.Parent = hrp
    S.jumpStateConnection = humanoid.StateChanged:Connect(function(_, new)
        if new == Enum.HumanoidStateType.Jumping
        or new == Enum.HumanoidStateType.Freefall then
            humanoid.PlatformStand = true
        end
    end)
    S.flyConnection = RunService.RenderStepped:Connect(function()
        local cam = workspace.CurrentCamera
        local move = Vector3.new()
        local md = humanoid.MoveDirection
        if md.Magnitude > 0 then
            local flatLook  = Vector3.new(cam.CFrame.LookVector.X, 0, cam.CFrame.LookVector.Z)
            local flatRight = Vector3.new(cam.CFrame.RightVector.X, 0, cam.CFrame.RightVector.Z)
            if flatLook.Magnitude > 0.01 and flatRight.Magnitude > 0.01 then
                local fwdAmt   = md:Dot(flatLook.Unit)
                local rightAmt = md:Dot(flatRight.Unit)
                move += cam.CFrame.LookVector  * fwdAmt
                move += cam.CFrame.RightVector * rightAmt
            end
        end
        if move.Magnitude > 0 then
            move = move.Unit
        end
        bv.Velocity = move * S.flySpeed * 50
        local look = cam.CFrame.LookVector
        bg.CFrame = CFrame.lookAt(hrp.Position, hrp.Position + look, Vector3.new(0, 1, 0))
    end)
end
local function stopFly()
    local char = LocalPlayer.Character
    if not char then return end
    local hrp = char:FindFirstChild("HumanoidRootPart")
    local humanoid = char:FindFirstChildOfClass("Humanoid")
    if humanoid then
        humanoid.PlatformStand = false
    end
    if S.jumpStateConnection then
        S.jumpStateConnection:Disconnect()
        S.jumpStateConnection = nil
    end
    if S.flyConnection then
        S.flyConnection:Disconnect()
        S.flyConnection = nil
    end
    if hrp then
        for _, v in pairs(hrp:GetChildren()) do
            if v:IsA("BodyVelocity") or v:IsA("BodyGyro") then
                v:Destroy()
            end
        end
    end
end
local function startInfJump()
    S.infJumpConnection = UserInputService.JumpRequest:Connect(function()
        if S.infJumpEnabled then
            local char = LocalPlayer.Character
            if not char then return end
            local humanoid = char:FindFirstChildOfClass("Humanoid")
            if humanoid then humanoid:ChangeState(Enum.HumanoidStateType.Jumping) end
        end
    end)
end
local function stopInfJump()
    if S.infJumpConnection then S.infJumpConnection:Disconnect(); S.infJumpConnection = nil end
end
local function startNoclip()
    S.noclipLoop = RunService.Stepped:Connect(function()
        if S.noclipEnabled then
            local char = LocalPlayer.Character
            if char then
                for _,v in pairs(char:GetDescendants()) do
                    if v:IsA("BasePart") then v.CanCollide = false end
                end
            end
        end
    end)
end
local function stopNoclip()
    if S.noclipLoop then S.noclipLoop:Disconnect(); S.noclipLoop = nil end
end
local function startFreezeBody()
    local char = LocalPlayer.Character
    if not char then return end
    local humanoid = char:FindFirstChildOfClass("Humanoid")
    if humanoid then
        S.freezeBodyPrevSpeed = humanoid.WalkSpeed
        S.freezeBodyPrevJump  = humanoid.JumpPower
        humanoid.WalkSpeed  = 0
        humanoid.JumpPower  = 0
    end
    for _, part in pairs(char:GetDescendants()) do
        if part:IsA("BasePart") then
            part.Anchored = true
        end
    end
    S.freezeBodyConnection = RunService.Heartbeat:Connect(function()
        if not S.freezeBodyEnabled then return end
        local c = LocalPlayer.Character
        if not c then return end
        local hum = c:FindFirstChildOfClass("Humanoid")
        if hum then
            hum.WalkSpeed = 0
            hum.JumpPower = 0
        end
        for _, part in pairs(c:GetDescendants()) do
            if part:IsA("BasePart") then
                part.Anchored = true
            end
        end
    end)
end
local function stopFreezeBody()
    if S.freezeBodyConnection then
        S.freezeBodyConnection:Disconnect()
        S.freezeBodyConnection = nil
    end
    local char = LocalPlayer.Character
    if not char then return end
    local humanoid = char:FindFirstChildOfClass("Humanoid")
    if humanoid then
        humanoid.WalkSpeed = S.freezeBodyPrevSpeed
        humanoid.JumpPower = S.freezeBodyPrevJump
    end
    for _, part in pairs(char:GetDescendants()) do
        if part:IsA("BasePart") then
            part.Anchored = false
        end
    end
end
S._startCameraNoclip = function()
    S.cameraNoclipEnabled = true
    S._originalOcclusion = LocalPlayer.DevCameraOcclusionMode
    RunService:BindToRenderStep("StarCrowCamNoclip", Enum.RenderPriority.Camera.Value + 1, function()
        if not S.cameraNoclipEnabled then return end
        LocalPlayer.DevCameraOcclusionMode = Enum.DevCameraOcclusionMode.Invisicam
        local char = LocalPlayer.Character
        if char then
            for _, part in ipairs(char:GetChildren()) do
                if part:IsA("BasePart") and part.Name ~= "HumanoidRootPart" then
                    part.LocalTransparencyModifier = 0
                end
            end
        end
    end)
end
S._stopCameraNoclip = function()
    S.cameraNoclipEnabled = false
    RunService:UnbindFromRenderStep("StarCrowCamNoclip")
    if S._originalOcclusion then
        LocalPlayer.DevCameraOcclusionMode = S._originalOcclusion
    end
end
local function floatCleanup()
    if S.floatLoop then S.floatLoop:Disconnect(); S.floatLoop = nil end
    if S.floatJumpConn then S.floatJumpConn:Disconnect(); S.floatJumpConn = nil end
    if S.floatPlatform then S.floatPlatform:Destroy(); S.floatPlatform = nil end
    S.isJumping = false
end
local function startFloat()
    floatCleanup()
    local char = LocalPlayer.Character
    local hrp = char and char:FindFirstChild("HumanoidRootPart")
    if not hrp then return end
    S.floatTargetY = hrp.Position.Y - 3.2
    S.floatPlatform = Instance.new("Part")
    S.floatPlatform.Size = Vector3.new(15, 0.5, 15)
    S.floatPlatform.Anchored = true
    S.floatPlatform.CanCollide = true
    S.floatPlatform.Transparency = 1
    S.floatPlatform.Parent = workspace
    S.floatLoop = RunService.Heartbeat:Connect(function()
        local c = LocalPlayer.Character
        local root = c and c:FindFirstChild("HumanoidRootPart")
        if root and S.floatPlatform and not S.isJumping then
            S.floatPlatform.CFrame = CFrame.new(root.Position.X, S.floatTargetY, root.Position.Z)
        end
    end)
    S.floatJumpConn = UserInputService.JumpRequest:Connect(function()
        if not S.floatEnabled or S.isJumping then return end
        local c = LocalPlayer.Character
        local root = c and c:FindFirstChild("HumanoidRootPart")
        local hum = c and c:FindFirstChildOfClass("Humanoid")
        if not root or not hum then return end
        S.isJumping = true
        if S.floatPlatform then S.floatPlatform.CanCollide = false end
        hum:ChangeState(Enum.HumanoidStateType.Jumping)
        task.wait(0.1)
        while root and root.Velocity.Y > 0 and S.floatEnabled do
            task.wait()
        end
        if root and hum and S.floatPlatform then
            S.floatTargetY = root.Position.Y - 3.2
            S.floatPlatform.CFrame = CFrame.new(root.Position.X, S.floatTargetY, root.Position.Z)
            S.floatPlatform.CanCollide = true
            hum:ChangeState(Enum.HumanoidStateType.Landed)
        end
        S.isJumping = false
    end)
end
local function stopFloat()
    floatCleanup()
end
local function FlingMessage(Title, Text, Time)
    game:GetService("StarterGui"):SetCore("SendNotification", {
        Title = Title,
        Text = Text,
        Duration = Time or 5
    })
end
local function SkidFling(TargetPlayer)
    local Character = LocalPlayer.Character
    local Humanoid = Character and Character:FindFirstChildOfClass("Humanoid")
    local RootPart = Humanoid and Humanoid.RootPart
    local TCharacter = TargetPlayer.Character
    if not TCharacter then return end
    local THumanoid, TRootPart, THead, Accessory, Handle
    if TCharacter:FindFirstChildOfClass("Humanoid") then
        THumanoid = TCharacter:FindFirstChildOfClass("Humanoid")
    end
    if THumanoid and THumanoid.RootPart then TRootPart = THumanoid.RootPart end
    if TCharacter:FindFirstChild("Head") then THead = TCharacter.Head end
    if TCharacter:FindFirstChildOfClass("Accessory") then Accessory = TCharacter:FindFirstChildOfClass("Accessory") end
    if Accessory and Accessory:FindFirstChild("Handle") then Handle = Accessory.Handle end
    if Character and Humanoid and RootPart then
        if RootPart.AssemblyLinearVelocity.Magnitude < 50 then
            getgenv().OldPos = RootPart.CFrame
        end
        if THumanoid and THumanoid.Sit then
        end
        if THead then
            workspace.CurrentCamera.CameraSubject = THead
        elseif Handle then
            workspace.CurrentCamera.CameraSubject = Handle
        elseif THumanoid and TRootPart then
            workspace.CurrentCamera.CameraSubject = THumanoid
        end
        if not TCharacter:FindFirstChildWhichIsA("BasePart") then return end
        local FPos = function(BasePart, Pos, Ang)
            RootPart.CFrame = CFrame.new(BasePart.Position) * Pos * Ang
            Character:SetPrimaryPartCFrame(CFrame.new(BasePart.Position) * Pos * Ang)
            RootPart.AssemblyLinearVelocity = Vector3.new(9e7, 9e7 * 10, 9e7)
            RootPart.AssemblyAngularVelocity = Vector3.new(9e8, 9e8, 9e8)
        end
        local SFBasePart = function(BasePart)
            local TimeToWait = 2
            local Time = tick()
            local Angle = 0
            repeat
                if RootPart and THumanoid then
                    if BasePart.AssemblyLinearVelocity.Magnitude < 50 then
                        Angle = Angle + 100
                        FPos(BasePart, CFrame.new(0, 1.5, 0) + THumanoid.MoveDirection * BasePart.Velocity.Magnitude / 1.25, CFrame.Angles(math.rad(Angle),0,0))
                        task.wait()
                        FPos(BasePart, CFrame.new(0, -1.5, 0) + THumanoid.MoveDirection * BasePart.Velocity.Magnitude / 1.25, CFrame.Angles(math.rad(Angle), 0, 0))
                        task.wait()
                        FPos(BasePart, CFrame.new(0, 1.5, 0) + THumanoid.MoveDirection * BasePart.Velocity.Magnitude / 1.25, CFrame.Angles(math.rad(Angle),0,0))
                        task.wait()
                        FPos(BasePart, CFrame.new(0, -1.5, 0) + THumanoid.MoveDirection * BasePart.Velocity.Magnitude / 1.25, CFrame.Angles(math.rad(Angle), 0, 0))
                        task.wait()
                        FPos(BasePart, CFrame.new(0, 1.5, 0) + THumanoid.MoveDirection, CFrame.Angles(math.rad(Angle),0,0))
                        task.wait()
                        FPos(BasePart, CFrame.new(0, -1.5, 0) + THumanoid.MoveDirection, CFrame.Angles(math.rad(Angle), 0, 0))
                        task.wait()
                    else
                        FPos(BasePart, CFrame.new(0, 1.5, THumanoid.WalkSpeed), CFrame.Angles(math.rad(90), 0, 0))
                        task.wait()
                        FPos(BasePart, CFrame.new(0, -1.5, -THumanoid.WalkSpeed), CFrame.Angles(0, 0, 0))
                        task.wait()
                        FPos(BasePart, CFrame.new(0, 1.5, THumanoid.WalkSpeed), CFrame.Angles(math.rad(90), 0, 0))
                        task.wait()
                        FPos(BasePart, CFrame.new(0, -1.5, 0), CFrame.Angles(math.rad(90), 0, 0))
                        task.wait()
                        FPos(BasePart, CFrame.new(0, -1.5, 0), CFrame.Angles(0, 0, 0))
                        task.wait()
                        FPos(BasePart, CFrame.new(0, -1.5, 0), CFrame.Angles(math.rad(90), 0, 0))
                        task.wait()
                        FPos(BasePart, CFrame.new(0, -1.5, 0), CFrame.Angles(0, 0, 0))
                        task.wait()
                    end
                end
            until Time + TimeToWait < tick() or not S.FlingActive
        end
        workspace.FallenPartsDestroyHeight = 0/0
        local BV = Instance.new("BodyVelocity")
        BV.Parent = RootPart
        BV.MaxForce = Vector3.new(9e9, 9e9, 9e9)
        Humanoid:SetStateEnabled(Enum.HumanoidStateType.Seated, false)
        if TRootPart then
            SFBasePart(TRootPart)
        elseif THead then
            SFBasePart(THead)
        elseif Handle then
            SFBasePart(Handle)
        else
        end
        BV:Destroy()
        Humanoid:SetStateEnabled(Enum.HumanoidStateType.Seated, true)
        workspace.CurrentCamera.CameraSubject = Humanoid
        if getgenv().OldPos then
            repeat
                RootPart.CFrame = getgenv().OldPos * CFrame.new(0, .5, 0)
                Character:SetPrimaryPartCFrame(getgenv().OldPos * CFrame.new(0, .5, 0))
                Humanoid:ChangeState("GettingUp")
                for _, part in pairs(Character:GetChildren()) do
                    if part:IsA("BasePart") then
                        part.AssemblyLinearVelocity = Vector3.new()
                        part.AssemblyAngularVelocity = Vector3.new()
                    end
                end
                task.wait()
            until (RootPart.Position - getgenv().OldPos.p).Magnitude < 25
            workspace.FallenPartsDestroyHeight = getgenv().FPDH
        end
    else
    end
end
local function toggleWalkFling()
    S.walkflinging = not S.walkflinging
    if S.walkflinging then
        local character = S.speaker.Character
        local humanoid = character and character:FindFirstChildWhichIsA("Humanoid")
        if S.walkFlingHumanoidDiedConn then
            S.walkFlingHumanoidDiedConn:Disconnect()
            S.walkFlingHumanoidDiedConn = nil
        end
        if humanoid then
            S.walkFlingHumanoidDiedConn = humanoid.Died:Connect(function()
                S.walkflinging = false
                if S.walkFlingBtn then
                    S.walkFlingBtn.Text = "WALK FLING : OFF"
                    S.walkFlingBtn.BackgroundColor3 = Color3.fromRGB(38, 38, 38)
                    S.walkFlingBtn.BackgroundTransparency = 0.35
                end
            end)
        end
        task.spawn(function()
            local movel = 0.1
            while S.walkflinging do
                RunService.Heartbeat:Wait()
                local char = S.speaker.Character
                local root = char and char:FindFirstChild("HumanoidRootPart")
                if char and root and root.Parent then
                    local vel = root.Velocity
                    root.Velocity = vel * 10000 + Vector3.new(0, 10000, 0)
                    RunService.RenderStepped:Wait()
                    if root and root.Parent then
                        root.Velocity = vel
                    end
                    RunService.Stepped:Wait()
                    if root and root.Parent then
                        root.Velocity = vel + Vector3.new(0, movel, 0)
                        movel = movel * -1
                    end
                end
            end
        end)
    else
        if S.walkFlingHumanoidDiedConn then
            S.walkFlingHumanoidDiedConn:Disconnect()
            S.walkFlingHumanoidDiedConn = nil
        end
    end
end
local function enableAntiFling()
    if S.antiFlingConnection then S.antiFlingConnection:Disconnect(); S.antiFlingConnection = nil end
    S.antiFlingConnection = RunService.Stepped:Connect(function()
        local myChar = LocalPlayer.Character
        if not myChar then return end
        for _, player in ipairs(Players:GetPlayers()) do
            if player ~= LocalPlayer then
                local otherChar = player.Character
                if otherChar then
                    local hrp = otherChar:FindFirstChild("HumanoidRootPart")
                    for _, part in ipairs(otherChar:GetChildren()) do
                        if part:IsA("BasePart") then
                            part.CanCollide = false
                        end
                    end
                    if hrp and not hrp.Anchored then
                        hrp.AssemblyLinearVelocity = Vector3.zero
                        hrp.AssemblyAngularVelocity = Vector3.zero
                    end
                end
            end
        end
    end)
end
local function disableAntiFling()
    if S.antiFlingConnection then S.antiFlingConnection:Disconnect(); S.antiFlingConnection = nil end
end
local function _mkCorner(radius, parent)
    local c = Instance.new("UICorner")
    c.CornerRadius = UDim.new(0, radius)
    c.Parent = parent
end
local function _mkStroke(color, thickness, parent)
    local s = Instance.new("UIStroke")
    s.Color = color
    s.Thickness = thickness
    s.Parent = parent
end
local old = LocalPlayer.PlayerGui:FindFirstChild("Gui")
if old then
    old:Destroy()
end
local gui = Instance.new("ScreenGui")
gui.Name = "Gui"
gui.ResetOnSpawn = false
gui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
gui.DisplayOrder = 999999
gui.IgnoreGuiInset = true
if LocalPlayer:FindFirstChild("PlayerGui") then
    local old = LocalPlayer.PlayerGui:FindFirstChild("Mobile_ESP_GUI_Fixed")
    if old then old:Destroy() end
end
pcall(function()
    gui.Parent = game:GetService("CoreGui")
end)
if not gui.Parent then
    gui.Parent = LocalPlayer:WaitForChild("PlayerGui")
end
local function showCustomNotif(text, duration, useCheck)
    duration = duration or 3.5
    local TweenService = game:GetService("TweenService")
    local parent
    pcall(function() parent = game:GetService("CoreGui") end)
    if not parent then parent = LocalPlayer:FindFirstChildOfClass("PlayerGui") end
    if not parent then return end
    local notifGui = parent:FindFirstChild("SCNotifGui_StarCrow")
    if not notifGui then
        notifGui = Instance.new("ScreenGui")
        notifGui.Name = "SCNotifGui_StarCrow"
        notifGui.ResetOnSpawn = false
        notifGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
        notifGui.DisplayOrder = 999998
        notifGui.IgnoreGuiInset = true
        local ok2 = pcall(function() notifGui.Parent = game:GetService("CoreGui") end)
        if not ok2 or not notifGui.Parent then
            notifGui.Parent = LocalPlayer:FindFirstChildOfClass("PlayerGui")
        end
    end
    if not notifGui or not notifGui.Parent then return end
    local container = notifGui:FindFirstChild("NotifContainer")
    if not container then
        container = Instance.new("Frame")
        container.Name = "NotifContainer"
        container.Size = UDim2.new(0, 260, 1, 0)
        container.Position = UDim2.new(1, -270, 0, 0)
        container.BackgroundTransparency = 1
        container.BorderSizePixel = 0
        container.Parent = notifGui
        local layout = Instance.new("UIListLayout")
        layout.VerticalAlignment = Enum.VerticalAlignment.Bottom
        layout.SortOrder = Enum.SortOrder.LayoutOrder
        layout.Padding = UDim.new(0, 8)
        layout.Parent = container
        local pad = Instance.new("UIPadding")
        pad.PaddingBottom = UDim.new(0, 24)
        pad.Parent = container
    end
    local notifFrame = Instance.new("Frame")
    notifFrame.Name = "NotifCard"
    notifFrame.Size = UDim2.new(1, 0, 0, 76)
    notifFrame.BackgroundColor3 = Color3.fromRGB(18, 18, 24)
    notifFrame.BackgroundTransparency = 0.35
    notifFrame.BorderSizePixel = 0
    notifFrame.ClipsDescendants = true
    notifFrame.Position = UDim2.new(2, 0, 0, 0)
    notifFrame.Parent = container
local blur = Instance.new("UIGradient")
blur.Color = ColorSequence.new({
    ColorSequenceKeypoint.new(0, Color3.fromRGB(255,255,255)),
    ColorSequenceKeypoint.new(1, Color3.fromRGB(200,200,255))
})
blur.Transparency = NumberSequence.new({
    NumberSequenceKeypoint.new(0, 0.9),
    NumberSequenceKeypoint.new(1, 1)
})
blur.Rotation = 90
blur.Parent = notifFrame
    local corner = Instance.new("UICorner")
    corner.CornerRadius = UDim.new(0, 8)
    corner.Parent = notifFrame
    local stroke = Instance.new("UIStroke")
stroke.Thickness = 2
stroke.Transparency = 0
stroke.ApplyStrokeMode = Enum.ApplyStrokeMode.Border
stroke.Color = Color3.fromRGB(255, 255, 255)
stroke.Parent = notifFrame
local gradient = Instance.new("UIGradient")
gradient.Color = ColorSequence.new({
    ColorSequenceKeypoint.new(0, Color3.fromRGB(170, 70, 255)),
    ColorSequenceKeypoint.new(0.5, Color3.fromRGB(0, 180, 255)),
    ColorSequenceKeypoint.new(1, Color3.fromRGB(170, 70, 255))
})
gradient.Rotation = 0
gradient.Parent = stroke
task.spawn(function()
    local TweenService = game:GetService("TweenService")
    local tweenInfo = TweenInfo.new(4, Enum.EasingStyle.Linear, Enum.EasingDirection.InOut, -1)
    local tween = TweenService:Create(gradient, tweenInfo, {Rotation = 360})
    tween:Play()
end)
    local icon = Instance.new("ImageLabel")
    if useCheck == true then
        icon.Size = UDim2.new(0, 20, 0, 20)
        icon.Position = UDim2.new(0, 14, 0, 10)
    elseif useCheck == "fail" then
        icon.Size = UDim2.new(0, 28, 0, 28)
        icon.Position = UDim2.new(0, 10, 0, 6)
    else
        icon.Size = UDim2.new(0, 40, 0, 40)
        icon.Position = UDim2.new(0, 14, 0.5, -20)
    end
    icon.BackgroundTransparency = 1
    if useCheck == true then
        icon.Image = "rbxassetid://13132087769"
    elseif useCheck == "fail" then
        icon.Image = "rbxassetid://5276992751"
    else
        icon.Image = "rbxassetid://73309457774546"
    end
    icon.ZIndex = 2
    icon.Parent = notifFrame
if useCheck == true or useCheck == "fail" then
        local titleLabel = Instance.new("TextLabel")
        titleLabel.Size = UDim2.new(1, -44, 0, 24)
        titleLabel.Position = UDim2.new(0, 40, 0, 7)
        titleLabel.BackgroundTransparency = 1
        titleLabel.Text = "StarCrow Says:"
        titleLabel.TextXAlignment = Enum.TextXAlignment.Left
        titleLabel.TextYAlignment = Enum.TextYAlignment.Center
        titleLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
        titleLabel.Font = Enum.Font.GothamBold
        titleLabel.TextSize = 15
        titleLabel.ZIndex = 2
        titleLabel.Parent = notifFrame
        local titleRainbow = Instance.new("UIGradient")
        titleRainbow.Color = ColorSequence.new({
            ColorSequenceKeypoint.new(0, Color3.fromRGB(210, 60, 255)),
            ColorSequenceKeypoint.new(1, Color3.fromRGB(0, 210, 255)),
        })
        titleRainbow.Rotation = 0
        titleRainbow.Parent = titleLabel
        task.spawn(function()
            TweenService:Create(titleRainbow, TweenInfo.new(3, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), { Rotation = 90 }):Play()
        end)
        local rawParts = string.split(text, "\n")
        local bodyText = ""
        for i = 2, #rawParts do
            local trimmed = rawParts[i]:match("^%s*(.-)%s*$")
            if trimmed and trimmed ~= "" then
                bodyText = trimmed
                break
            end
        end
        local bodyLabel = Instance.new("TextLabel")
        bodyLabel.Size = UDim2.new(1, -20, 0, 28)
        bodyLabel.Position = UDim2.new(0, 14, 0, 36)
        bodyLabel.BackgroundTransparency = 1
        bodyLabel.Text = bodyText
        bodyLabel.TextXAlignment = Enum.TextXAlignment.Left
        bodyLabel.TextYAlignment = Enum.TextYAlignment.Top
        bodyLabel.TextColor3 = Color3.fromRGB(220, 220, 220)
        bodyLabel.Font = Enum.Font.GothamSemibold
        bodyLabel.TextSize = 13
        bodyLabel.TextWrapped = true
        bodyLabel.ZIndex = 2
        bodyLabel.Parent = notifFrame
    else
        local label = Instance.new("TextLabel")
        label.Size = UDim2.new(1, -64, 1, -12)
        label.Position = UDim2.new(0, 60, 0, 4)
        label.BackgroundTransparency = 1
        label.Text = text
        label.TextXAlignment = Enum.TextXAlignment.Left
        label.TextColor3 = Color3.fromRGB(255, 255, 255)
        label.Font = Enum.Font.GothamSemibold
        label.TextSize = 14
        label.TextWrapped = true
        label.TextXAlignment = Enum.TextXAlignment.Left
        label.TextYAlignment = Enum.TextYAlignment.Center
        label.ZIndex = 2
        label.Parent = notifFrame
    end
    local timerTrack = Instance.new("Frame")
    timerTrack.Name = "TimerTrack"
    timerTrack.Size = UDim2.new(1, -16, 0, 3)
    timerTrack.Position = UDim2.new(0, 8, 1, -7)
    timerTrack.BackgroundColor3 = Color3.fromRGB(10, 35, 55)
    timerTrack.BackgroundTransparency = 0
    timerTrack.BorderSizePixel = 0
    timerTrack.ZIndex = 5
    timerTrack.ClipsDescendants = true
    timerTrack.Parent = notifFrame
    local trackCorner = Instance.new("UICorner")
    trackCorner.CornerRadius = UDim.new(1, 0)
    trackCorner.Parent = timerTrack
    local timerFill = Instance.new("Frame")
    timerFill.Name = "TimerFill"
    timerFill.Size = UDim2.new(1, 0, 1, 0)
    timerFill.Position = UDim2.new(0, 0, 0, 0)
    timerFill.BackgroundColor3 = Color3.fromRGB(0, 180, 255)
    timerFill.BackgroundTransparency = 0
    timerFill.BorderSizePixel = 0
    timerFill.ZIndex = 6
    timerFill.Parent = timerTrack
    local fillCorner = Instance.new("UICorner")
    fillCorner.CornerRadius = UDim.new(1, 0)
    fillCorner.Parent = timerFill
    notifFrame.Position = UDim2.new(2, 0, 0, 0)
    TweenService:Create(notifFrame, TweenInfo.new(0.35, Enum.EasingStyle.Back, Enum.EasingDirection.Out), {
        Position = UDim2.new(0, 0, 0, 0)
    }):Play()
    task.delay(0.35, function()
        if timerFill and timerFill.Parent then
            TweenService:Create(timerFill, TweenInfo.new(duration, Enum.EasingStyle.Linear, Enum.EasingDirection.Out), {
                Size = UDim2.new(0, 0, 1, 0)
            }):Play()
        end
    end)
    task.delay(duration, function()
        if not notifFrame or not notifFrame.Parent then return end
        local tw = TweenService:Create(notifFrame, TweenInfo.new(0.28, Enum.EasingStyle.Quad, Enum.EasingDirection.In), {
            Position = UDim2.new(2, 0, 0, 0)
        })
        tw:Play()
        tw.Completed:Connect(function()
            if notifFrame and notifFrame.Parent then notifFrame:Destroy() end
        end)
    end)
end
local toggleBtn = Instance.new("TextButton")
toggleBtn.Name = "OpenMenuButton"
toggleBtn.Size = UDim2.new(0, 190, 0, 38)
toggleBtn.Position = UDim2.new(0.5, -95, 0, 72)
toggleBtn.BackgroundColor3 = Color3.fromRGB(28, 28, 28)
toggleBtn.BackgroundTransparency = 0.42
toggleBtn.BorderSizePixel = 0
toggleBtn.Text = ""
toggleBtn.ZIndex = 5
toggleBtn.Active = true
toggleBtn.AutoButtonColor = false
toggleBtn.Parent = gui
do
    local corner = Instance.new("UICorner")
    corner.CornerRadius = UDim.new(0, 6)
    corner.Parent = toggleBtn
    local strokeFrame = Instance.new("Frame")
    strokeFrame.Name = "StrokeFrame"
    strokeFrame.Size = UDim2.new(1, 0, 1, 0)
    strokeFrame.Position = UDim2.new(0, 0, 0, 0)
    strokeFrame.BackgroundTransparency = 1
    strokeFrame.BorderSizePixel = 0
    strokeFrame.ZIndex = 6
    strokeFrame.Parent = toggleBtn
    local strokeCorner = Instance.new("UICorner")
    strokeCorner.CornerRadius = UDim.new(0, 6)
    strokeCorner.Parent = strokeFrame
    local stroke = Instance.new("UIStroke")
    stroke.Color = Color3.fromRGB(255, 255, 255)
    stroke.Thickness = 2
    stroke.Transparency = 0
    stroke.ApplyStrokeMode = Enum.ApplyStrokeMode.Border
    stroke.Parent = strokeFrame
    local strokeGradient = Instance.new("UIGradient")
    strokeGradient.Color = ColorSequence.new({
        ColorSequenceKeypoint.new(0, Color3.fromRGB(170, 70, 255)),
        ColorSequenceKeypoint.new(0.5, Color3.fromRGB(0, 180, 255)),
        ColorSequenceKeypoint.new(1, Color3.fromRGB(170, 70, 255))
    })
    strokeGradient.Rotation = 0
    strokeGradient.Parent = stroke
    task.spawn(function()
        local TweenService = game:GetService("TweenService")
        local tweenInfo = TweenInfo.new(5, Enum.EasingStyle.Linear, Enum.EasingDirection.InOut, -1)
        local tween = TweenService:Create(strokeGradient, tweenInfo, {Rotation = 360})
        tween:Play()
    end)
    local dragIcon = Instance.new("ImageLabel")
    dragIcon.Name = "DragIcon"
    dragIcon.Size = UDim2.new(0, 17, 0, 17)
    dragIcon.Position = UDim2.new(0, 8, 0.5, -8)
    dragIcon.BackgroundTransparency = 1
    dragIcon.Image = "rbxassetid://6710235139"
    dragIcon.ImageTransparency = 0.12
    dragIcon.ScaleType = Enum.ScaleType.Fit
    dragIcon.ZIndex = 7
    dragIcon.Parent = toggleBtn
    local divider = Instance.new("Frame")
    divider.Name = "Divider"
    divider.Size = UDim2.new(0, 1, 1, 0)
    divider.Position = UDim2.new(0, 32, 0, 0)
    divider.BackgroundColor3 = Color3.fromRGB(200, 200, 200)
    divider.BackgroundTransparency = 0.58
    divider.BorderSizePixel = 0
    divider.ZIndex = 7
    divider.Parent = toggleBtn
    local sideIcon = Instance.new("ImageLabel")
    sideIcon.Name = "SideIcon"
    sideIcon.Size = UDim2.new(0, 22, 0, 22)
    sideIcon.Position = UDim2.new(0, 35, 0.5, -11)
    sideIcon.BackgroundTransparency = 1
    sideIcon.Image = "rbxassetid://11400928498"
    sideIcon.ImageTransparency = 0.04
    sideIcon.ScaleType = Enum.ScaleType.Fit
    sideIcon.ZIndex = 8
    sideIcon.Parent = toggleBtn
    local nameLabel = Instance.new("TextLabel")
    nameLabel.Name = "NameLabel"
    nameLabel.Size = UDim2.new(0, 104, 1, 0)
    nameLabel.Position = UDim2.new(0, 68, 0, 0)
    nameLabel.BackgroundTransparency = 1
    nameLabel.Text = "Open StarCrow"
    nameLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
    nameLabel.Font = Enum.Font.GothamSemibold
    nameLabel.TextSize = 14
    nameLabel.TextXAlignment = Enum.TextXAlignment.Center
    nameLabel.TextYAlignment = Enum.TextYAlignment.Center
    nameLabel.ZIndex = 8
    nameLabel.Parent = toggleBtn
end
local _tbDragging = false
local _tbMoved = false
local _tbIsTouch = false
local _tbDragInput = nil
local _tbDragStart = nil
local _tbDragPos = nil
toggleBtn.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 then
        _tbDragging = true
        _tbMoved = false
        _tbIsTouch = false
        _tbDragInput = nil
        _tbDragStart = input.Position
        _tbDragPos = toggleBtn.Position
    elseif input.UserInputType == Enum.UserInputType.Touch then
        _tbDragging = true
        _tbMoved = false
        _tbIsTouch = true
        _tbDragInput = input
        _tbDragStart = input.Position
        _tbDragPos = toggleBtn.Position
        input.Changed:Connect(function()
            if input.UserInputState == Enum.UserInputState.End then
                _tbDragging = false
                _tbDragInput = nil
            end
        end)
    end
end)
toggleBtn.InputEnded:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 then
        _tbDragging = false
    end
end)
UserInputService.InputChanged:Connect(function(input)
    if not _tbDragging then return end
    if not _tbIsTouch and input.UserInputType == Enum.UserInputType.MouseMovement then
        local delta = input.Position - _tbDragStart
        if delta.Magnitude > 6 then _tbMoved = true end
        toggleBtn.Position = UDim2.new(
            _tbDragPos.X.Scale, _tbDragPos.X.Offset + delta.X,
            _tbDragPos.Y.Scale, _tbDragPos.Y.Offset + delta.Y
        )
    elseif _tbIsTouch and input == _tbDragInput then
        local delta = input.Position - _tbDragStart
        if delta.Magnitude > 6 then _tbMoved = true end
        toggleBtn.Position = UDim2.new(
            _tbDragPos.X.Scale, _tbDragPos.X.Offset + delta.X,
            _tbDragPos.Y.Scale, _tbDragPos.Y.Offset + delta.Y
        )
    end
end)
local mainFrame = Instance.new("Frame")
mainFrame.Size = UDim2.new(0, 320, 0, 380)
mainFrame.Position = UDim2.new(0.5, -160, 0.5, -190)
mainFrame.BackgroundColor3 = Color3.fromRGB(40, 40, 40)
mainFrame.BackgroundTransparency = 0.7
mainFrame.BorderSizePixel = 0
mainFrame.Visible = false
mainFrame.Active = true
mainFrame.Draggable = false
mainFrame.Parent = gui
_mkCorner(8, mainFrame)
do
    local mStroke = Instance.new("UIStroke")
    mStroke.ApplyStrokeMode = Enum.ApplyStrokeMode.Border
    mStroke.Thickness = 2.5
    mStroke.Transparency = 0
    mStroke.Color = Color3.fromRGB(255, 255, 255)
    mStroke.Parent = mainFrame
    local mGrad = Instance.new("UIGradient")
    mGrad.Color = ColorSequence.new({
        ColorSequenceKeypoint.new(0,    Color3.fromRGB(140, 0, 255)),
        ColorSequenceKeypoint.new(0.25, Color3.fromRGB(0, 180, 255)),
        ColorSequenceKeypoint.new(0.5,  Color3.fromRGB(140, 0, 255)),
        ColorSequenceKeypoint.new(0.75, Color3.fromRGB(0, 180, 255)),
        ColorSequenceKeypoint.new(1,    Color3.fromRGB(140, 0, 255)),
    })
    mGrad.Rotation = 0
    mGrad.Parent = mStroke
    local shimmer = Instance.new("Frame")
    shimmer.Name = "BorderShimmer"
    shimmer.Size = UDim2.new(1, 0, 0, 60)
    shimmer.Position = UDim2.new(0, 0, -0.2, 0)
    shimmer.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
    shimmer.BackgroundTransparency = 0.82
    shimmer.BorderSizePixel = 0
    shimmer.ZIndex = mainFrame.ZIndex + 1
    shimmer.Parent = mainFrame
    _mkCorner(8, shimmer)
    local shimGrad = Instance.new("UIGradient")
    shimGrad.Transparency = NumberSequence.new({
        NumberSequenceKeypoint.new(0, 1),
        NumberSequenceKeypoint.new(0.5, 0.7),
        NumberSequenceKeypoint.new(1, 1),
    })
    shimGrad.Rotation = 90
    shimGrad.Parent = shimmer
    task.spawn(function()
        local TS = game:GetService("TweenService")
        TS:Create(mGrad, TweenInfo.new(5, Enum.EasingStyle.Linear, Enum.EasingDirection.InOut, -1), { Rotation = 360 }):Play()
        while true do
            shimmer.Position = UDim2.new(0, 0, -0.2, 0)
            shimmer.BackgroundTransparency = 0.85
            TS:Create(shimmer, TweenInfo.new(1.6, Enum.EasingStyle.Quad, Enum.EasingDirection.InOut), {
                Position = UDim2.new(0, 0, 1.1, 0),
                BackgroundTransparency = 0.85,
            }):Play()
            task.wait(3.5)
        end
    end)
end
local titleBar = Instance.new("Frame")
titleBar.Size = UDim2.new(1, 0, 0, 28)
titleBar.BackgroundColor3 = Color3.fromRGB(20, 20, 30)
titleBar.BackgroundTransparency = 0.2
titleBar.BorderSizePixel = 0
titleBar.Active = true
titleBar.Parent = mainFrame
_mkCorner(8, titleBar)
local _mfDragging = false
local _mfDragStart = nil
local _mfStartPos = nil
local _mfDragInput = nil
local function _startMenuDrag(input)
    if _mfDragging then return end
    _mfDragging = true
    _mfDragStart = input.Position
    _mfStartPos  = mainFrame.Position
    _mfDragInput = input
    input.Changed:Connect(function()
        if input.UserInputState == Enum.UserInputState.End then
            _mfDragging = false
            _mfDragInput = nil
        end
    end)
end
local function _onDragInput(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1
    or input.UserInputType == Enum.UserInputType.Touch then
        _startMenuDrag(input)
    end
end
do
    titleBar.InputBegan:Connect(_onDragInput)
    UserInputService.InputChanged:Connect(function(input)
        if not _mfDragging then return end
        if input.UserInputType == Enum.UserInputType.MouseMovement then
            local delta = input.Position - _mfDragStart
            mainFrame.Position = UDim2.new(
                _mfStartPos.X.Scale, _mfStartPos.X.Offset + delta.X,
                _mfStartPos.Y.Scale, _mfStartPos.Y.Offset + delta.Y
            )
        elseif input.UserInputType == Enum.UserInputType.Touch and input == _mfDragInput then
            local delta = input.Position - _mfDragStart
            mainFrame.Position = UDim2.new(
                _mfStartPos.X.Scale, _mfStartPos.X.Offset + delta.X,
                _mfStartPos.Y.Scale, _mfStartPos.Y.Offset + delta.Y
            )
        end
    end)
    UserInputService.InputEnded:Connect(function(input)
        if not _mfDragging then return end
        if input.UserInputType == Enum.UserInputType.MouseButton1
        or (input.UserInputType == Enum.UserInputType.Touch and input == _mfDragInput) then
            _mfDragging = false
            _mfDragInput = nil
        end
    end)
end
do
    local tbGrad = Instance.new("UIGradient")
    tbGrad.Color = ColorSequence.new({
        ColorSequenceKeypoint.new(0,   Color3.fromRGB(80, 0, 140)),
        ColorSequenceKeypoint.new(0.5, Color3.fromRGB(0, 60, 120)),
        ColorSequenceKeypoint.new(1,   Color3.fromRGB(80, 0, 140)),
    })
    tbGrad.Transparency = NumberSequence.new({
        NumberSequenceKeypoint.new(0, 0.5),
        NumberSequenceKeypoint.new(0.5, 0.3),
        NumberSequenceKeypoint.new(1, 0.5),
    })
    tbGrad.Rotation = 0
    tbGrad.Parent = titleBar
    local tbStroke = Instance.new("UIStroke")
    tbStroke.ApplyStrokeMode = Enum.ApplyStrokeMode.Border
    tbStroke.Thickness = 1
    tbStroke.Color = Color3.fromRGB(255, 255, 255)
    tbStroke.Transparency = 0
    tbStroke.Parent = titleBar
    local tbSGrad = Instance.new("UIGradient")
    tbSGrad.Color = ColorSequence.new({
        ColorSequenceKeypoint.new(0,   Color3.fromRGB(140, 0, 255)),
        ColorSequenceKeypoint.new(0.5, Color3.fromRGB(0, 180, 255)),
        ColorSequenceKeypoint.new(1,   Color3.fromRGB(140, 0, 255)),
    })
    tbSGrad.Rotation = 0
    tbSGrad.Parent = tbStroke
    task.spawn(function()
        local TS = game:GetService("TweenService")
        TS:Create(tbSGrad, TweenInfo.new(5, Enum.EasingStyle.Linear, Enum.EasingDirection.InOut, -1), { Rotation = 360 }):Play()
        TS:Create(tbGrad,  TweenInfo.new(5, Enum.EasingStyle.Linear, Enum.EasingDirection.InOut, -1), { Rotation = 360 }):Play()
    end)
end
do
    local sep = Instance.new("Frame")
    sep.Size = UDim2.new(1, -16, 0, 1)
    sep.Position = UDim2.new(0, 8, 1, -1)
    sep.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
    sep.BackgroundTransparency = 0
    sep.BorderSizePixel = 0
    sep.ZIndex = titleBar.ZIndex + 1
    sep.Parent = titleBar
    local sepGrad = Instance.new("UIGradient")
    sepGrad.Color = ColorSequence.new({
        ColorSequenceKeypoint.new(0,   Color3.fromRGB(140, 0, 255)),
        ColorSequenceKeypoint.new(0.5, Color3.fromRGB(0, 200, 255)),
        ColorSequenceKeypoint.new(1,   Color3.fromRGB(140, 0, 255)),
    })
    sepGrad.Transparency = NumberSequence.new({
        NumberSequenceKeypoint.new(0, 1),
        NumberSequenceKeypoint.new(0.15, 0),
        NumberSequenceKeypoint.new(0.85, 0),
        NumberSequenceKeypoint.new(1, 1),
    })
    sepGrad.Rotation = 0
    sepGrad.Parent = sep
    task.spawn(function()
        local TS = game:GetService("TweenService")
        TS:Create(sepGrad, TweenInfo.new(5, Enum.EasingStyle.Linear, Enum.EasingDirection.InOut, -1), { Rotation = 360 }):Play()
    end)
end
local titleLabel = Instance.new("TextLabel")
titleLabel.Size = UDim2.new(0, 76, 1, 0)
titleLabel.Position = UDim2.new(0, 8, 0, 0)
titleLabel.BackgroundTransparency = 1
titleLabel.Text = "StarCrow"
titleLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
titleLabel.Font = Enum.Font.GothamBold
titleLabel.TextSize = 13
titleLabel.TextXAlignment = Enum.TextXAlignment.Left
titleLabel.Parent = titleBar
do
    local tlGrad = Instance.new("UIGradient")
    tlGrad.Color = ColorSequence.new({
        ColorSequenceKeypoint.new(0,   Color3.fromRGB(200, 100, 255)),
        ColorSequenceKeypoint.new(1,   Color3.fromRGB(0, 210, 255)),
    })
    tlGrad.Rotation = 0
    tlGrad.Parent = titleLabel
    task.spawn(function()
        local TS = game:GetService("TweenService")
        TS:Create(tlGrad, TweenInfo.new(3, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), { Rotation = 90 }):Play()
    end)
end
local pingLabel = Instance.new("TextLabel")
pingLabel.Size = UDim2.new(0, 92, 1, 0)
pingLabel.Position = UDim2.new(0, 82, 0, 0)
pingLabel.BackgroundTransparency = 1
pingLabel.Text = "| Ping --ms"
pingLabel.TextColor3 = Color3.fromRGB(170, 170, 170)
pingLabel.Font = Enum.Font.GothamMedium
pingLabel.TextSize = 12
pingLabel.TextXAlignment = Enum.TextXAlignment.Left
pingLabel.Parent = titleBar
local fpsLabel = Instance.new("TextLabel")
fpsLabel.Size = UDim2.new(0, 70, 1, 0)
fpsLabel.Position = UDim2.new(0, 170, 0, 0)
fpsLabel.BackgroundTransparency = 1
fpsLabel.Text = "| Fps: --"
fpsLabel.TextColor3 = Color3.fromRGB(170, 170, 170)
fpsLabel.Font = Enum.Font.GothamMedium
fpsLabel.TextSize = 12
fpsLabel.TextXAlignment = Enum.TextXAlignment.Left
fpsLabel.Parent = titleBar
local function getBestPingValue()
    local ping = nil
    pcall(function()
        local stats = game:GetService("Stats")
        local network = stats:FindFirstChild("Network")
        if not network then return end
        local serverStatsItem = network:FindFirstChild("ServerStatsItem")
        if not serverStatsItem then return end
        local dataPing = serverStatsItem:FindFirstChild("Data Ping")
        if not dataPing then return end
        if typeof(dataPing.GetValue) == "function" then
            local raw = dataPing:GetValue()
            if typeof(raw) == "number" then
                ping = math.floor(raw + 0.5)
                return
            end
        end
        if typeof(dataPing.GetValueString) == "function" then
            local txt = dataPing:GetValueString()
            local n = tonumber(string.match(txt, "%d+"))
            if n then
                ping = n
            end
        end
    end)
    return ping
end
local fpsFrames = 0
local fpsValue = 0
local fpsLastTime = tick()
RunService.RenderStepped:Connect(function()
    fpsFrames += 1
    local now = tick()
    if now - fpsLastTime >= 1 then
        fpsValue = math.floor(fpsFrames / (now - fpsLastTime) + 0.5)
        fpsFrames = 0
        fpsLastTime = now
    end
end)
task.spawn(function()
    while pingLabel and pingLabel.Parent and fpsLabel and fpsLabel.Parent do
        local ping = getBestPingValue()
        if ping then
            pingLabel.Text = "| Ping " .. tostring(ping) .. "ms"
            if ping <= 200 then
                pingLabel.TextColor3 = Color3.fromRGB(70, 255, 120)
            elseif ping <= 500 then
                pingLabel.TextColor3 = Color3.fromRGB(255, 210, 90)
            else
                pingLabel.TextColor3 = Color3.fromRGB(255, 95, 95)
            end
        else
            pingLabel.Text = "| Ping --ms"
            pingLabel.TextColor3 = Color3.fromRGB(170, 170, 170)
        end
        fpsLabel.Text = "| Fps: " .. tostring(fpsValue)
        if fpsValue >= 50 then
            fpsLabel.TextColor3 = Color3.fromRGB(70, 255, 120)
        elseif fpsValue >= 30 then
            fpsLabel.TextColor3 = Color3.fromRGB(255, 210, 90)
        else
            fpsLabel.TextColor3 = Color3.fromRGB(255, 95, 95)
        end
        task.wait(0.35)
    end
end)
local closeButton = Instance.new("TextButton")
closeButton.Size = UDim2.new(0, 28, 0, 24)
closeButton.Position = UDim2.new(1, -30, 0, 2)
closeButton.Text = "X"
closeButton.TextColor3 = Color3.fromRGB(255,255,255)
closeButton.BackgroundColor3 = Color3.fromRGB(200,50,50)
closeButton.BackgroundTransparency = 0.3
closeButton.BorderSizePixel = 0
closeButton.Font = Enum.Font.GothamBold
closeButton.TextSize = 13
closeButton.Parent = mainFrame
_mkCorner(4, closeButton)
local TAB_W = 82
local sideBar = Instance.new("Frame")
sideBar.Size = UDim2.new(0, TAB_W, 1, -32)
sideBar.Position = UDim2.new(0, 0, 0, 32)
sideBar.BackgroundColor3 = Color3.fromRGB(25, 25, 25)
sideBar.BackgroundTransparency = 0.5
sideBar.BorderSizePixel = 0
sideBar.Parent = mainFrame
_mkCorner(6, sideBar)
sideBar.InputBegan:Connect(_onDragInput)
do
    local sbStroke = Instance.new("UIStroke")
    sbStroke.ApplyStrokeMode = Enum.ApplyStrokeMode.Border
    sbStroke.Thickness = 1
    sbStroke.Transparency = 0.3
    sbStroke.Color = Color3.fromRGB(255, 255, 255)
    sbStroke.Parent = sideBar
    local sbGrad = Instance.new("UIGradient")
    sbGrad.Color = ColorSequence.new({
        ColorSequenceKeypoint.new(0,   Color3.fromRGB(140, 0, 255)),
        ColorSequenceKeypoint.new(0.5, Color3.fromRGB(0, 180, 255)),
        ColorSequenceKeypoint.new(1,   Color3.fromRGB(140, 0, 255)),
    })
    sbGrad.Rotation = 90
    sbGrad.Parent = sbStroke
end
do
    local sideLayout = Instance.new("UIListLayout")
    sideLayout.SortOrder = Enum.SortOrder.LayoutOrder
    sideLayout.Padding = UDim.new(0, 4)
    sideLayout.HorizontalAlignment = Enum.HorizontalAlignment.Center
    sideLayout.Parent = sideBar
    local sidePad = Instance.new("UIPadding")
    sidePad.PaddingTop = UDim.new(0, 6)
    sidePad.PaddingLeft = UDim.new(0, 4)
    sidePad.PaddingRight = UDim.new(0, 4)
    sidePad.Parent = sideBar
end
local tabBtns = {}
do
    local tabColors = {
        Color3.fromRGB(25,25,25), Color3.fromRGB(25,25,25),
        Color3.fromRGB(25,25,25), Color3.fromRGB(25,25,25),
        Color3.fromRGB(25,25,25), Color3.fromRGB(25,25,25),
        Color3.fromRGB(25,25,25), Color3.fromRGB(25,25,25),
        Color3.fromRGB(25,25,25)
    }
    local tabNames = {"Player", "ESP", "Teleport", "Troll", "Misc", "Server", "Tools", "Aimbot", "Setting"}
    local tabIcons = {
    "rbxassetid://117259180607823",
    "rbxassetid://120129574453255",
    "rbxassetid://6723742952",
    "rbxassetid://6862780932",
    "rbxassetid://85940683783058",
    "rbxassetid://9692125126",
    "rbxassetid://484396034",
    "rbxassetid://95264995174476",
    "rbxassetid://7059346373"
}
    for i = 1, #tabNames do
        local btn = Instance.new("TextButton")
        btn.Size = UDim2.new(1, 0, 0, 30)
        btn.BackgroundColor3 = Color3.fromRGB(35, 15, 65)
        btn.BackgroundTransparency = 0.35
        btn.BorderSizePixel = 0
        btn.Text = ""
        btn.LayoutOrder = i
        btn.Parent = sideBar
        local bc = Instance.new("UICorner")
        bc.CornerRadius = UDim.new(0,6)
        bc.Parent = btn
        local tabGrad = Instance.new("UIGradient")
        tabGrad.Color = ColorSequence.new({
            ColorSequenceKeypoint.new(0,   Color3.fromRGB(55, 15, 100)),
            ColorSequenceKeypoint.new(1,   Color3.fromRGB(15, 20, 60)),
        })
        tabGrad.Rotation = 135
        tabGrad.Parent = btn
        local icon = Instance.new("ImageLabel")
icon.Name = "TabIcon"
icon.Size = UDim2.new(0, 18, 0, 18)
icon.Position = UDim2.new(0, 6, 0.5, -9)
icon.BackgroundTransparency = 1
icon.Image = tabIcons[i]
icon.ScaleType = Enum.ScaleType.Fit
icon.ImageColor3 = Color3.fromRGB(245,245,245)
icon.Parent = btn
        local label = Instance.new("TextLabel")
        label.Name = "TabLabel"
        label.BackgroundTransparency = 1
        label.Position = UDim2.new(0, 26, 0, 0)
        label.Size = UDim2.new(1, -28, 1, 0)
        label.Text = tabNames[i]
        label.TextColor3 = Color3.fromRGB(255, 255, 255)
        label.Font = Enum.Font.GothamBold
        label.TextSize = 11
        label.TextScaled = true
        local tc = Instance.new("UITextSizeConstraint")
        tc.MaxTextSize = 11
        tc.MinTextSize = 7
        tc.Parent = label
        label.TextXAlignment = Enum.TextXAlignment.Left
        label.TextYAlignment = Enum.TextYAlignment.Center
        label.ClipsDescendants = true
        label.Parent = btn
        tabBtns[i] = btn
        btn.InputBegan:Connect(_onDragInput)
    end
end
local function makeScrollPanel()
    local PANEL_W = 320 - TAB_W - 8
    local PANEL_X = TAB_W + 4
    local PANEL_Y = 32
    local PANEL_H = 380 - 32
    local panel = Instance.new("ScrollingFrame")
    panel.Size = UDim2.new(0, PANEL_W, 0, PANEL_H)
    panel.Position = UDim2.new(0, PANEL_X, 0, PANEL_Y)
    panel.BackgroundTransparency = 1
    panel.BorderSizePixel = 0
    panel.ScrollBarThickness = 4
    panel.CanvasSize = UDim2.new(0, 0, 0, 0)
    panel.Visible = false
    panel.Parent = mainFrame
    local layout = Instance.new("UIListLayout")
    layout.SortOrder = Enum.SortOrder.LayoutOrder
    layout.Padding = UDim.new(0, 4)
    layout.Parent = panel
    local pad = Instance.new("UIPadding")
    pad.PaddingTop = UDim.new(0,4)
    pad.PaddingLeft = UDim.new(0,4)
    pad.PaddingRight = UDim.new(0,4)
    pad.Parent = panel
    layout:GetPropertyChangedSignal("AbsoluteContentSize"):Connect(function()
        panel.CanvasSize = UDim2.new(0,0,0,layout.AbsoluteContentSize.Y + 14)
    end)
    return panel, layout
end
panel2 = makeScrollPanel()
panel3 = makeScrollPanel()
panel4 = makeScrollPanel()
panel5 = makeScrollPanel()
panel6 = makeScrollPanel()
panel7 = makeScrollPanel()
panel8 = makeScrollPanel()
panel9 = makeScrollPanel()
panel10 = makeScrollPanel()

do
    local _lbl = createTransparentLabel("Hit Box", panel2)
    _lbl.TextColor3 = Color3.fromRGB(255,255,255)
    _lbl.Font = Enum.Font.GothamBold
    _lbl.TextSize = 14
end
S.sizeInput = createTransparentInput("Size (e.g. 15)", tostring(S.DEFAULT_HITBOX_SIZE), panel2)
autoToggle = createTransparentButton("Head Hit Box: OFF", Color3.fromRGB(38, 38, 38), panel2)
setToggleState(autoToggle, false)
S.btnRefs.autoToggle = autoToggle
bodyToggle = createTransparentButton("Body Hit Box: OFF", Color3.fromRGB(38, 38, 38), panel2)
setToggleState(bodyToggle, false)
S.btnRefs.bodyToggle = bodyToggle

S.npcHitboxToggleBtn = createTransparentButton("Npc Hit Box: OFF", Color3.fromRGB(38, 38, 38), panel2)
setToggleState(S.npcHitboxToggleBtn, false)

S.autoNPCHitboxEnabled = false
local bossHitboxNPCs = {}
local bossOrigSizes = {}

local function isNPC(model)
    if not model:IsA("Model") then return false end
    if not model:FindFirstChildOfClass("Humanoid") then return false end
    if Players:GetPlayerFromCharacter(model) then return false end
    return true
end

local function cleanupBossHitbox(npc)
    local data = bossHitboxNPCs[npc]
    if not data then return end
    if data.connection then data.connection:Disconnect() end
    local head = data.head
    if head and head.Parent then
        head.Size = bossOrigSizes[head] or Vector3.new(2, 1, 1)
        head.Transparency = 0
        head.CanCollide = true
        head.Massless = false
        head.Material = Enum.Material.Plastic
        local meshBackup = head:FindFirstChild("__MeshBackup")
        if meshBackup then
            local realMesh = meshBackup:FindFirstChildOfClass("SpecialMesh") or meshBackup:FindFirstChildOfClass("BlockMesh")
            if realMesh then realMesh:Clone().Parent = head end
            meshBackup:Destroy()
        end
        local decalBackup = head:FindFirstChild("__DecalBackup")
        if decalBackup then
            local realDecal = decalBackup:FindFirstChildOfClass("Decal")
            if realDecal then realDecal:Clone().Parent = head end
            decalBackup:Destroy()
        end
    end
    removeOutlineChams(npc)
    bossOrigSizes[head] = nil
    bossHitboxNPCs[npc] = nil
end

local function applyBossHitbox(npc, size)
    if bossHitboxNPCs[npc] then return end
    if not npc or not npc.Parent then return end
    if not isNPC(npc) then return end

    local hum = npc:FindFirstChildOfClass("Humanoid")
    local head = npc:FindFirstChild("Head") or npc:FindFirstChild("HumanoidRootPart")
    if not hum or not head then return end
    if hum.Health <= 0 then return end

    bossOrigSizes[head] = head.Size

    local _bhThrottle = 0
    local connection = RunService.Heartbeat:Connect(function(dt)
        if not S.autoNPCHitboxEnabled or not npc.Parent or hum.Health <= 0 then
            cleanupBossHitbox(npc)
            return
        end
        _bhThrottle = _bhThrottle + dt
        if _bhThrottle < 0.1 then return end
        _bhThrottle = 0

        local meshBackup = head:FindFirstChild("__MeshBackup")
        if not meshBackup then
            local existingMesh = head:FindFirstChildOfClass("SpecialMesh") or head:FindFirstChildOfClass("BlockMesh")
            if existingMesh then
                local folder = Instance.new("Folder")
                folder.Name = "__MeshBackup"
                folder.Parent = head
                existingMesh:Clone().Parent = folder
                existingMesh:Destroy()
            end
        else
            local strayMesh = head:FindFirstChildOfClass("SpecialMesh") or head:FindFirstChildOfClass("BlockMesh")
            if strayMesh then strayMesh:Destroy() end
        end

        local decalBackup = head:FindFirstChild("__DecalBackup")
        if not decalBackup then
            local existingDecal = head:FindFirstChildOfClass("Decal")
            if existingDecal then
                local folder = Instance.new("Folder")
                folder.Name = "__DecalBackup"
                folder.Parent = head
                existingDecal:Clone().Parent = folder
                existingDecal:Destroy()
            end
        else
            local strayDecal = head:FindFirstChildOfClass("Decal")
            if strayDecal then strayDecal:Destroy() end
        end

        local wanted = Vector3.new(size, size, size)
        local needsRefresh = head.Size ~= wanted or head.Transparency ~= 0.5 or head.Material ~= Enum.Material.ForceField
        if needsRefresh then
            head.Size = wanted
            head.Transparency = 0.5
            head.CanCollide = false
            head.Massless = true
            head.Material = Enum.Material.ForceField
            head.Color = Color3.fromRGB(255, 50, 50)
        end
        if not npc:FindFirstChild("StarCrowOutline") then
            addOutlineChams(npc)
        end
    end)

    bossHitboxNPCs[npc] = { head = head, connection = connection }
end

S.npcHitboxToggleBtn.MouseButton1Click:Connect(function()
    S.autoNPCHitboxEnabled = not S.autoNPCHitboxEnabled
    S.npcHitboxToggleBtn.Text = S.autoNPCHitboxEnabled and "Npc Hit Box: ON" or "Npc Hit Box: OFF"
    setToggleState(S.npcHitboxToggleBtn, S.autoNPCHitboxEnabled)
    
    if not S.autoNPCHitboxEnabled then
        for npc in pairs(bossHitboxNPCs) do
            cleanupBossHitbox(npc)
        end
    end
end)

task.spawn(function()
    while task.wait(1) do
        if S.autoNPCHitboxEnabled then
            local size = tonumber(S.sizeInput.Text) or 15
            for _, v in ipairs(workspace:GetDescendants()) do
                if isNPC(v) then
                    applyBossHitbox(v, size)
                end
            end
        end
    end
end)
removeAllBtn = createTransparentButton("Remove All Hitboxes", Color3.fromRGB(38, 38, 38), panel2)
removeAllBtn.BackgroundColor3 = Color3.fromRGB(38,38,38)
removeAllBtn.BackgroundTransparency = 0.35
S.statusLabel = createTransparentLabel("Processing: 0 Players", panel2)
S.statusLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
do
    local specLbl = createTransparentLabel("Spectate Player", panel2)
    specLbl.TextColor3 = Color3.fromRGB(255, 255, 255)
end
S.specContainer = Instance.new("ScrollingFrame")
S.specContainer.Size = UDim2.new(1,0,0,120)
S.specContainer.BackgroundTransparency = 1
S.specContainer.BorderSizePixel = 0
S.specContainer.ScrollBarThickness = 4
S.specContainer.CanvasSize = UDim2.new(0,0,0,0)
S.specContainer.Parent = panel2
do
    local specLayout = Instance.new("UIListLayout")
    specLayout.Parent = S.specContainer
    specLayout.Padding = UDim.new(0,4)
    specLayout:GetPropertyChangedSignal("AbsoluteContentSize"):Connect(function()
        S.specContainer.CanvasSize = UDim2.new(0,0,0,specLayout.AbsoluteContentSize.Y + 10)
    end)
end
local function refreshSpecList()
    for _,b in pairs(S.specButtons) do
        b:Destroy()
    end
    S.specButtons = {}
    for _,plr in pairs(Players:GetPlayers()) do
        if plr ~= LocalPlayer then
           local btn = createTransparentButton(plr.Name, Color3.fromRGB(38, 38, 38), S.specContainer)
btn.BackgroundTransparency = 0.35
btn.BackgroundColor3 = Color3.fromRGB(38, 38, 38)
            table.insert(S.specButtons, btn)
            btn.MouseButton1Click:Connect(function()
                if S.selectedSpecButton then
    S.selectedSpecButton.BackgroundColor3 = Color3.fromRGB(38, 38, 38)
    S.selectedSpecButton.BackgroundTransparency = 0.35
end
                btn.BackgroundColor3 = Color3.fromRGB(50, 200, 120)
btn.BackgroundTransparency = 0
S.selectedSpecButton = btn
                SpectatePlayer(plr)
            end)
        end
    end
end
refreshSpecList()
Players.PlayerAdded:Connect(refreshSpecList)
Players.PlayerRemoving:Connect(refreshSpecList)
local stopSpec = createTransparentButton("Stop Spectate", Color3.fromRGB(180,50,50), panel2)
stopSpec.BackgroundColor3 = Color3.fromRGB(38,38,38)
stopSpec.BackgroundTransparency = 0.35
stopSpec.MouseButton1Click:Connect(function()
    StopSpectate()
    for _,v in pairs(S.specContainer:GetChildren()) do
        if v:IsA("TextButton") then
            v.BackgroundColor3 = Color3.fromRGB(38, 38, 38)
            v.BackgroundTransparency = 0.35
        end
    end
    S.selectedSpecButton = nil
end)
do
    local loopspeedLbl = createTransparentLabel("Loopspeed", panel2)
    loopspeedLbl.TextColor3 = Color3.fromRGB(255, 255, 255)
end
S.loopspeedInput = createTransparentInput("Speed (e.g. 30)", "30", panel2)
S.loopspeedToggle = createTransparentButton("Loopspeed: OFF", Color3.fromRGB(38, 38, 38), panel2)
setToggleState(S.loopspeedToggle, false)
S.btnRefs.loopspeedToggle = S.loopspeedToggle
S.invisButton = createTransparentButton("Invisible: OFF", Color3.fromRGB(38, 38, 38), panel2)
setToggleState(S.invisButton, false)
local invisFloatToggleBtn = createTransparentButton("Invisible Float: OFF", Color3.fromRGB(38, 38, 38), panel2)
setToggleState(invisFloatToggleBtn, false)
S.btnRefs.invisFloatToggleBtn = invisFloatToggleBtn
S.invisSound = Instance.new("Sound")
S.invisSound.SoundId = S.INVIS_CONFIG.SOUND_ID
S.invisSound.Volume = 0.5
S.invisSound.Parent = gui
do
    local _lbl = createTransparentLabel("ESP", panel3)
    _lbl.TextColor3 = Color3.fromRGB(255,255,255)
    _lbl.Font = Enum.Font.GothamBold
    _lbl.TextSize = 14
end
S.btnRefs.espShowNameBtn = createTransparentButton("ESP Name: OFF", Color3.fromRGB(38, 38, 38), panel3)
setToggleState(S.btnRefs.espShowNameBtn, false)
S.btnRefs.espShowDistBtn = createTransparentButton("ESP Distance: OFF", Color3.fromRGB(38, 38, 38), panel3)
setToggleState(S.btnRefs.espShowDistBtn, false)
S.btnRefs.espShowHpBtn = createTransparentButton("ESP HP: OFF", Color3.fromRGB(38, 38, 38), panel3)
setToggleState(S.btnRefs.espShowHpBtn, false)
chamsToggle = createTransparentButton("Esp Chams: OFF", Color3.fromRGB(38, 38, 38), panel3)
setToggleState(chamsToggle, false)
S.btnRefs.chamsToggle = chamsToggle
chamsToggle.MouseButton1Click:Connect(function()
    if not S.chamsEnabled and (S.autoHitboxEnabled or S.autoBodyHitboxEnabled) then
        return
    end
    S.chamsEnabled = not S.chamsEnabled
    if S.chamsEnabled then
        chamsToggle.Text = "Esp Chams: ON"
        setToggleState(chamsToggle, true)
        for _, p in pairs(game.Players:GetPlayers()) do
            if p ~= game.Players.LocalPlayer and p.Character and isAlive(p.Character) then
                addChams(p.Character, getTeamColor(p))
            end
        end
    else
        chamsToggle.Text = "Esp Chams: OFF"
        setToggleState(chamsToggle, false)
        for _, p in pairs(game.Players:GetPlayers()) do
            if p.Character then
                removeChams(p.Character)
            end
        end
    end
end)
tracerToggle = createTransparentButton("Esp Tracer: OFF", Color3.fromRGB(38, 38, 38), panel3)
setToggleState(tracerToggle, false)
S.btnRefs.tracerToggle = tracerToggle
tracerToggle.MouseButton1Click:Connect(function()
    S.tracerESPEnabled = not S.tracerESPEnabled
    if S.tracerESPEnabled then
        tracerToggle.Text = "Esp Tracer: ON"
        setToggleState(tracerToggle, true)
        for _, p in pairs(game.Players:GetPlayers()) do
            if p ~= game.Players.LocalPlayer then
                createTracerESP(p)
            end
        end
    else
        tracerToggle.Text = "Esp Tracer: OFF"
        setToggleState(tracerToggle, false)
        for _, p in pairs(game.Players:GetPlayers()) do
            removeTracerESP(p)
        end
    end
end)
do
    local flyLbl = createTransparentLabel("Fly", panel2)
    flyLbl.TextColor3 = Color3.fromRGB(255, 255, 255)
end
local flySpeedInput = createTransparentInput("Fly Speed", "5", panel2)
S.btnRefs.flySpeedInput = flySpeedInput
S.flyToggle = createTransparentButton("Fly: OFF", Color3.fromRGB(38, 38, 38), panel2)
setToggleState(S.flyToggle, false)
S.btnRefs.flyToggle = S.flyToggle
S.vehicleFlyEnabled = false
S.vehicleFlyBtn = createTransparentButton("Vehicle Fly: OFF", Color3.fromRGB(38, 38, 38), panel2)
setToggleState(S.vehicleFlyBtn, false)
S.btnRefs.vehicleFlyBtn = S.vehicleFlyBtn
S.stopVehicleFly = function()
    if S.vFlyConnection then S.vFlyConnection:Disconnect(); S.vFlyConnection = nil end
    if S.vFlyBV then S.vFlyBV:Destroy(); S.vFlyBV = nil end
    if S.vFlyBG then S.vFlyBG:Destroy(); S.vFlyBG = nil end
end
S.startVehicleFly = function()
    S.stopVehicleFly()
    local char = LocalPlayer.Character
    if not char then return end
    local hum = char:FindFirstChildWhichIsA("Humanoid")
    if not hum or not hum.SeatPart then return end
    local vehicle = hum.SeatPart:FindFirstAncestorWhichIsA("Model") or hum.SeatPart.Parent
    local primary = vehicle.PrimaryPart or hum.SeatPart
    S.vFlyBV = Instance.new("BodyVelocity")
    S.vFlyBV.MaxForce = Vector3.new(math.huge, math.huge, math.huge)
    S.vFlyBV.Velocity = Vector3.zero
    S.vFlyBV.Parent = primary
    S.vFlyBG = Instance.new("BodyGyro")
    S.vFlyBG.MaxTorque = Vector3.new(math.huge, math.huge, math.huge)
    S.vFlyBG.P = 9e9
    S.vFlyBG.D = 9e9
    S.vFlyBG.CFrame = primary.CFrame
    S.vFlyBG.Parent = primary
    S.vFlyConnection = game:GetService("RunService").Heartbeat:Connect(function()
        local cam = workspace.CurrentCamera
        local move = Vector3.zero
        local moveVec = Vector3.zero
        local uis = game:GetService("UserInputService")
        pcall(function()
            moveVec = require(LocalPlayer.PlayerScripts.PlayerModule):GetControls():GetMoveVector()
        end)
        if moveVec.Magnitude > 0 then
            move += cam.CFrame.LookVector * -moveVec.Z
            move += cam.CFrame.RightVector * moveVec.X
        else
            if uis:IsKeyDown(Enum.KeyCode.W) then move += cam.CFrame.LookVector end
            if uis:IsKeyDown(Enum.KeyCode.S) then move -= cam.CFrame.LookVector end
            if uis:IsKeyDown(Enum.KeyCode.A) then move -= cam.CFrame.RightVector end
            if uis:IsKeyDown(Enum.KeyCode.D) then move += cam.CFrame.RightVector end
        end
        if uis:IsKeyDown(Enum.KeyCode.Space) then move += Vector3.new(0, 1, 0) end
        if uis:IsKeyDown(Enum.KeyCode.LeftControl) or uis:IsKeyDown(Enum.KeyCode.LeftShift) then move -= Vector3.new(0, 1, 0) end
        if move.Magnitude > 0 then move = move.Unit end
        local speed = tonumber(S.btnRefs.flySpeedInput and S.btnRefs.flySpeedInput.Text) or S.flySpeed or 2
        if S.vFlyBV and S.vFlyBG and primary then
            S.vFlyBV.Velocity = move * speed * 50
            primary.AssemblyAngularVelocity = Vector3.zero
            primary.RotVelocity = Vector3.zero
            local flatLook = Vector3.new(cam.CFrame.LookVector.X, 0, cam.CFrame.LookVector.Z)
            if flatLook.Magnitude > 0.001 then
                S.vFlyBG.CFrame = CFrame.lookAt(primary.Position, primary.Position + flatLook)
            end
        end
    end)
end
S.vehicleFlyBtn.MouseButton1Click:Connect(function()
    S.vehicleFlyEnabled = not S.vehicleFlyEnabled
    S.vehicleFlyBtn.Text = S.vehicleFlyEnabled and "Vehicle Fly: ON" or "Vehicle Fly: OFF"
    setToggleState(S.vehicleFlyBtn, S.vehicleFlyEnabled)
    if S.vehicleFlyEnabled then
        S.startVehicleFly()
    else
        S.stopVehicleFly()
    end
end)
flyFloatToggleBtn = createTransparentButton("Fly Float: OFF", Color3.fromRGB(38, 38, 38), panel2)
setToggleState(flyFloatToggleBtn, false)
S.btnRefs.flyFloatToggleBtn = flyFloatToggleBtn
do
    S.flyFloatDiv = Instance.new("Frame")
    S.flyFloatDiv.Parent = panel2
    S.flyFloatDiv.Size = UDim2.new(1, 0, 0, 2)
    S.flyFloatDiv.BackgroundColor3 = Color3.fromRGB(70, 70, 70)
    S.flyFloatDiv.BackgroundTransparency = 0.2
    S.flyFloatDiv.BorderSizePixel = 0
end
infJumpToggle = createTransparentButton("Inf Jump: OFF", Color3.fromRGB(38, 38, 38), panel2)
setToggleState(infJumpToggle, false)
S.btnRefs.infJumpToggle = infJumpToggle
noclipToggle = createTransparentButton("Noclip: OFF", Color3.fromRGB(38, 38, 38), panel2)
setToggleState(noclipToggle, false)
S.btnRefs.noclipToggle = noclipToggle
local noclipFloatToggleBtn = createTransparentButton("Noclip Float: OFF", Color3.fromRGB(38, 38, 38), panel2)
setToggleState(noclipFloatToggleBtn, false)
S.btnRefs.noclipFloatToggleBtn = noclipFloatToggleBtn
do
    local _div = Instance.new("Frame")
    _div.Parent = panel2
    _div.Size = UDim2.new(1, 0, 0, 2)
    _div.BackgroundColor3 = Color3.fromRGB(70, 70, 70)
    _div.BackgroundTransparency = 0.2
    _div.BorderSizePixel = 0
end
local freezeBodyBtn = createTransparentButton("Freeze Body: OFF", Color3.fromRGB(38, 38, 38), panel2)
setToggleState(freezeBodyBtn, false)
S.btnRefs.freezeBodyBtn = freezeBodyBtn
local cameraNoclipBtn = createTransparentButton("Camera Noclip: OFF", Color3.fromRGB(38, 38, 38), panel2)
setToggleState(cameraNoclipBtn, false)
S.btnRefs.cameraNoclipBtn = cameraNoclipBtn
do
    local floatBtn = createTransparentButton("Float: OFF", Color3.fromRGB(38, 38, 38), panel2)
    setToggleState(floatBtn, false)
    S.btnRefs.floatBtn = floatBtn
end
do
    local jumpPowerInput = createTransparentInput("Jump Power (e.g. 50)", "50", panel2)
    S.btnRefs.jumpPowerInput = jumpPowerInput
    local jumpPowerBtn = createTransparentButton("Jump Power: OFF", Color3.fromRGB(38, 38, 38), panel2)
    setToggleState(jumpPowerBtn, false)
    S.btnRefs.jumpPowerBtn = jumpPowerBtn
end
do
    local _lbl = createTransparentLabel("Teleport Player", panel4)
    _lbl.TextColor3 = Color3.fromRGB(255,255,255)
    _lbl.Font = Enum.Font.GothamBold
    _lbl.TextSize = 14
end
S.tpPlayerList = Instance.new("ScrollingFrame")
S.tpPlayerList.Size = UDim2.new(1,0,0,110)
S.tpPlayerList.BackgroundTransparency = 1
S.tpPlayerList.BorderSizePixel = 0
S.tpPlayerList.ScrollBarThickness = 4
S.tpPlayerList.CanvasSize = UDim2.new(0,0,0,0)
S.tpPlayerList.Parent = panel4
do
    local tpListLayout = Instance.new("UIListLayout")
    tpListLayout.Padding = UDim.new(0,2)
    tpListLayout.Parent = S.tpPlayerList
    tpListLayout:GetPropertyChangedSignal("AbsoluteContentSize"):Connect(function()
        S.tpPlayerList.CanvasSize = UDim2.new(0,0,0,tpListLayout.AbsoluteContentSize.Y + 5)
    end)
end
S.autoTPBtn = createTransparentButton("Auto TP: OFF", Color3.fromRGB(38, 38, 38), panel4)
setToggleState(S.autoTPBtn, false)
local teleportBtn = createTransparentButton("TP", Color3.fromRGB(38, 38, 38), panel4)
teleportBtn.BackgroundTransparency = 0.35
teleportBtn.BackgroundColor3 = Color3.fromRGB(38, 38, 38)
local tpToolBtn = createTransparentButton("TP Tool", Color3.fromRGB(38, 38, 38), panel4)
tpToolBtn.BackgroundTransparency = 0.35
tpToolBtn.BackgroundColor3 = Color3.fromRGB(38, 38, 38)
local ctrlClickTPBtn = createTransparentButton("Ctrl Click TP: OFF", Color3.fromRGB(38, 38, 38), panel4)
setToggleState(ctrlClickTPBtn, false)
S.btnRefs.ctrlClickTPBtn = ctrlClickTPBtn
do
    local _lbl = createTransparentLabel("Troll", panel5)
    _lbl.TextColor3 = Color3.fromRGB(255,255,255)
    _lbl.Font = Enum.Font.GothamBold
    _lbl.TextSize = 14
end
S.flingStatusLabel = createTransparentLabel("Select targets to fling", panel5)
S.flingStatusLabel.TextColor3 = Color3.fromRGB(255,255,255)
S.flingPlayerList = Instance.new("ScrollingFrame")
S.flingPlayerList.Size = UDim2.new(1, 0, 0, 130)
S.flingPlayerList.BackgroundColor3 = Color3.fromRGB(30,30,30)
S.flingPlayerList.BackgroundTransparency = 0.5
S.flingPlayerList.BorderSizePixel = 0
S.flingPlayerList.ScrollBarThickness = 5
S.flingPlayerList.CanvasSize = UDim2.new(0, 0, 0, 0)
S.flingPlayerList.Parent = panel5
do
    local flingListLayout = Instance.new("UIListLayout")
    flingListLayout.SortOrder = Enum.SortOrder.LayoutOrder
    flingListLayout.Padding = UDim.new(0, 3)
    flingListLayout.Parent = S.flingPlayerList
    flingListLayout:GetPropertyChangedSignal("AbsoluteContentSize"):Connect(function()
        S.flingPlayerList.CanvasSize = UDim2.new(0,0,0,flingListLayout.AbsoluteContentSize.Y + 10)
    end)
end
S.flingStartBtn = createTransparentButton("FLING PLAYER : OFF", Color3.fromRGB(38, 38, 38), panel5)
S.flingStartBtn.BackgroundTransparency = 0.35
S.flingStartBtn.BackgroundColor3 = Color3.fromRGB(38, 38, 38)
S.walkFlingBtn = createTransparentButton("WALK FLING : OFF", Color3.fromRGB(38, 38, 38), panel5)
S.walkFlingBtn.BackgroundTransparency = 0.35
S.walkFlingBtn.BackgroundColor3 = Color3.fromRGB(38, 38, 38)
S.bangBtn = createTransparentButton("BANG PLAYER : OFF", Color3.fromRGB(38, 38, 38), panel5)
S.bangBtn.BackgroundTransparency = 0.35
S.bangBtn.BackgroundColor3 = Color3.fromRGB(38, 38, 38)
S.loopBringBtn = createTransparentButton("LOOP BRING PLAYER : OFF", Color3.fromRGB(38, 38, 38), panel5)
S.loopBringBtn.BackgroundTransparency = 0.35
S.loopBringBtn.BackgroundColor3 = Color3.fromRGB(38, 38, 38)
local flingSelectAllBtn = createTransparentButton("Select All", Color3.fromRGB(38, 38, 38), panel5)
flingSelectAllBtn.BackgroundTransparency = 0.35
flingSelectAllBtn.BackgroundColor3 = Color3.fromRGB(38, 38, 38)
local flingDeselectAllBtn = createTransparentButton("Deselect All", Color3.fromRGB(38, 38, 38), panel5)
flingDeselectAllBtn.BackgroundTransparency = 0.35
flingDeselectAllBtn.BackgroundColor3 = Color3.fromRGB(38, 38, 38)
do
    local _lbl = createTransparentLabel("Misc", panel6)
    _lbl.TextColor3 = Color3.fromRGB(255,255,255)
    _lbl.Font = Enum.Font.GothamBold
    _lbl.TextSize = 14
end
antiFlingBtn = createTransparentButton("Anti Fling: OFF", Color3.fromRGB(38, 38, 38), panel6)
setToggleState(antiFlingBtn, false)
S.btnRefs.antiFlingBtn = antiFlingBtn
antiVoidBtn = createTransparentButton("Anti Void: OFF", Color3.fromRGB(38, 38, 38), panel6)
setToggleState(antiVoidBtn, false)
S.btnRefs.antiVoidBtn = antiVoidBtn
antiAfkBtn = createTransparentButton("Anti AFK: OFF", Color3.fromRGB(38, 38, 38), panel6)
setToggleState(antiAfkBtn, false)
S.btnRefs.antiAfkBtn = antiAfkBtn
local loopFBBtn = createTransparentButton("LoopFullBright: OFF", Color3.fromRGB(38, 38, 38), panel6)
loopFBBtn.BackgroundTransparency = 0.35
loopFBBtn.BackgroundColor3 = Color3.fromRGB(38, 38, 38)
S.btnRefs.loopFBBtn = loopFBBtn
local noFogBtn = createTransparentButton("No Fog", Color3.fromRGB(38, 38, 38), panel6)
noFogBtn.BackgroundTransparency = 0.35
noFogBtn.BackgroundColor3 = Color3.fromRGB(38, 38, 38)
do
    local miscDivider = Instance.new("Frame")
    miscDivider.Parent = panel6
    miscDivider.Size = UDim2.new(1, 0, 0, 2)
    miscDivider.BackgroundColor3 = Color3.fromRGB(70, 70, 70)
    miscDivider.BackgroundTransparency = 0.2
    miscDivider.BorderSizePixel = 0
end
local instantBuyBtn = createTransparentButton("Instant Interact: OFF", Color3.fromRGB(38, 38, 38), panel6)
instantBuyBtn.BackgroundTransparency = 0.35
instantBuyBtn.BackgroundColor3 = Color3.fromRGB(38, 38, 38)
S.btnRefs.instantBuyBtn = instantBuyBtn
S.firstPersonCameraBtn = createTransparentButton("To First Person Camera", Color3.fromRGB(38, 38, 38), panel6)
S.firstPersonCameraBtn.BackgroundTransparency = 0.35
S.firstPersonCameraBtn.BackgroundColor3 = Color3.fromRGB(38, 38, 38)
S.firstPersonCameraBtn.MouseButton1Click:Connect(function()
    local cam = workspace.CurrentCamera
    local char = LocalPlayer.Character
    if cam and char then
        local hum = char:FindFirstChildWhichIsA("Humanoid")
        if hum then
            cam.CameraSubject = hum
        end
        cam.CameraType = Enum.CameraType.Custom
    end
    LocalPlayer.CameraMode = Enum.CameraMode.LockFirstPerson
    LocalPlayer.CameraMinZoomDistance = 0.5
    LocalPlayer.CameraMaxZoomDistance = 0.5
    pcall(function() game:GetService("RunService"):UnbindFromRenderStep("StarCrow_UnlockCam_Force") end)
end)
local unlockCameraBtn = createTransparentButton("To Third Person Camera", Color3.fromRGB(38, 38, 38), panel6)
unlockCameraBtn.BackgroundTransparency = 0.35
unlockCameraBtn.BackgroundColor3 = Color3.fromRGB(38, 38, 38)
local mobileShiftLockBtn = createTransparentButton("Mobile ShiftLock: OFF", Color3.fromRGB(38, 38, 38), panel6)
mobileShiftLockBtn.BackgroundTransparency = 0.35
mobileShiftLockBtn.BackgroundColor3 = Color3.fromRGB(38, 38, 38)
S.btnRefs.mobileShiftLockBtn = mobileShiftLockBtn
local autoClickBtn = createTransparentButton("Auto Click: OFF", Color3.fromRGB(38, 38, 38), panel6)
setToggleState(autoClickBtn, false)
S.btnRefs.autoClickBtn = autoClickBtn
local AUTOCLICK_DELAY_PC = 0.15
local AUTOCLICK_DELAY_MOBILE = 0.08
local function stopAutoClick()
    S.autoClickEnabled = false
    S.autoClickConnection = nil
end
local function doGlobalClick()
    local vim = game:GetService("VirtualInputManager")
    local cam = workspace.CurrentCamera
    if not cam then return end
    local vp = cam.ViewportSize
    local x = math.floor(vp.X * 0.78)
    local y = math.floor(vp.Y * 0.82)
    pcall(function()
        vim:SendMouseButtonEvent(x, y, 0, true, game, 0)
        task.wait()
        vim:SendMouseButtonEvent(x, y, 0, false, game, 0)
    end)
end
local function activateHeldTool()
    local char = LocalPlayer.Character
    if not char then return false end
    local tool = char:FindFirstChildWhichIsA("Tool")
    if not tool then return false end
    pcall(function()
        tool:Activate()
    end)
    return true
end
local function startAutoClick()
    stopAutoClick()
    S.autoClickEnabled = true
    local isMobile = UserInputService.TouchEnabled and not UserInputService.KeyboardEnabled
    S.autoClickConnection = task.spawn(function()
        while S.autoClickEnabled do
            if mainFrame and mainFrame.Visible then
                task.wait(0.1)
            else
                if isMobile then
                    local usedTool = activateHeldTool()
                    if not usedTool then
                        task.wait(0.15)
                    else
                        task.wait(AUTOCLICK_DELAY_MOBILE)
                    end
                else
                    doGlobalClick()
                    task.wait(AUTOCLICK_DELAY_PC)
                end
            end
        end
    end)
end
autoClickBtn.MouseButton1Click:Connect(function()
    if S.autoClickEnabled then
        stopAutoClick()
        autoClickBtn.Text = "Auto Click: OFF"
        setToggleState(autoClickBtn, false)
        return
    end
    startAutoClick()
    autoClickBtn.Text = "Auto Click: ON"
    setToggleState(autoClickBtn, true)
end)
antiAfkBtn.MouseButton1Click:Connect(function()
    S.antiAfkEnabled = not S.antiAfkEnabled
    antiAfkBtn.Text = "Anti AFK: "..(S.antiAfkEnabled and "ON" or "OFF")
    if S.antiAfkEnabled then
        setToggleState(antiAfkBtn, true)
    else
        setToggleState(antiAfkBtn, false)
    end
end)
game:GetService("Players").LocalPlayer.Idled:Connect(function()
    if S.antiAfkEnabled then
        local VU = game:GetService("VirtualUser")
        VU:Button2Down(Vector2.new(0,0), workspace.CurrentCamera.CFrame)
        task.wait(1)
        VU:Button2Up(Vector2.new(0,0), workspace.CurrentCamera.CFrame)
    end
end)
do
    local antiVoidEnabled = false
    local lastSafePos = nil
    local function setAntiVoid(state, silent)
        antiVoidEnabled = state
        antiVoidBtn.Text = "Anti Void: "..(antiVoidEnabled and "ON" or "OFF")
        if antiVoidEnabled then
            setToggleState(antiVoidBtn, true)
        else
            setToggleState(antiVoidBtn, false)
        end
    end
    S.setAntiVoid = setAntiVoid
    antiVoidBtn.MouseButton1Click:Connect(function()
        setAntiVoid(not antiVoidEnabled)
    end)
    RunService.Heartbeat:Connect(function()
        if not antiVoidEnabled then return end
        local char = LocalPlayer.Character
        if not char then return end
        local hrp = char:FindFirstChild("HumanoidRootPart")
        if not hrp then return end
        if hrp.Position.Y > -5 then
            lastSafePos = hrp.CFrame
        end
        if hrp.Position.Y < -30 then
            if lastSafePos then
                hrp.CFrame = lastSafePos
                hrp.AssemblyLinearVelocity = Vector3.zero
            end
        end
    end)
end
do
    local _lbl = createTransparentLabel("Server", panel7)
    _lbl.TextColor3 = Color3.fromRGB(255,255,255)
    _lbl.Font = Enum.Font.GothamBold
    _lbl.TextSize = 14
end
local rejoinBtn = createTransparentButton("Rejoin", Color3.fromRGB(38,38,38), panel7)
rejoinBtn.BackgroundTransparency = 0.35
rejoinBtn.BackgroundColor3 = Color3.fromRGB(38,38,38)
local serverHopBtn = createTransparentButton("Server Hop", Color3.fromRGB(38,38,38), panel7)
serverHopBtn.BackgroundTransparency = 0.35
serverHopBtn.BackgroundColor3 = Color3.fromRGB(38,38,38)
local jobIdInput = Instance.new("TextBox")
jobIdInput.Parent = panel7
jobIdInput.Size = UDim2.new(1, 0, 0, 36)
jobIdInput.PlaceholderText = "Enter JobId..."
jobIdInput.Text = ""
jobIdInput.BackgroundColor3 = Color3.fromRGB(38,38,38)
jobIdInput.BackgroundTransparency = 0.35
jobIdInput.BorderSizePixel = 0
jobIdInput.TextColor3 = Color3.new(1,1,1)
jobIdInput.PlaceholderColor3 = Color3.fromRGB(170,170,170)
jobIdInput.ClearTextOnFocus = false
jobIdInput.Font = Enum.Font.Gotham
jobIdInput.TextSize = 14
do
    local jobIdCorner = Instance.new("UICorner")
    jobIdCorner.CornerRadius = UDim.new(0,8)
    jobIdCorner.Parent = jobIdInput
end
local joinJobBtn = createTransparentButton("Join", Color3.fromRGB(38,38,38), panel7)
joinJobBtn.BackgroundTransparency = 0.35
joinJobBtn.BackgroundColor3 = Color3.fromRGB(38,38,38)
local copyJobBtn = createTransparentButton("Copy JobId", Color3.fromRGB(38,38,38), panel7)
copyJobBtn.BackgroundTransparency = 0.35
copyJobBtn.BackgroundColor3 = Color3.fromRGB(38,38,38)
do
    local serverDivider = Instance.new("Frame")
    serverDivider.Parent = panel7
    serverDivider.Size = UDim2.new(1, 0, 0, 2)
    serverDivider.BackgroundColor3 = Color3.fromRGB(70, 70, 70)
    serverDivider.BackgroundTransparency = 0.2
    serverDivider.BorderSizePixel = 0
end
local posInput = Instance.new("TextBox")
posInput.Parent = panel7
posInput.Size = UDim2.new(1, 0, 0, 36)
posInput.PlaceholderText = "Enter Position..."
posInput.Text = ""
posInput.BackgroundColor3 = Color3.fromRGB(38,38,38)
posInput.BackgroundTransparency = 0.35
posInput.BorderSizePixel = 0
posInput.TextColor3 = Color3.new(1,1,1)
posInput.PlaceholderColor3 = Color3.fromRGB(170,170,170)
posInput.ClearTextOnFocus = false
posInput.Font = Enum.Font.Gotham
posInput.TextSize = 14
do
    local posInputCorner = Instance.new("UICorner")
    posInputCorner.CornerRadius = UDim.new(0,8)
    posInputCorner.Parent = posInput
end
local tpPosBtn = createTransparentButton("Tp Position", Color3.fromRGB(38,38,38), panel7)
tpPosBtn.BackgroundTransparency = 0.35
tpPosBtn.BackgroundColor3 = Color3.fromRGB(38,38,38)
local copyPosBtn = createTransparentButton("Copy Position", Color3.fromRGB(38,38,38), panel7)
copyPosBtn.BackgroundTransparency = 0.35
copyPosBtn.BackgroundColor3 = Color3.fromRGB(38,38,38)
do
    local savePosDiv = Instance.new("Frame")
    savePosDiv.Size = UDim2.new(1, 0, 0, 2)
    savePosDiv.BackgroundColor3 = Color3.fromRGB(70, 70, 70)
    savePosDiv.BackgroundTransparency = 0.2
    savePosDiv.BorderSizePixel = 0
    savePosDiv.Parent = panel7
end
local savePosNameInput = createTransparentInput("Location name...", "", panel7)
local savePosBtn = createTransparentButton("Save Position", Color3.fromRGB(38,38,38), panel7)
savePosBtn.BackgroundTransparency = 0.35
savePosBtn.BackgroundColor3 = Color3.fromRGB(38,38,38)
local tpSavedPosBtn = createTransparentButton("Tp Position", Color3.fromRGB(38,38,38), panel7)
tpSavedPosBtn.BackgroundTransparency = 0.35
tpSavedPosBtn.BackgroundColor3 = Color3.fromRGB(38,38,38)
local spawnHereBtn = createTransparentButton("Set Spawn Here", Color3.fromRGB(38,38,38), panel7)
spawnHereBtn.BackgroundTransparency = 0.35
spawnHereBtn.BackgroundColor3 = Color3.fromRGB(38,38,38)
do
    local _lbl = createTransparentLabel("Tools", panel8)
    _lbl.TextColor3 = Color3.fromRGB(255, 255, 255)
    _lbl.Font = Enum.Font.GothamBold
    _lbl.TextSize = 14
end
local function runToolAura()
    if S.isAuraRunning then return end
    S.isAuraRunning = true
    local char = LocalPlayer.Character
    if not char then
        S.isAuraRunning = false
        return
    end
    local root = char:FindFirstChild("HumanoidRootPart")
    if not root then
        S.isAuraRunning = false
        return
    end
    local RunService = game:GetService("RunService")
    task.spawn(function()
        local count = 0
        local objectsToScan = {workspace}
        local currentIndex = 1
        while currentIndex <= #objectsToScan do
            local currentParent = objectsToScan[currentIndex]
            currentIndex = currentIndex + 1
            local children = currentParent:GetChildren()
            for i = 1, #children do
                local obj = children[i]
                count = count + 1
                if count % 1000 == 0 then
                    RunService.Heartbeat:Wait()
                end
                if not (obj:IsA("Model") and obj:FindFirstChildOfClass("Humanoid")) then
                    if obj:IsA("Tool") then
                        local handle = obj:FindFirstChild("Handle") or obj:FindFirstChildWhichIsA("BasePart")
                        if handle then
                            task.spawn(function()
                                if pcall(function() firetouchinterest(root, handle, 0) end) then
                                    task.wait()
                                    pcall(function() firetouchinterest(root, handle, 1) end)
                                end
                            end)
                        else
                            pcall(function() obj.Parent = LocalPlayer.Backpack end)
                        end
                    else
                        table.insert(objectsToScan, obj)
                    end
                end
            end
        end
        S.isAuraRunning = false
    end)
end
local function runEquipAllTools()
    local char = LocalPlayer.Character
    if not char then return end
    local moved = false
    for _, obj in pairs(LocalPlayer.Backpack:GetChildren()) do
        if obj:IsA("Tool") then
            pcall(function()
                obj.Parent = char
            end)
            moved = true
            task.wait(0.03)
        end
    end
    if moved then
        task.wait(0.05)
        refreshFeTools()
    end
end
S.floatDragLocked = false
local function makeDraggableFloat(btn)
    local fDragging = false
    local fMoved = false
    local fDragStart = nil
    local fDragPos = nil
    local fDragInput = nil
    btn.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1
        or input.UserInputType == Enum.UserInputType.Touch then
            if S.floatDragLocked then
                fDragging = false
                fDragInput = nil
                return
            end
            fDragging = true
            fMoved = false
            fDragStart = input.Position
            fDragPos = btn.Position
            fDragInput = input
            input.Changed:Connect(function()
                if input.UserInputState == Enum.UserInputState.End then
                    fDragging = false
                    fDragInput = nil
                end
            end)
        end
    end)
    UserInputService.InputChanged:Connect(function(input)
        if S.floatDragLocked then
            return
        end
        if fDragging and (
            input == fDragInput or
            input.UserInputType == Enum.UserInputType.MouseMovement
        ) then
            local delta = input.Position - fDragStart
            if delta.Magnitude > 6 then fMoved = true end
            btn.Position = UDim2.new(
                fDragPos.X.Scale, fDragPos.X.Offset + delta.X,
                fDragPos.Y.Scale, fDragPos.Y.Offset + delta.Y
            )
        end
    end)
    return function() return fMoved end, function() fMoved = false end
end
local function updateNoclipFloatVisual()
    if not S.noclipFloatBtn or not S.noclipFloatStroke then return end
    setFloatStrokeActive(S.noclipFloatStroke, S.noclipEnabled)
    S.noclipFloatBtn.Text = "Noclip"
end
local function destroyNoclipFloat()
    if S.noclipFloatBtn then
        S.noclipFloatBtn:Destroy()
        S.noclipFloatBtn = nil
    end
    S.noclipFloatStroke = nil
end
local function createNoclipFloat()
    destroyNoclipFloat()
    S.noclipFloatBtn = Instance.new("TextButton")
    S.noclipFloatBtn.Size = UDim2.new(0, S.floatBtnSize, 0, S.floatBtnSize)
    S.noclipFloatBtn.Position = UDim2.new(0.85, S.floatBtnSize + 14, 0.5, -(S.floatBtnSize * 2 + 18))
    if S.noclipFloatPos then
        local p = S.noclipFloatPos
        S.noclipFloatBtn.Position = UDim2.new(p[1], p[2], p[3], p[4])
    end
    S.noclipFloatBtn.BackgroundColor3 = Color3.fromRGB(20, 20, 20)
    S.noclipFloatBtn.BackgroundTransparency = 0.88
    S.noclipFloatBtn.BorderSizePixel = 0
    S.noclipFloatBtn.Text = "Noclip"
    S.noclipFloatBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
    S.noclipFloatBtn.TextScaled = false
    S.noclipFloatBtn.TextSize = math.max(14, math.floor(S.floatBtnSize * 0.25))
    S.noclipFloatBtn.TextWrapped = true
    S.noclipFloatBtn.Font = Enum.Font.GothamBold
    S.noclipFloatBtn.TextStrokeTransparency = 1
    S.noclipFloatBtn.TextStrokeColor3 = Color3.fromRGB(0, 0, 0)
    S.noclipFloatBtn.Active = true
    S.noclipFloatBtn.ZIndex = 15
    S.noclipFloatBtn.Parent = gui
    local c = Instance.new("UICorner")
    c.CornerRadius = UDim.new(0, 8)
    c.Parent = S.noclipFloatBtn
    local s = Instance.new("UIStroke")
    s.ApplyStrokeMode = Enum.ApplyStrokeMode.Border
    s.Color = Color3.fromRGB(62, 62, 62)
    s.Thickness = 1.8
    s.Transparency = 0
    s.Parent = S.noclipFloatBtn
    S.noclipFloatStroke = s
    local getWasMoved, clearMoved = makeDraggableFloat(S.noclipFloatBtn)
    S.noclipFloatBtn.MouseButton1Click:Connect(function()
        if getWasMoved() then clearMoved(); return end
        _playClick()
        S.noclipEnabled = not S.noclipEnabled
        if S.btnRefs.noclipToggle then
            S.btnRefs.noclipToggle.Text = S.noclipEnabled and "Noclip: ON" or "Noclip: OFF"
            setToggleState(S.btnRefs.noclipToggle, S.noclipEnabled)
        end
        if S.noclipEnabled then
            startNoclip()
        else
            stopNoclip()
        end
        updateNoclipFloatVisual()
    end)
    updateNoclipFloatVisual()
end
local function updateInvisFloatVisual()
    if not S.invisFloatBtn or not S.invisFloatStroke then return end
    setFloatStrokeActive(S.invisFloatStroke, S.isInvisible)
    S.invisFloatBtn.Text = "Invisible"
end
local function destroyInvisFloat()
    if S.invisFloatBtn then
        S.invisFloatBtn:Destroy()
        S.invisFloatBtn = nil
    end
    S.invisFloatStroke = nil
end
local function createInvisFloat()
    destroyInvisFloat()
    S.invisFloatBtn = Instance.new("TextButton")
    S.invisFloatBtn.Size = UDim2.new(0, S.floatBtnSize, 0, S.floatBtnSize)
    S.invisFloatBtn.Position = UDim2.new(0.85, 0, 0.5, -(S.floatBtnSize * 3 + 30))
    if S.invisFloatPos then
        local p = S.invisFloatPos
        S.invisFloatBtn.Position = UDim2.new(p[1], p[2], p[3], p[4])
    end
    S.invisFloatBtn.BackgroundColor3 = Color3.fromRGB(20, 20, 20)
    S.invisFloatBtn.BackgroundTransparency = 0.88
    S.invisFloatBtn.BorderSizePixel = 0
    S.invisFloatBtn.Text = "Invisible"
    S.invisFloatBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
    S.invisFloatBtn.TextScaled = true
    S.invisFloatBtn.TextWrapped = true
    S.invisFloatBtn.Font = Enum.Font.GothamBold
    S.invisFloatBtn.TextStrokeTransparency = 1
    S.invisFloatBtn.TextStrokeColor3 = Color3.fromRGB(0, 0, 0)
    S.invisFloatBtn.Active = true
    S.invisFloatBtn.ZIndex = 15
    S.invisFloatBtn.Parent = gui
    local c = Instance.new("UICorner")
    c.CornerRadius = UDim.new(0, 8)
    c.Parent = S.invisFloatBtn
    local s = Instance.new("UIStroke")
    s.ApplyStrokeMode = Enum.ApplyStrokeMode.Border
    s.Color = Color3.fromRGB(62, 62, 62)
    s.Thickness = 1.8
    s.Transparency = 0
    s.Parent = S.invisFloatBtn
    S.invisFloatStroke = s
    local getWasMoved, clearMoved = makeDraggableFloat(S.invisFloatBtn)
    S.invisFloatBtn.MouseButton1Click:Connect(function()
        if getWasMoved() then clearMoved(); return end
        _playClick()
        toggleInvisibility()
    end)
    updateInvisFloatVisual()
end
local function updateFlyFloatVisual()
    if not S.flyFloatBtn or not S.flyFloatStroke then return end
    setFloatStrokeActive(S.flyFloatStroke, S.flyEnabled)
    S.flyFloatBtn.Text = "Fly"
end
local function destroyFlyFloat()
    if S.flyFloatBtn then
        S.flyFloatBtn:Destroy()
        S.flyFloatBtn = nil
    end
    S.flyFloatStroke = nil
end
local function createFlyFloat()
    destroyFlyFloat()
    S.flyFloatBtn = Instance.new("TextButton")
    S.flyFloatBtn.Size = UDim2.new(0, S.floatBtnSize, 0, S.floatBtnSize)
   S.flyFloatBtn.Position = UDim2.new(0.85, S.floatBtnSize + 14, 0.5, -(S.floatBtnSize * 4 - 16))
    if S.flyFloatPos then
        local p = S.flyFloatPos
        S.flyFloatBtn.Position = UDim2.new(p[1], p[2], p[3], p[4])
    end
    S.flyFloatBtn.BackgroundColor3 = Color3.fromRGB(20, 20, 20)
    S.flyFloatBtn.BackgroundTransparency = 0.88
    S.flyFloatBtn.BorderSizePixel = 0
    S.flyFloatBtn.Text = "Fly"
S.flyFloatBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
S.flyFloatBtn.TextScaled = false
S.flyFloatBtn.TextSize = math.max(16, math.floor(S.floatBtnSize * 0.28))
S.flyFloatBtn.TextWrapped = true
S.flyFloatBtn.Font = Enum.Font.GothamBold
    S.flyFloatBtn.TextStrokeTransparency = 1
    S.flyFloatBtn.TextStrokeColor3 = Color3.fromRGB(0, 0, 0)
    S.flyFloatBtn.Active = true
    S.flyFloatBtn.ZIndex = 15
    S.flyFloatBtn.Parent = gui
    local c = Instance.new("UICorner")
    c.CornerRadius = UDim.new(0, 8)
    c.Parent = S.flyFloatBtn
    local s = Instance.new("UIStroke")
    s.ApplyStrokeMode = Enum.ApplyStrokeMode.Border
    s.Color = Color3.fromRGB(62, 62, 62)
    s.Thickness = 1.8
    s.Transparency = 0
    s.Parent = S.flyFloatBtn
    S.flyFloatStroke = s
    local getWasMoved, clearMoved = makeDraggableFloat(S.flyFloatBtn)
    S.flyFloatBtn.MouseButton1Click:Connect(function()
        if getWasMoved() then clearMoved(); return end
        _playClick()
        S.flySpeed = tonumber(flySpeedInput.Text) or S.flySpeed or 2
        S.flyEnabled = not S.flyEnabled
        S.flyToggle.Text = S.flyEnabled and "Fly: ON" or "Fly: OFF"
        setToggleState(S.flyToggle, S.flyEnabled)
        if S.flyEnabled then
            startFly()
        else
            stopFly()
        end
        updateFlyFloatVisual()
    end)
    updateFlyFloatVisual()
end
local function destroyAuraFloat()
    if S.auraFloatBtn then
        S.auraFloatBtn:Destroy()
        S.auraFloatBtn = nil
    end
end
local function createAuraFloat()
    destroyAuraFloat()
    S.auraFloatBtn = Instance.new("TextButton")
    S.auraFloatBtn.Size = UDim2.new(0, S.floatBtnSize, 0, S.floatBtnSize)
    S.auraFloatBtn.Position = UDim2.new(0.85, 0, 0.5, -(S.floatBtnSize * 2 + 18))
    if S.auraFloatPos then
        local p = S.auraFloatPos
        S.auraFloatBtn.Position = UDim2.new(p[1], p[2], p[3], p[4])
    end
    S.auraFloatBtn.BackgroundColor3 = Color3.fromRGB(20, 20, 20)
    S.auraFloatBtn.BackgroundTransparency = 0.88
    S.auraFloatBtn.BorderSizePixel = 0
    S.auraFloatBtn.Text = "Aura"
    S.auraFloatBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
    S.auraFloatBtn.TextScaled = false
    S.auraFloatBtn.TextSize = math.max(14, math.floor(S.floatBtnSize * 0.25))
    S.auraFloatBtn.TextWrapped = true
    S.auraFloatBtn.Font = Enum.Font.GothamBold
    S.auraFloatBtn.TextStrokeTransparency = 1
    S.auraFloatBtn.TextStrokeColor3 = Color3.fromRGB(0, 0, 0)
    S.auraFloatBtn.Active = true
    S.auraFloatBtn.ZIndex = 15
    S.auraFloatBtn.Parent = gui
    local c = Instance.new("UICorner")
    c.CornerRadius = UDim.new(0, 8)
    c.Parent = S.auraFloatBtn
    local s = Instance.new("UIStroke")
    s.ApplyStrokeMode = Enum.ApplyStrokeMode.Border
    s.Color = Color3.fromRGB(62, 62, 62)
    s.Thickness = 1.8
    s.Transparency = 0
    s.Parent = S.auraFloatBtn
    setFloatStrokeActive(s, false)
    local getWasMoved, clearMoved = makeDraggableFloat(S.auraFloatBtn)
    S.auraFloatBtn.MouseButton1Click:Connect(function()
        if getWasMoved() then clearMoved(); return end
        _playClick()
        runToolAura()
    end)
end
local function destroyDropFloat()
    if S.dropFloatBtn then
        S.dropFloatBtn:Destroy()
        S.dropFloatBtn = nil
    end
end
local function createDropFloat()
    destroyDropFloat()
    S.dropFloatBtn = Instance.new("TextButton")
    S.dropFloatBtn.Size = UDim2.new(0, S.floatBtnSize, 0, S.floatBtnSize)
    S.dropFloatBtn.Position = UDim2.new(0.85, 0, 0.5, -(S.floatBtnSize + 6))
    if S.dropFloatPos then
        local p = S.dropFloatPos
        S.dropFloatBtn.Position = UDim2.new(p[1], p[2], p[3], p[4])
    end
    S.dropFloatBtn.BackgroundColor3 = Color3.fromRGB(20, 20, 20)
    S.dropFloatBtn.BackgroundTransparency = 0.88
    S.dropFloatBtn.BorderSizePixel = 0
    S.dropFloatBtn.Text = "Drop\nTool"
    S.dropFloatBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
    S.dropFloatBtn.TextScaled = false
    S.dropFloatBtn.TextSize = math.max(14, math.floor(S.floatBtnSize * 0.25))
    S.dropFloatBtn.TextWrapped = true
    S.dropFloatBtn.Font = Enum.Font.GothamBold
    S.dropFloatBtn.TextStrokeTransparency = 1
    S.dropFloatBtn.TextStrokeColor3 = Color3.fromRGB(0, 0, 0)
    S.dropFloatBtn.Active = true
    S.dropFloatBtn.ZIndex = 15
    S.dropFloatBtn.Parent = gui
    local c = Instance.new("UICorner")
    c.CornerRadius = UDim.new(0, 8)
    c.Parent = S.dropFloatBtn
    local s = Instance.new("UIStroke")
    s.ApplyStrokeMode = Enum.ApplyStrokeMode.Border
    s.Color = Color3.fromRGB(62, 62, 62)
    s.Thickness = 1.8
    s.Transparency = 0
    s.Parent = S.dropFloatBtn
    setFloatStrokeActive(s, false)
    local getWasMoved, clearMoved = makeDraggableFloat(S.dropFloatBtn)
    S.dropFloatBtn.MouseButton1Click:Connect(function()
        if getWasMoved() then clearMoved(); return end
        _playClick()
        local char = LocalPlayer.Character
        if not char then return end
        local tool = char:FindFirstChildOfClass("Tool")
        if tool then tool.Parent = workspace end
    end)
end
local function destroyEquipAllFloat()
    if S.equipAllFloatBtn then
        S.equipAllFloatBtn:Destroy()
        S.equipAllFloatBtn = nil
    end
end
local function createEquipAllFloat()
    destroyEquipAllFloat()
    S.equipAllFloatBtn = Instance.new("TextButton")
    S.equipAllFloatBtn.Size = UDim2.new(0, S.floatBtnSize, 0, S.floatBtnSize)
    S.equipAllFloatBtn.Position = UDim2.new(0.85, 0, 0.5, 6)
    if S.equipAllFloatPos then
        local p = S.equipAllFloatPos
        S.equipAllFloatBtn.Position = UDim2.new(p[1], p[2], p[3], p[4])
    end
    S.equipAllFloatBtn.BackgroundColor3 = Color3.fromRGB(20, 20, 20)
    S.equipAllFloatBtn.BackgroundTransparency = 0.88
    S.equipAllFloatBtn.BorderSizePixel = 0
    S.equipAllFloatBtn.Text = "Equip\nAll"
    S.equipAllFloatBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
    S.equipAllFloatBtn.TextScaled = false
    S.equipAllFloatBtn.TextSize = math.max(14, math.floor(S.floatBtnSize * 0.25))
    S.equipAllFloatBtn.TextWrapped = true
    S.equipAllFloatBtn.Font = Enum.Font.GothamBold
    S.equipAllFloatBtn.TextStrokeTransparency = 1
    S.equipAllFloatBtn.TextStrokeColor3 = Color3.fromRGB(0, 0, 0)
    S.equipAllFloatBtn.Active = true
    S.equipAllFloatBtn.ZIndex = 15
    S.equipAllFloatBtn.Parent = gui
    local c = Instance.new("UICorner")
    c.CornerRadius = UDim.new(0, 8)
    c.Parent = S.equipAllFloatBtn
    local s = Instance.new("UIStroke")
    s.ApplyStrokeMode = Enum.ApplyStrokeMode.Border
    s.Color = Color3.fromRGB(62, 62, 62)
    s.Thickness = 1.8
    s.Transparency = 0
    s.Parent = S.equipAllFloatBtn
    setFloatStrokeActive(s, false)
    local getWasMoved, clearMoved = makeDraggableFloat(S.equipAllFloatBtn)
    S.equipAllFloatBtn.MouseButton1Click:Connect(function()
        if getWasMoved() then
            clearMoved()
            return
        end
        _playClick()
        runEquipAllTools()
    end)
end
local function destroyUseFloat()
    if S.useFloatBtn then
        S.useFloatBtn:Destroy()
        S.useFloatBtn = nil
    end
end
local function findUseTool(name)
    local low = name:lower()
    local char = LocalPlayer.Character
    for _, v in ipairs(LocalPlayer.Backpack:GetChildren()) do
        if v:IsA("Tool") and v.Name:lower() == low then return v end
    end
    if char then
        for _, v in ipairs(char:GetChildren()) do
            if v:IsA("Tool") and v.Name:lower() == low then return v end
        end
    end
end
local function doUseTool()
    if S.useCooling then return end
    if not S.feUseInput then return end
    local name = S.feUseInput.Text:match("^%s*(.-)%s*$")
    if name == "" then
        showCustomNotif("StarCrow Says:\n\n   Item name not entered", 3, "fail")
        return
    end
    local char = LocalPlayer.Character
    local human = char and char:FindFirstChildOfClass("Humanoid")
    if not char or not human then
        showCustomNotif("StarCrow Says:\n\n   Character not found", 3, "fail")
        return
    end
    local tool = findUseTool(name)
    if not tool then
        showCustomNotif("StarCrow Says:\n\n   Not found: " .. name, 3, "fail")
        return
    end
    S.useCooling = true
    if S.useFloatBtn then S.useFloatBtn.Text = "..." end
    if tool.Parent == LocalPlayer.Backpack then
        human:EquipTool(tool)
        task.wait(0.12)
    end
    local eq = char:FindFirstChildWhichIsA("Tool")
    if eq and eq.Name:lower() == name:lower() then
        eq:Activate()
    else
        showCustomNotif("StarCrow Says:\n\n   Equip failed", 3, "fail")
    end
    task.wait(0.28)
    if S.useFloatBtn then S.useFloatBtn.Text = "USE" end
    S.useCooling = false
end
local function createUseFloat()
    destroyUseFloat()
    S.useFloatBtn = Instance.new("TextButton")
    S.useFloatBtn.Size = UDim2.new(0, S.floatBtnSize, 0, S.floatBtnSize)
    S.useFloatBtn.Position = UDim2.new(0.85, 0, 0.5, S.floatBtnSize + 18)
    if S.useFloatPos then
        local p = S.useFloatPos
        S.useFloatBtn.Position = UDim2.new(p[1], p[2], p[3], p[4])
    end
    S.useFloatBtn.BackgroundColor3 = Color3.fromRGB(20, 20, 20)
    S.useFloatBtn.BackgroundTransparency = 0.88
    S.useFloatBtn.BorderSizePixel = 0
    S.useFloatBtn.Text = "USE"
    S.useFloatBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
    S.useFloatBtn.TextScaled = false
    S.useFloatBtn.TextSize = math.max(14, math.floor(S.floatBtnSize * 0.25))
    S.useFloatBtn.TextWrapped = true
    S.useFloatBtn.Font = Enum.Font.GothamBold
    S.useFloatBtn.TextStrokeTransparency = 1
    S.useFloatBtn.TextStrokeColor3 = Color3.fromRGB(0, 0, 0)
    S.useFloatBtn.Active = true
    S.useFloatBtn.ZIndex = 15
    S.useFloatBtn.Parent = gui
    local c = Instance.new("UICorner")
    c.CornerRadius = UDim.new(0, 8)
    c.Parent = S.useFloatBtn
    local s = Instance.new("UIStroke")
    s.ApplyStrokeMode = Enum.ApplyStrokeMode.Border
    s.Color = Color3.fromRGB(62, 62, 62)
    s.Thickness = 1.8
    s.Transparency = 0
    s.Parent = S.useFloatBtn
    setFloatStrokeActive(s, false)
    local getWasMoved, clearMoved = makeDraggableFloat(S.useFloatBtn)
    S.useFloatBtn.MouseButton1Click:Connect(function()
        if getWasMoved() then clearMoved(); return end
        _playClick()
        doUseTool()
    end)
end
S.feAuraBtn = createTransparentButton("Tool Aura", Color3.fromRGB(38, 38, 38), panel8)
S.feAuraBtn.BackgroundTransparency = 0.35
S.feAuraBtn.BackgroundColor3 = Color3.fromRGB(38, 38, 38)
S.feAuraBtn.MouseButton1Click:Connect(function()
    runToolAura()
end)
local feAuraFloatBtn = createTransparentButton("Aura Float: OFF", Color3.fromRGB(38, 38, 38), panel8)
setToggleState(feAuraFloatBtn, false)
S.btnRefs.feAuraFloatBtn = feAuraFloatBtn
feAuraFloatBtn.MouseButton1Click:Connect(function()
    S.auraFloatEnabled = not S.auraFloatEnabled
    if S.auraFloatEnabled then
        feAuraFloatBtn.Text = "Aura Float: ON"
        setToggleState(feAuraFloatBtn, true)
        createAuraFloat()
    else
        feAuraFloatBtn.Text = "Aura Float: OFF"
        setToggleState(feAuraFloatBtn, false)
        destroyAuraFloat()
    end
end)
local feDropBtn = createTransparentButton("Drop Tool", Color3.fromRGB(38, 38, 38), panel8)
feDropBtn.BackgroundTransparency = 0.35
feDropBtn.BackgroundColor3 = Color3.fromRGB(38, 38, 38)
feDropBtn.MouseButton1Click:Connect(function()
    local char = LocalPlayer.Character
    if not char then return end
    local tool = char:FindFirstChildOfClass("Tool")
    if tool then tool.Parent = workspace end
end)
local feDropFloatBtn = createTransparentButton("Drop Float: OFF", Color3.fromRGB(38, 38, 38), panel8)
setToggleState(feDropFloatBtn, false)
S.btnRefs.feDropFloatBtn = feDropFloatBtn
feDropFloatBtn.MouseButton1Click:Connect(function()
    S.dropFloatEnabled = not S.dropFloatEnabled
    if S.dropFloatEnabled then
        feDropFloatBtn.Text = "Drop Float: ON"
        setToggleState(feDropFloatBtn, true)
        createDropFloat()
    else
        feDropFloatBtn.Text = "Drop Float: OFF"
        setToggleState(feDropFloatBtn, false)
        destroyDropFloat()
    end
end)
local feEquipAllBtn = createTransparentButton("Equip All", Color3.fromRGB(38, 38, 38), panel8)
feEquipAllBtn.BackgroundTransparency = 0.35
feEquipAllBtn.BackgroundColor3 = Color3.fromRGB(38, 38, 38)
feEquipAllBtn.MouseButton1Click:Connect(function()
    runEquipAllTools()
end)
local feEquipAllFloatToggleBtn = createTransparentButton("Equip All Float: OFF", Color3.fromRGB(38, 38, 38), panel8)
setToggleState(feEquipAllFloatToggleBtn, false)
S.btnRefs.feEquipAllFloatToggleBtn = feEquipAllFloatToggleBtn
feEquipAllFloatToggleBtn.MouseButton1Click:Connect(function()
    S.equipAllFloatEnabled = not S.equipAllFloatEnabled
    if S.equipAllFloatEnabled then
        feEquipAllFloatToggleBtn.Text = "Equip All Float: ON"
        setToggleState(feEquipAllFloatToggleBtn, true)
        createEquipAllFloat()
    else
        feEquipAllFloatToggleBtn.Text = "Equip All Float: OFF"
        setToggleState(feEquipAllFloatToggleBtn, false)
        destroyEquipAllFloat()
    end
end)
do
    local useDiv = createTransparentLabel(" Use Tool", panel8)
    useDiv.TextColor3 = Color3.fromRGB(255, 255, 255)
end
S.feUseInput = createTransparentInput("Item name", "", panel8)
local feUseBtn = createTransparentButton("Use Tool", Color3.fromRGB(38, 38, 38), panel8)
feUseBtn.BackgroundTransparency = 0.35
feUseBtn.BackgroundColor3 = Color3.fromRGB(38, 38, 38)
feUseBtn.MouseButton1Click:Connect(function()
    doUseTool()
end)
local feUseFloatToggleBtn = createTransparentButton("Use Float: OFF", Color3.fromRGB(38, 38, 38), panel8)
setToggleState(feUseFloatToggleBtn, false)
S.btnRefs.feUseFloatToggleBtn = feUseFloatToggleBtn
feUseFloatToggleBtn.MouseButton1Click:Connect(function()
    S.useFloatEnabled = not S.useFloatEnabled
    if S.useFloatEnabled then
        feUseFloatToggleBtn.Text = "Use Float: ON"
        setToggleState(feUseFloatToggleBtn, true)
        createUseFloat()
    else
        feUseFloatToggleBtn.Text = "Use Float: OFF"
        setToggleState(feUseFloatToggleBtn, false)
        destroyUseFloat()
    end
end)
do
    local feEquipDiv = createTransparentLabel(" Equip Tool ", panel8)
    feEquipDiv.TextColor3 = Color3.fromRGB(255, 255, 255)
end
local feToolScroll = Instance.new("ScrollingFrame")
feToolScroll.Size = UDim2.new(1, 0, 0, 95)
feToolScroll.BackgroundColor3 = Color3.fromRGB(22, 22, 22)
feToolScroll.BackgroundTransparency = 0.35
feToolScroll.BorderSizePixel = 0
feToolScroll.ScrollBarThickness = 3
feToolScroll.ScrollBarImageColor3 = Color3.fromRGB(200, 25, 25)
feToolScroll.CanvasSize = UDim2.new(0, 0, 0, 0)
feToolScroll.AutomaticCanvasSize = Enum.AutomaticSize.Y
feToolScroll.Parent = panel8
do
    local feTSLayout = Instance.new("UIListLayout", feToolScroll)
    feTSLayout.SortOrder = Enum.SortOrder.LayoutOrder
    feTSLayout.Padding = UDim.new(0, 2)
    local feTSPad = Instance.new("UIPadding", feToolScroll)
    feTSPad.PaddingLeft = UDim.new(0, 3)
    feTSPad.PaddingRight = UDim.new(0, 3)
    feTSPad.PaddingTop = UDim.new(0, 3)
    feTSPad.PaddingBottom = UDim.new(0, 3)
    _mkCorner(5, feToolScroll)
end
local function refreshFeTools()
    for _, child in pairs(feToolScroll:GetChildren()) do
        if child:IsA("TextButton") or (child:IsA("TextLabel") and child.Name == "EmptyLabel") then
            child:Destroy()
        end
    end
    local char = LocalPlayer.Character
    local seen = {}
    local toolList = {}
    for _, obj in pairs(LocalPlayer.Backpack:GetChildren()) do
        if obj:IsA("Tool") and not seen[obj.Name] then
            seen[obj.Name] = true
            table.insert(toolList, {
                Name = obj.Name,
                Equipped = false
            })
        end
    end
    if char then
        for _, obj in pairs(char:GetChildren()) do
            if obj:IsA("Tool") and not seen[obj.Name] then
                seen[obj.Name] = true
                table.insert(toolList, {
                    Name = obj.Name,
                    Equipped = true
                })
            end
        end
    end
    for order, data in ipairs(toolList) do
        local tName = data.Name
        local tBtn = Instance.new("TextButton")
        tBtn.Parent = feToolScroll
        tBtn.Size = UDim2.new(1, 0, 0, 22)
        tBtn.BackgroundColor3 = Color3.fromRGB(38, 38, 38)
        tBtn.BackgroundTransparency = 0.35
        tBtn.BorderSizePixel = 0
        tBtn.Text = data.Equipped and (tName .. " [EQUIPPED]") or tName
        tBtn.TextColor3 = data.Equipped and Color3.fromRGB(140, 255, 170) or Color3.fromRGB(230, 230, 230)
        tBtn.TextScaled = false
        tBtn.TextSize = 11
        tBtn.Font = Enum.Font.GothamMedium
        tBtn.TextXAlignment = Enum.TextXAlignment.Left
        tBtn.LayoutOrder = order
        local tc = Instance.new("UICorner", tBtn)
        tc.CornerRadius = UDim.new(0, 4)
        local pad = Instance.new("UIPadding", tBtn)
        pad.PaddingLeft = UDim.new(0, 8)
        pad.PaddingRight = UDim.new(0, 6)
        tBtn.MouseButton1Click:Connect(function()
            local char = LocalPlayer.Character
            if not char then return end
            if data.Equipped then
                return
            end
            local tool = LocalPlayer.Backpack:FindFirstChild(tName)
            if tool and tool:IsA("Tool") then
                pcall(function()
                    tool.Parent = char
                end)
            end
            task.wait(0.05)
            refreshFeTools()
        end)
    end
    if #toolList == 0 then
        local empty = Instance.new("TextLabel")
        empty.Parent = feToolScroll
        empty.Name = "EmptyLabel"
        empty.Size = UDim2.new(1, 0, 0, 22)
        empty.BackgroundTransparency = 1
        empty.Text = "Backpack is empty"
        empty.TextColor3 = Color3.fromRGB(120, 120, 120)
        empty.TextScaled = false
        empty.TextSize = 11
        empty.Font = Enum.Font.Gotham
    end
end
local feRefreshBtn = createTransparentButton("Refresh Tools", Color3.fromRGB(38, 38, 38), panel8)
feRefreshBtn.BackgroundTransparency = 0.35
feRefreshBtn.BackgroundColor3 = Color3.fromRGB(38, 38, 38)
feRefreshBtn.MouseButton1Click:Connect(function()
    refreshFeTools()
end)
local function scheduleFeRefresh()
    if S.feRefreshBusy then return end
    S.feRefreshBusy = true
    task.delay(0.06, function()
        S.feRefreshBusy = false
        pcall(function()
            refreshFeTools()
        end)
    end)
end
LocalPlayer.Backpack.ChildAdded:Connect(function(obj)
    if obj:IsA("Tool") then
        scheduleFeRefresh()
    end
end)
LocalPlayer.Backpack.ChildRemoved:Connect(function(obj)
    if obj:IsA("Tool") then
        scheduleFeRefresh()
    end
end)
local function bindFeToolCharacter(char)
    if not char then return end
    char.ChildAdded:Connect(function(obj)
        if obj:IsA("Tool") then
            scheduleFeRefresh()
        end
    end)
    char.ChildRemoved:Connect(function(obj)
        if obj:IsA("Tool") then
            scheduleFeRefresh()
        end
    end)
    local hum = char:FindFirstChildOfClass("Humanoid")
    if hum then
        hum.Died:Connect(function()
            scheduleFeRefresh()
        end)
    end
    scheduleFeRefresh()
end
if LocalPlayer.Character then
    bindFeToolCharacter(LocalPlayer.Character)
end
LocalPlayer.CharacterAdded:Connect(function(char)
    task.wait(0.15)
    bindFeToolCharacter(char)
end)
refreshFeTools()
do
    local function _setupAimSection()
local function _mkAimDrop(par, onSel)
    local TS = game:GetService("TweenService")
    local IH, HH = 30, 40
    local pts = {
        {l="Head",  v="Head"},
        {l="Body",  v="HumanoidRootPart"},
    }
    local ci = 1
    local w = Instance.new("Frame")
    w.Size = UDim2.new(1,0,0,HH); w.BackgroundColor3 = Color3.fromRGB(38,38,38)
    w.BackgroundTransparency = 0.35; w.BorderSizePixel = 0
    w.ClipsDescendants = true; w.Parent = par
    Instance.new("UICorner",w).CornerRadius = UDim.new(0,8)
    local g = Instance.new("UIGradient",w)
    g.Color = ColorSequence.new({ColorSequenceKeypoint.new(0,Color3.fromRGB(255,255,255)),ColorSequenceKeypoint.new(1,Color3.fromRGB(200,200,255))})
    g.Transparency = NumberSequence.new({NumberSequenceKeypoint.new(0,0.1),NumberSequenceKeypoint.new(1,0.25)})
    g.Rotation = 90
    local tl = Instance.new("TextLabel",w)
    tl.Size=UDim2.new(1,-40,0,13); tl.Position=UDim2.new(0,12,0,4)
    tl.BackgroundTransparency=1; tl.Text="Target Aimpart"
    tl.TextColor3=Color3.fromRGB(175,175,200); tl.Font=Enum.Font.Gotham
    tl.TextSize=10; tl.TextXAlignment=Enum.TextXAlignment.Left
    local cl = Instance.new("TextLabel",w)
    cl.Size=UDim2.new(1,-44,0,18); cl.Position=UDim2.new(0,12,0,18)
    cl.BackgroundTransparency=1; cl.Text=pts[ci].l
    cl.TextColor3=Color3.fromRGB(255,255,255); cl.Font=Enum.Font.GothamBold
    cl.TextSize=13; cl.TextXAlignment=Enum.TextXAlignment.Left
    local ar = Instance.new("TextLabel",w)
    ar.Size=UDim2.new(0,28,0,HH); ar.Position=UDim2.new(1,-32,0,0)
    ar.BackgroundTransparency=1; ar.Text="▲"
    ar.TextColor3=Color3.fromRGB(220,220,220); ar.Font=Enum.Font.GothamBold
    ar.TextSize=10; ar.TextYAlignment=Enum.TextYAlignment.Center
    local dv = Instance.new("Frame",w)
    dv.Size=UDim2.new(1,-24,0,1); dv.Position=UDim2.new(0,12,0,HH)
    dv.BackgroundColor3=Color3.fromRGB(120,120,140); dv.BackgroundTransparency=0.4
    dv.BorderSizePixel=0
    local hb = Instance.new("TextButton",w)
    hb.Size=UDim2.new(1,0,0,HH); hb.BackgroundTransparency=1; hb.Text=""; hb.ZIndex=5
    local ibs = {}
    for i,p in ipairs(pts) do
        local ib = Instance.new("TextButton",w)
        ib.Size=UDim2.new(1,-24,0,IH-4)
        ib.Position=UDim2.new(0,12,0,HH+4+(i-1)*IH)
        ib.BackgroundTransparency=1; ib.Text=p.l
        ib.TextColor3=(i==ci) and Color3.fromRGB(255,255,255) or Color3.fromRGB(175,175,200)
        ib.Font=(i==ci) and Enum.Font.GothamSemibold or Enum.Font.Gotham
        ib.TextSize=13; ib.TextXAlignment=Enum.TextXAlignment.Left; ib.ZIndex=5
        ibs[i]=ib
    end
    local TH = HH+4+#pts*IH
    local open = false
    local function doSel(idx)
        ci=idx; cl.Text=pts[idx].l
        for j,ib in ipairs(ibs) do
            ib.TextColor3=(j==idx) and Color3.fromRGB(255,255,255) or Color3.fromRGB(175,175,200)
            ib.Font=(j==idx) and Enum.Font.GothamSemibold or Enum.Font.Gotham
        end
        onSel(pts[idx].v)
    end
    local function tog()
        open=not open; ar.Text=open and "▼" or "▲"
        TS:Create(w,TweenInfo.new(0.15,Enum.EasingStyle.Quad),{Size=UDim2.new(1,0,0,open and TH or HH)}):Play()
    end
    hb.MouseButton1Click:Connect(function() _playClick(); tog() end)
    for i,ib in ipairs(ibs) do
        ib.MouseButton1Click:Connect(function()
            _playClick(); doSel(i)
            if open then tog() end
        end)
    end
    return doSel
end

        local _lbl = createTransparentLabel("Aimbot", panel9)
            _lbl.TextColor3 = Color3.fromRGB(255, 255, 255)
            _lbl.Font = Enum.Font.GothamBold
            _lbl.TextSize = 14
            local AimSettings = {
                AimEnabled          = false,
                FOV                 = 75,
                AimPart             = "Head",
                WallCheck           = false,
                HPCheck             = true,
                ShowFOV             = false,
                TeamCheck           = false,
                FriendCheck         = false,
                ForceFieldCheck     = true,
                Smoothness          = 0.90,
                FOVOffsetX          = 0,
                FOVOffsetY          = -55,
                Prediction          = true,
                PredictionAmount    = 0.12,
                AirPrediction       = true,
                AirPredictionAmount = 0.16,
            }
            S.aimSettings = AimSettings
            local AIM_ON  = Color3.fromRGB(50, 200, 120)
            local AIM_OFF = Color3.fromRGB(38, 38, 38)
            local FOV_COL = Color3.fromRGB(80, 255, 100)
            pcall(function()
                local cg = game:GetService("CoreGui")
                if cg:FindFirstChild("AimAssistUI") then cg.AimAssistUI:Destroy() end
            end)
            local aimGui = Instance.new("ScreenGui")
            aimGui.Name             = "AimAssistUI"
            aimGui.ResetOnSpawn     = false
            aimGui.ZIndexBehavior   = Enum.ZIndexBehavior.Sibling
            local _cgOk = pcall(function() aimGui.Parent = game:GetService("CoreGui") end)
            if not _cgOk then aimGui.Parent = LocalPlayer.PlayerGui end
            local FOVCircleAim = Instance.new("Frame")
            FOVCircleAim.Name                   = "FOVCircle"
            FOVCircleAim.Parent                 = aimGui
            FOVCircleAim.AnchorPoint            = Vector2.new(0.5, 0.5)
            FOVCircleAim.BackgroundTransparency = 1
            FOVCircleAim.BorderSizePixel        = 0
            FOVCircleAim.ZIndex                 = 999
            FOVCircleAim.Visible                = AimSettings.ShowFOV
            do
                local _fc = Instance.new("UICorner", FOVCircleAim)
                _fc.CornerRadius = UDim.new(1, 0)
                local _fs = Instance.new("UIStroke", FOVCircleAim)
                _fs.ApplyStrokeMode = Enum.ApplyStrokeMode.Border
                _fs.Color           = FOV_COL
                _fs.Thickness       = 2
                _fs.Transparency    = 0.08
            end
            local aimToggleBtn = createTransparentButton("Aimbot: OFF", AIM_OFF, panel9)
            setToggleState(aimToggleBtn, false)
            S.btnRefs.aimToggleBtn = aimToggleBtn
            local wallCheckBtn = createTransparentButton("Wall Check: OFF", AIM_OFF, panel9)
            setToggleState(wallCheckBtn, AimSettings.WallCheck)
            S.btnRefs.wallCheckBtn = wallCheckBtn
            local teamCheckBtn = createTransparentButton("Team Check: OFF", AIM_OFF, panel9)
            setToggleState(teamCheckBtn, false)
            S.btnRefs.teamCheckBtn = teamCheckBtn
           local friendCheckBtn = createTransparentButton("Friend Check: OFF", AIM_OFF, panel9)
            setToggleState(friendCheckBtn, AimSettings.FriendCheck)
            S.btnRefs.friendCheckBtn = friendCheckBtn
            local predictionBtn = createTransparentButton("Prediction: ON", AIM_OFF, panel9)
            setToggleState(predictionBtn, AimSettings.Prediction)
            S.btnRefs.predictionBtn = predictionBtn
            _mkAimDrop(panel9, function(v) AimSettings.AimPart = v end)
            local function _setupAimFOVSlider(panel, aimSettings, fovCircle)
                local FOV_MIN = 20
                local FOV_MAX = 300
                local UIS2 = game:GetService("UserInputService")
                local TS2  = game:GetService("TweenService")
                local fovLabelRow = Instance.new("Frame")
                fovLabelRow.BackgroundTransparency = 1
                fovLabelRow.Size   = UDim2.new(1, 0, 0, 16)
                fovLabelRow.Parent = panel
                local fovLbl = Instance.new("TextLabel")
                fovLbl.Size                   = UDim2.new(0.5, 0, 1, 0)
                fovLbl.BackgroundTransparency = 1
                fovLbl.Text                   = "FOV"
                fovLbl.TextColor3             = Color3.fromRGB(180, 180, 180)
                fovLbl.Font                   = Enum.Font.Gotham
                fovLbl.TextSize               = 12
                fovLbl.TextXAlignment         = Enum.TextXAlignment.Left
                fovLbl.Parent                 = fovLabelRow
                local fovValLbl = Instance.new("TextLabel")
                fovValLbl.Size                   = UDim2.new(0.5, 0, 1, 0)
                fovValLbl.Position               = UDim2.new(0.5, 0, 0, 0)
                fovValLbl.BackgroundTransparency = 1
                fovValLbl.Text                   = tostring(aimSettings.FOV)
                fovValLbl.TextColor3             = Color3.fromRGB(200, 200, 200)
                fovValLbl.Font                   = Enum.Font.GothamBold
                fovValLbl.TextSize               = 12
                fovValLbl.TextXAlignment         = Enum.TextXAlignment.Right
                fovValLbl.Parent                 = fovLabelRow
                local sliderWrapper = Instance.new("Frame")
                sliderWrapper.Size                   = UDim2.new(1, 0, 0, 20)
                sliderWrapper.BackgroundTransparency = 1
                sliderWrapper.BorderSizePixel        = 0
                sliderWrapper.Active                 = true
                sliderWrapper.Parent                 = panel
                local trackBg = Instance.new("Frame")
                trackBg.Size                   = UDim2.new(1, 0, 0, 6)
                trackBg.Position               = UDim2.new(0, 0, 0.5, -3)
                trackBg.BackgroundColor3       = Color3.fromRGB(55, 55, 60)
                trackBg.BackgroundTransparency = 0
                trackBg.BorderSizePixel        = 0
                trackBg.Active                 = true
                trackBg.Parent                 = sliderWrapper
                Instance.new("UICorner", trackBg).CornerRadius = UDim.new(1, 0)
                local trackGrad = Instance.new("UIGradient")
                trackGrad.Color = ColorSequence.new({
                    ColorSequenceKeypoint.new(0, Color3.fromRGB(35,35,38)),
                    ColorSequenceKeypoint.new(1, Color3.fromRGB(70,70,75)),
                })
                trackGrad.Rotation = 0
                trackGrad.Parent   = trackBg
                local trackFill = Instance.new("Frame")
                trackFill.Size                = UDim2.new(0, 0, 1, 0)
                trackFill.BackgroundColor3    = Color3.fromRGB(160, 160, 170)
                trackFill.BackgroundTransparency = 0
                trackFill.BorderSizePixel     = 0
                trackFill.ZIndex              = 2
                trackFill.Parent              = trackBg
                Instance.new("UICorner", trackFill).CornerRadius = UDim.new(1, 0)
                local knob = Instance.new("Frame")
                knob.Size                   = UDim2.new(0, 14, 0, 14)
                knob.AnchorPoint            = Vector2.new(0.5, 0.5)
                knob.Position               = UDim2.new(0, 0, 0.5, 0)
                knob.BackgroundColor3       = Color3.fromRGB(255, 255, 255)
                knob.BackgroundTransparency = 0
                knob.BorderSizePixel        = 0
                knob.ZIndex                 = 10
                knob.Active                 = true
                knob.Parent                 = trackBg
                Instance.new("UICorner", knob).CornerRadius = UDim.new(1, 0)
                local fovDragging = false
                local fovDragInput2 = nil
                local function _setFOV(val)
                    aimSettings.FOV = math.clamp(math.round(val), FOV_MIN, FOV_MAX)
                    local pct = (aimSettings.FOV - FOV_MIN) / (FOV_MAX - FOV_MIN)
                    trackFill.Size   = UDim2.new(pct, 0, 1, 0)
                    knob.Position    = UDim2.new(pct, 0, 0.5, 0)
                    fovValLbl.Text   = tostring(aimSettings.FOV)
                    fovCircle.Size   = UDim2.new(0, aimSettings.FOV * 2, 0, aimSettings.FOV * 2)
                end
                _setFOV(aimSettings.FOV)
                local function _calcFOVFromX(inputX)
                    local absPos  = trackBg.AbsolutePosition.X
                    local absSize = trackBg.AbsoluteSize.X
                    local relX    = math.clamp(inputX - absPos, 0, absSize)
                    _setFOV(FOV_MIN + (relX / absSize) * (FOV_MAX - FOV_MIN))
                end
                sliderWrapper.InputBegan:Connect(function(input)
                    if input.UserInputType == Enum.UserInputType.MouseButton1
                    or input.UserInputType == Enum.UserInputType.Touch then
                        if fovDragging then return end
                        fovDragging = true
                        fovDragInput2 = input
                        _calcFOVFromX(input.Position.X)
                        TS2:Create(knob, TweenInfo.new(0.08), { Size = UDim2.new(0, 17, 0, 17) }):Play()
                    end
                end)
                trackBg.InputBegan:Connect(function(input)
                    if input.UserInputType == Enum.UserInputType.MouseButton1
                    or input.UserInputType == Enum.UserInputType.Touch then
                        if fovDragging then return end
                        fovDragging = true
                        fovDragInput2 = input
                        _calcFOVFromX(input.Position.X)
                        TS2:Create(knob, TweenInfo.new(0.08), { Size = UDim2.new(0, 17, 0, 17) }):Play()
                    end
                end)
                UIS2.InputChanged:Connect(function(input)
                    if not fovDragging then return end
                    if input.UserInputType == Enum.UserInputType.MouseMovement then
                        _calcFOVFromX(input.Position.X)
                    elseif input.UserInputType == Enum.UserInputType.Touch and input == fovDragInput2 then
                        _calcFOVFromX(input.Position.X)
                    end
                end)
                UIS2.InputEnded:Connect(function(input)
                    if not fovDragging then return end
                    if input.UserInputType == Enum.UserInputType.MouseButton1
                    or (input.UserInputType == Enum.UserInputType.Touch and input == fovDragInput2) then
                        fovDragging = false
                        fovDragInput2 = nil
                        TS2:Create(knob, TweenInfo.new(0.08), { Size = UDim2.new(0, 14, 0, 14) }):Play()
                    end
                end)
            end
            _setupAimFOVSlider(panel9, AimSettings, FOVCircleAim)
            aimToggleBtn.MouseButton1Click:Connect(function()
                AimSettings.AimEnabled = not AimSettings.AimEnabled
                AimSettings.ShowFOV    = AimSettings.AimEnabled
               aimToggleBtn.Text = "Aimbot: " .. (AimSettings.AimEnabled and "ON" or "OFF")
                setToggleState(aimToggleBtn, AimSettings.AimEnabled)
                FOVCircleAim.Visible = AimSettings.ShowFOV
            end)
            wallCheckBtn.MouseButton1Click:Connect(function()
                AimSettings.WallCheck = not AimSettings.WallCheck
                wallCheckBtn.Text = "Wall Check: " .. (AimSettings.WallCheck and "ON" or "OFF")
                setToggleState(wallCheckBtn, AimSettings.WallCheck)
            end)
            teamCheckBtn.MouseButton1Click:Connect(function()
                AimSettings.TeamCheck = not AimSettings.TeamCheck
                teamCheckBtn.Text = "Team Check: " .. (AimSettings.TeamCheck and "ON" or "OFF")
                setToggleState(teamCheckBtn, AimSettings.TeamCheck)
            end)
            friendCheckBtn.MouseButton1Click:Connect(function()
                AimSettings.FriendCheck = not AimSettings.FriendCheck
                friendCheckBtn.Text = "Friend Check: " .. (AimSettings.FriendCheck and "ON" or "OFF")
                setToggleState(friendCheckBtn, AimSettings.FriendCheck)
            end)
            predictionBtn.MouseButton1Click:Connect(function()
                AimSettings.Prediction = not AimSettings.Prediction
                predictionBtn.Text = "Prediction: " .. (AimSettings.Prediction and "ON" or "OFF")
                setToggleState(predictionBtn, AimSettings.Prediction)
            end)
            local function _aimAlive(char)
            local h = char and char:FindFirstChildOfClass("Humanoid")
            return h and h.Health > 0
        end
        local AimFriendCache = {}
        Players.PlayerRemoving:Connect(function(player)
            AimFriendCache[player.UserId] = nil
        end)
        local function _aimFriend(player)
            if not AimSettings.FriendCheck then return false end
            local cached = AimFriendCache[player.UserId]
            if cached ~= nil then
                return cached
            end
            local isFriend = false
            local ok, res = pcall(function()
                return LocalPlayer:IsFriendsWith(player.UserId)
            end)
            if ok and res then
                isFriend = true
            elseif player:GetAttribute("Friendly") == true then
                isFriend = true
            end
            AimFriendCache[player.UserId] = isFriend
            return isFriend
        end
            local function _aimHasFF(char)
                if not AimSettings.ForceFieldCheck then return false end
                if char:FindFirstChildOfClass("ForceField") then return true end
                return char:GetAttribute("SpawnProtected") == true
            end
            local function _aimCanSee(part, char)
                if not AimSettings.WallCheck then return true end
                local rp = RaycastParams.new()
                rp.FilterType = Enum.RaycastFilterType.Exclude
                rp.FilterDescendantsInstances = {LocalPlayer.Character, Camera}
                rp.IgnoreWater = true
                local result = workspace:Raycast(
                    Camera.CFrame.Position,
                    part.Position - Camera.CFrame.Position,
                    rp
                )
                if result then return result.Instance:IsDescendantOf(char) end
                return true
            end
            local function _getClosest()
                local closest, shortest = nil, AimSettings.FOV
                local cx = Camera.ViewportSize.X / 2 + AimSettings.FOVOffsetX
                local cy = Camera.ViewportSize.Y / 2 + AimSettings.FOVOffsetY
                for _, p in ipairs(Players:GetPlayers()) do
                    if p == LocalPlayer then continue end
                    if AimSettings.TeamCheck and LocalPlayer.Team ~= nil and p.Team == LocalPlayer.Team then continue end
                    if _aimFriend(p) then continue end
                    local char = p.Character
                    if char and _aimAlive(char) then
                        if _aimHasFF(char) then continue end
                        local hum  = char:FindFirstChildOfClass("Humanoid")
                        local part = char:FindFirstChild(AimSettings.AimPart)
                        if hum and part then
                            if AimSettings.HPCheck and hum.Health <= 0 then continue end
                            if not _aimCanSee(part, char) then continue end
                            local sp, onScreen = Camera:WorldToViewportPoint(part.Position)
                            if onScreen then
                                local d = (Vector2.new(sp.X, sp.Y) - Vector2.new(cx, cy)).Magnitude
                                if d < shortest then shortest = d; closest = part end
                            end
                        end
                    end
                end
                return closest
            end
            local function _aimAt(part)
                if not part then return end
                local pos = part.Position
                if AimSettings.Prediction then
                    local vel = Vector3.zero
                    pcall(function() vel = part.AssemblyLinearVelocity end)
                    if vel.Magnitude <= 0.05 then pcall(function() vel = part.Velocity end) end
                    local t = AimSettings.PredictionAmount
                    if AimSettings.AirPrediction and math.abs(vel.Y) > 2 then
                        t = AimSettings.AirPredictionAmount
                    end
                    pos = pos + vel * t
                end
                Camera.CFrame = Camera.CFrame:Lerp(
                    CFrame.new(Camera.CFrame.Position, pos),
                    AimSettings.Smoothness
                )
            end
         local currentAimTarget = nil
        local lastAimScan = 0
        local AIM_SCAN_INTERVAL = 0.05
        local SWITCH_THRESHOLD = 40
        local function _isTargetValid(part)
            if not part or not part.Parent then return false end
            local char = part.Parent
            if not _aimAlive(char) then return false end
            if _aimHasFF(char) then return false end
            local hum = char:FindFirstChildOfClass("Humanoid")
            if not hum or (AimSettings.HPCheck and hum.Health <= 0) then return false end
            if not _aimCanSee(part, char) then return false end
            local sp, onScreen = Camera:WorldToViewportPoint(part.Position)
            if not onScreen then return false end
            local cx = Camera.ViewportSize.X / 2 + AimSettings.FOVOffsetX
            local cy = Camera.ViewportSize.Y / 2 + AimSettings.FOVOffsetY
            local d = (Vector2.new(sp.X, sp.Y) - Vector2.new(cx, cy)).Magnitude
            return d < AimSettings.FOV
        end
        RunService.RenderStepped:Connect(function()
            local vs = Camera.ViewportSize
            FOVCircleAim.Position = UDim2.new(
                0, vs.X / 2 + AimSettings.FOVOffsetX,
                0, vs.Y / 2 + AimSettings.FOVOffsetY
            )
            FOVCircleAim.Size    = UDim2.new(0, AimSettings.FOV * 2, 0, AimSettings.FOV * 2)
            FOVCircleAim.Visible = AimSettings.ShowFOV
            if not AimSettings.AimEnabled then
                currentAimTarget = nil
                return
            end
            if tick() - lastAimScan >= AIM_SCAN_INTERVAL then
                lastAimScan = tick()
                if currentAimTarget and _isTargetValid(currentAimTarget) then
                    local sp_cur = Camera:WorldToViewportPoint(currentAimTarget.Position)
                    local cx = Camera.ViewportSize.X / 2 + AimSettings.FOVOffsetX
                    local cy = Camera.ViewportSize.Y / 2 + AimSettings.FOVOffsetY
                    local curDist = (Vector2.new(sp_cur.X, sp_cur.Y) - Vector2.new(cx, cy)).Magnitude
                    local newTarget, newDist = _getClosest(), math.huge
                    if newTarget then
                        local sp_new = Camera:WorldToViewportPoint(newTarget.Position)
                        newDist = (Vector2.new(sp_new.X, sp_new.Y) - Vector2.new(cx, cy)).Magnitude
                    end
                    if newTarget and newTarget ~= currentAimTarget and (curDist - newDist) >= SWITCH_THRESHOLD then
                        currentAimTarget = newTarget
                    end
                else
                    currentAimTarget = _getClosest()
                end
            end
            _aimAt(currentAimTarget)
        end)
    end
    _setupAimSection()
end
local panels = {panel2, panel3, panel4, panel5, panel6, panel7, panel8, panel9, panel10}
local function switchTab(idx)
    for i, p in ipairs(panels) do
        p.Visible = i == idx
    end
    for i, btn in ipairs(tabBtns) do
        local isActive = i == idx
        btn.BackgroundTransparency = isActive and 0.0 or 0.35
        btn.BackgroundColor3 = isActive and Color3.fromRGB(90, 20, 160) or Color3.fromRGB(35, 15, 65)
        local tabGrad = btn:FindFirstChildOfClass("UIGradient")
        if tabGrad then
            if isActive then
                tabGrad.Color = ColorSequence.new({
                    ColorSequenceKeypoint.new(0, Color3.fromRGB(140, 40, 255)),
                    ColorSequenceKeypoint.new(1, Color3.fromRGB(60, 10, 140)),
                })
            else
                tabGrad.Color = ColorSequence.new({
                    ColorSequenceKeypoint.new(0, Color3.fromRGB(55, 15, 100)),
                    ColorSequenceKeypoint.new(1, Color3.fromRGB(15, 20, 60)),
                })
            end
        end
        local bar = btn:FindFirstChild("AccentBar")
        if not bar then
            bar = Instance.new("Frame")
            bar.Name = "AccentBar"
            bar.Size = UDim2.new(0, 3, 0.65, 0)
            bar.Position = UDim2.new(0, 1, 0.175, 0)
            bar.BorderSizePixel = 0
            bar.ZIndex = btn.ZIndex + 1
            _mkCorner(2, bar)
            local barGrad = Instance.new("UIGradient")
            barGrad.Color = ColorSequence.new({
                ColorSequenceKeypoint.new(0,   Color3.fromRGB(170, 70, 255)),
                ColorSequenceKeypoint.new(1,   Color3.fromRGB(0, 200, 255)),
            })
            barGrad.Rotation = 90
            barGrad.Parent = bar
            bar.Parent = btn
        end
        bar.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
        bar.Visible = isActive
        local icon = btn:FindFirstChild("TabIcon")
        if icon then
            icon.ImageColor3 = isActive and Color3.fromRGB(220, 160, 255) or Color3.fromRGB(170, 140, 210)
        end
        local lbl = btn:FindFirstChild("TabLabel")
        if lbl then
            lbl.TextColor3 = isActive and Color3.fromRGB(255, 210, 255) or Color3.fromRGB(180, 150, 220)
        end
    end
end
for i, btn in ipairs(tabBtns) do
    btn.MouseButton1Click:Connect(function()
        local TS = game:GetService("TweenService")
        local s = Instance.new("Sound")
        s.SoundId = "rbxassetid://116394299154002"
        s.Volume = 0.5
        s.Parent = game:GetService("SoundService")
        s:Play()
        game:GetService("Debris"):AddItem(s, 2)
        TS:Create(btn, TweenInfo.new(0.07, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {
            Size = UDim2.new(1, -8, 0, 26),
        }):Play()
        task.delay(0.07, function()
            TS:Create(btn, TweenInfo.new(0.18, Enum.EasingStyle.Back, Enum.EasingDirection.Out), {
                Size = UDim2.new(1, 0, 0, 30),
            }):Play()
        end)
        local ripple = Instance.new("Frame")
        ripple.Name = "Ripple"
        ripple.Size = UDim2.new(0, 0, 0, 0)
        ripple.Position = UDim2.new(0.5, 0, 0.5, 0)
        ripple.AnchorPoint = Vector2.new(0.5, 0.5)
        ripple.BackgroundColor3 = Color3.fromRGB(180, 100, 255)
        ripple.BackgroundTransparency = 0.4
        ripple.BorderSizePixel = 0
        ripple.ZIndex = btn.ZIndex + 5
        ripple.Parent = btn
        _mkCorner(999, ripple)
        TS:Create(ripple, TweenInfo.new(0.45, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {
            Size = UDim2.new(2, 0, 4, 0),
            BackgroundTransparency = 1,
        }):Play()
        game:GetService("Debris"):AddItem(ripple, 0.5)
        local shine = Instance.new("Frame")
        shine.Size = UDim2.new(0, 16, 1, 0)
        shine.Position = UDim2.new(-0.2, 0, 0, 0)
        shine.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
        shine.BackgroundTransparency = 0.6
        shine.BorderSizePixel = 0
        shine.ZIndex = btn.ZIndex + 4
        shine.ClipsDescendants = false
        shine.Parent = btn
        local shineGrad = Instance.new("UIGradient")
        shineGrad.Transparency = NumberSequence.new({
            NumberSequenceKeypoint.new(0, 1),
            NumberSequenceKeypoint.new(0.5, 0.5),
            NumberSequenceKeypoint.new(1, 1),
        })
        shineGrad.Rotation = 0
        shineGrad.Parent = shine
        TS:Create(shine, TweenInfo.new(0.35, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {
            Position = UDim2.new(1.2, 0, 0, 0),
        }):Play()
        game:GetService("Debris"):AddItem(shine, 0.4)
        switchTab(i)
    end)
end
switchTab(1)
do
    local SAVE_FOLDER = "StarCrow"
    local SAVE_FILE   = "StarCrow/StarSaveSetting"
    local function ensureFolder()
        if not isfolder(SAVE_FOLDER) then
            makefolder(SAVE_FOLDER)
        end
    end
    local function loadConfig()
        if not isfile(SAVE_FILE) then return nil end
        local ok, data = pcall(function()
            return game:GetService("HttpService"):JSONDecode(readfile(SAVE_FILE))
        end)
        if ok and type(data) == "table" then return data end
        return nil
    end
    local function saveConfig()
        ensureFolder()
        local cfg = {
            hitboxSize          = S.sizeInput and S.sizeInput.Text or "4",
            headHitbox          = S.autoHitboxEnabled,
            bodyHitbox          = S.autoBodyHitboxEnabled,
            loopspeedEnabled    = S.loopspeedToggle and S.loopspeedToggle.Text:find("ON") ~= nil,
            loopspeedValue      = S.loopspeedInput and S.loopspeedInput.Text or "16",
            invisFloatEnabled   = S.invisFloatEnabled,
            flyEnabled          = S.flyEnabled,
            flySpeed            = S.btnRefs.flySpeedInput and S.btnRefs.flySpeedInput.Text or "2",
            infJumpEnabled      = S.infJumpEnabled,
            noclipEnabled       = S.noclipEnabled,
            nameESPEnabled      = S.nameESPEnabled,
            espShowName         = S.espShowName,
            espShowDist         = S.espShowDist,
            espShowHp           = S.espShowHp,
            chamsEnabled        = S.chamsEnabled,
            tracerESPEnabled    = S.tracerESPEnabled,
            antiFlingEnabled    = S.antiFlingEnabled,
            antiAfkEnabled      = S.antiAfkEnabled,
            instantBuyEnabled   = S.btnRefs.instantBuyBtn and S.btnRefs.instantBuyBtn.Text:find("ON") ~= nil,
            loopFBEnabled       = S.btnRefs.loopFBBtn and S.btnRefs.loopFBBtn.Text:find("ON") ~= nil,
            mobileShiftLock     = S.btnRefs.mobileShiftLockBtn and S.btnRefs.mobileShiftLockBtn.Text:find("ON") ~= nil,
            autoClickEnabled    = S.autoClickEnabled,
            antiVoidEnabled     = S.btnRefs.antiVoidBtn and S.btnRefs.antiVoidBtn.Text:find("ON") ~= nil,
            ctrlClickTPEnabled  = S.ctrlClickTPEnabled,
            freezeBodyEnabled   = S.freezeBodyEnabled,
            cameraNoclipEnabled = S.cameraNoclipEnabled,
            floatEnabled        = S.floatEnabled,
            jumpPowerEnabled    = S.jumpPowerEnabled,
            jumpPowerValue      = S.btnRefs.jumpPowerInput and S.btnRefs.jumpPowerInput.Text or "50",
            auraFloatEnabled    = S.auraFloatEnabled,
dropFloatEnabled    = S.dropFloatEnabled,
equipAllFloatEnabled= S.equipAllFloatEnabled,
useFloatEnabled     = S.useFloatEnabled,
flyFloatEnabled     = S.flyFloatEnabled,
noclipFloatEnabled  = S.noclipFloatEnabled,
feUseInputText      = S.feUseInput and S.feUseInput.Text or "",
floatBtnSize        = S.floatBtnSize,
keybindsEnabled     = S.keybindsEnabled,
floatDragLocked     = S.floatDragLocked,
invisKeyName        = tostring(S.invisKeyCode):gsub("Enum.KeyCode.", ""),
flyKeyName          = tostring(S.flyKeyCode):gsub("Enum.KeyCode.", ""),
noclipKeyName       = tostring(S.noclipKeyCode or Enum.KeyCode.J):gsub("Enum.KeyCode.", ""),
menuKeyName         = tostring(S.menuKeyCode or S.menuKeybind):gsub("Enum.KeyCode.", ""),
            invisFloatPos    = S.invisFloatBtn    and {S.invisFloatBtn.Position.X.Scale,    S.invisFloatBtn.Position.X.Offset,    S.invisFloatBtn.Position.Y.Scale,    S.invisFloatBtn.Position.Y.Offset}    or nil,
            flyFloatPos      = S.flyFloatBtn      and {S.flyFloatBtn.Position.X.Scale,      S.flyFloatBtn.Position.X.Offset,      S.flyFloatBtn.Position.Y.Scale,      S.flyFloatBtn.Position.Y.Offset}      or nil,
            noclipFloatPos   = S.noclipFloatBtn   and {S.noclipFloatBtn.Position.X.Scale,   S.noclipFloatBtn.Position.X.Offset,   S.noclipFloatBtn.Position.Y.Scale,   S.noclipFloatBtn.Position.Y.Offset}   or nil,
            auraFloatPos     = S.auraFloatBtn     and {S.auraFloatBtn.Position.X.Scale,     S.auraFloatBtn.Position.X.Offset,     S.auraFloatBtn.Position.Y.Scale,     S.auraFloatBtn.Position.Y.Offset}     or nil,
            dropFloatPos     = S.dropFloatBtn     and {S.dropFloatBtn.Position.X.Scale,     S.dropFloatBtn.Position.X.Offset,     S.dropFloatBtn.Position.Y.Scale,     S.dropFloatBtn.Position.Y.Offset}     or nil,
            equipAllFloatPos = S.equipAllFloatBtn and {S.equipAllFloatBtn.Position.X.Scale, S.equipAllFloatBtn.Position.X.Offset, S.equipAllFloatBtn.Position.Y.Scale, S.equipAllFloatBtn.Position.Y.Offset} or nil,
            useFloatPos      = S.useFloatBtn      and {S.useFloatBtn.Position.X.Scale,      S.useFloatBtn.Position.X.Offset,      S.useFloatBtn.Position.Y.Scale,      S.useFloatBtn.Position.Y.Offset}      or nil,
        }
        local ok2, err = pcall(function()
            writefile(SAVE_FILE, game:GetService("HttpService"):JSONEncode(cfg))
        end)
        if ok2 then
    showCustomNotif("StarCrow Says:\n\n   Settings saved successfully", 3, true)
else
    showCustomNotif("Error saving config: "..tostring(err), 3)
end
    end
    local function safeClick(btn)
        if not btn or not btn.Parent then return false end
        local ok = pcall(function() btn:FireMouseButton1Click() end)
        return ok
    end
    local function syncBtn(btn, isOn, textOn, textOff)
        if not btn then return end
        btn.Text = isOn and textOn or textOff
        setToggleState(btn, isOn)
    end
    local function applyConfig(cfg)
        if not cfg then return end
        pcall(function()
            if S.sizeInput and cfg.hitboxSize then
                S.sizeInput.Text = tostring(cfg.hitboxSize)
            end
        end)
        pcall(function()
            if cfg.headHitbox and not S.autoHitboxEnabled then
                S.autoHitboxEnabled = true
                local size = tonumber(S.sizeInput and S.sizeInput.Text) or S.DEFAULT_HITBOX_SIZE
                for _, player in pairs(Players:GetPlayers()) do
                    if player ~= LocalPlayer and player.Character and isAlive(player.Character) then
                        applyHeadHitbox(player.Character, size)
                    end
                end
                syncBtn(S.btnRefs.autoToggle, true, "Head Hit Box: ON", "Head Hit Box: OFF")
            end
        end)
        pcall(function()
            if cfg.bodyHitbox and not S.autoBodyHitboxEnabled then
                S.autoBodyHitboxEnabled = true
                local size = tonumber(S.sizeInput and S.sizeInput.Text) or S.DEFAULT_HITBOX_SIZE
                for _, player in pairs(Players:GetPlayers()) do
                    if player ~= LocalPlayer and player.Character and isAlive(player.Character) then
                        applyBodyHitbox(player.Character, size)
                    end
                end
                syncBtn(S.btnRefs.bodyToggle, true, "Body Hit Box: ON", "Body Hit Box: OFF")
            end
        end)
        pcall(function()
            if S.loopspeedInput and cfg.loopspeedValue then
                S.loopspeedInput.Text = tostring(cfg.loopspeedValue)
            end
        end)
        pcall(function()
            if cfg.loopspeedEnabled and S.setLoopspeedEnabled then
                S.setLoopspeedEnabled(true)
            end
        end)
        pcall(function()
            if S.btnRefs.flySpeedInput and cfg.flySpeed then
                S.btnRefs.flySpeedInput.Text = tostring(cfg.flySpeed)
                S.flySpeed = tonumber(cfg.flySpeed) or S.flySpeed
            end
        end)
        pcall(function()
            if cfg.flyEnabled and not S.flyEnabled then
                S.flyEnabled = true
                startFly()
                syncBtn(S.btnRefs.flyToggle, true, "Fly: ON", "Fly: OFF")
            end
        end)
        pcall(function()
            if cfg.infJumpEnabled and not S.infJumpEnabled then
                S.infJumpEnabled = true
                startInfJump()
                syncBtn(S.btnRefs.infJumpToggle, true, "Inf Jump: ON", "Inf Jump: OFF")
            end
        end)
        pcall(function()
            if cfg.noclipEnabled and not S.noclipEnabled then
                S.noclipEnabled = true
                startNoclip()
                syncBtn(S.btnRefs.noclipToggle, true, "Noclip: ON", "Noclip: OFF")
            end
        end)
        pcall(function()
            if cfg.freezeBodyEnabled and not S.freezeBodyEnabled then
                S.freezeBodyEnabled = true
                startFreezeBody()
                syncBtn(S.btnRefs.freezeBodyBtn, true, "Freeze Body: ON", "Freeze Body: OFF")
            end
        end)
        pcall(function()
            if cfg.cameraNoclipEnabled and not S.cameraNoclipEnabled then
                S.cameraNoclipEnabled = true
                S._startCameraNoclip()
                syncBtn(S.btnRefs.cameraNoclipBtn, true, "Camera Noclip: ON", "Camera Noclip: OFF")
            end
        end)
        pcall(function()
            if cfg.floatEnabled and not S.floatEnabled then
                S.floatEnabled = true
                S.btnRefs.floatBtn.Text = "Float: ON"
                setToggleState(S.btnRefs.floatBtn, true)
                startFloat()
            end
        end)
        pcall(function()
            if cfg.jumpPowerValue and S.btnRefs.jumpPowerInput then
                S.btnRefs.jumpPowerInput.Text = tostring(cfg.jumpPowerValue)
            end
            if cfg.jumpPowerEnabled and not S.jumpPowerEnabled then
                S.jumpPowerEnabled = true
                S.btnRefs.jumpPowerBtn.Text = "Jump Power: ON"
                setToggleState(S.btnRefs.jumpPowerBtn, true)
                if S.jumpPowerConnection then S.jumpPowerConnection:Disconnect() end
                S.jumpPowerConnection = RunService.Heartbeat:Connect(function()
                    if not S.jumpPowerEnabled then return end
                    local val = tonumber(S.btnRefs.jumpPowerInput.Text) or 50
                    local char = LocalPlayer.Character
                    local hum = char and char:FindFirstChildOfClass("Humanoid")
                    if hum then hum.JumpPower = val end
                end)
            end
        end)
        pcall(function()
            if cfg.nameESPEnabled and not S.nameESPEnabled then
                S.nameESPEnabled = true
                for _, player in pairs(Players:GetPlayers()) do
                    if player ~= LocalPlayer then
                        S.selectedPlayers[player] = true
                    end
                end
                syncBtn(S.btnRefs.autoNameESP, true, "Target ESP: ON", "Target ESP: OFF")
                local anyEnabled = S.espShowName or S.espShowDist or S.espShowHp
                if anyEnabled then
                    for _, player in pairs(Players:GetPlayers()) do
                        if player ~= LocalPlayer and player.Character and isAlive(player.Character) then
                            pcall(createNameESP, player, player.Character)
                        end
                    end
                end
            end
        end)
        pcall(function()
            if cfg.espShowName and not S.espShowName then
                S.espShowName = true
                syncBtn(S.btnRefs.espShowNameBtn, true, "ESP Name: ON", "ESP Name: OFF")
            end
            if cfg.espShowDist and not S.espShowDist then
                S.espShowDist = true
                syncBtn(S.btnRefs.espShowDistBtn, true, "ESP Distance: ON", "ESP Distance: OFF")
            end
            if cfg.espShowHp and not S.espShowHp then
                S.espShowHp = true
                syncBtn(S.btnRefs.espShowHpBtn, true, "ESP HP: ON", "ESP HP: OFF")
            end
            local anyEnabled = S.espShowName or S.espShowDist or S.espShowHp
            if anyEnabled then
                for _, player in pairs(Players:GetPlayers()) do
                    if player ~= LocalPlayer and player.Character and isAlive(player.Character) then
                        pcall(createNameESP, player, player.Character)
                    end
                end
            end
        end)
        pcall(function()
            if cfg.tracerESPEnabled and not S.tracerESPEnabled then
                S.tracerESPEnabled = true
                for _, player in pairs(Players:GetPlayers()) do
                    if player ~= LocalPlayer then
                        createTracerESP(player)
                    end
                end
                syncBtn(S.btnRefs.tracerToggle, true, "Esp Tracer: ON", "Esp Tracer: OFF")
            end
        end)
        pcall(function()
            if cfg.chamsEnabled and not S.chamsEnabled then
                S.chamsEnabled = true
                for _, player in pairs(Players:GetPlayers()) do
                    if player ~= LocalPlayer and player.Character and isAlive(player.Character) then
                        addChams(player.Character, getTeamColor(player))
                    end
                end
                syncBtn(S.btnRefs.chamsToggle, true, "Esp Chams: ON", "Esp Chams: OFF")
            end
        end)
        pcall(function()
            if cfg.ctrlClickTPEnabled and not S.ctrlClickTPEnabled then
                S.ctrlClickTPEnabled = true
                syncBtn(S.btnRefs.ctrlClickTPBtn, true, "Ctrl Click TP: ON", "Ctrl Click TP: OFF")
            end
        end)
        pcall(function()
            if cfg.antiFlingEnabled and not S.antiFlingEnabled then
                S.antiFlingEnabled = true
                enableAntiFling()
                syncBtn(S.btnRefs.antiFlingBtn, true, "Anti Fling: ON", "Anti Fling: OFF")
            end
        end)
        pcall(function()
            if cfg.antiAfkEnabled and not S.antiAfkEnabled then
                S.antiAfkEnabled = true
                syncBtn(S.btnRefs.antiAfkBtn, true, "Anti AFK: ON", "Anti AFK: OFF")
            end
        end)
        pcall(function()
            if cfg.antiVoidEnabled and S.setAntiVoid then
                S.setAntiVoid(true, true)
            end
        end)
        pcall(function()
            if cfg.instantBuyEnabled and S.setInstantBuyEnabled then
                S.setInstantBuyEnabled(true)
            end
        end)
        pcall(function()
            if cfg.loopFBEnabled and S.setLoopFB then
                S.setLoopFB(true, true)
            end
        end)
        pcall(function()
            if cfg.mobileShiftLock and S.setMobileShiftLockEnabled then
                S.setMobileShiftLockEnabled(true)
            end
        end)
        pcall(function()
            if cfg.autoClickEnabled and not S.autoClickEnabled then
                S.autoClickEnabled = true
                startAutoClick()
                syncBtn(S.btnRefs.autoClickBtn, true, "Auto Click: ON", "Auto Click: OFF")
            end
        end)
        pcall(function()
            if cfg.invisFloatPos and type(cfg.invisFloatPos) == "table" and #cfg.invisFloatPos == 4 then
                S.invisFloatPos = cfg.invisFloatPos
            end
            if cfg.invisFloatEnabled and not S.invisFloatEnabled then
                S.invisFloatEnabled = true
                createInvisFloat()
                syncBtn(S.btnRefs.invisFloatToggleBtn, true, "Invisible Float: ON", "Invisible Float: OFF")
            end
        end)
        pcall(function()
    if cfg.flyFloatPos and type(cfg.flyFloatPos) == "table" and #cfg.flyFloatPos == 4 then
        S.flyFloatPos = cfg.flyFloatPos
    end
    if cfg.flyFloatEnabled and not S.flyFloatEnabled then
        S.flyFloatEnabled = true
        createFlyFloat()
        syncBtn(S.btnRefs.flyFloatToggleBtn, true, "Fly Float: ON", "Fly Float: OFF")
    end
end)
pcall(function()
    if cfg.noclipFloatPos and type(cfg.noclipFloatPos) == "table" and #cfg.noclipFloatPos == 4 then
        S.noclipFloatPos = cfg.noclipFloatPos
    end
    if cfg.noclipFloatEnabled and not S.noclipFloatEnabled then
        S.noclipFloatEnabled = true
        createNoclipFloat()
        syncBtn(S.btnRefs.noclipFloatToggleBtn, true, "Noclip Float: ON", "Noclip Float: OFF")
    end
end)
        pcall(function()
            if cfg.auraFloatPos and type(cfg.auraFloatPos) == "table" and #cfg.auraFloatPos == 4 then
                S.auraFloatPos = cfg.auraFloatPos
            end
            if cfg.auraFloatEnabled and not S.auraFloatEnabled then
                S.auraFloatEnabled = true
                createAuraFloat()
                syncBtn(S.btnRefs.feAuraFloatBtn, true, "Aura Float: ON", "Aura Float: OFF")
            end
        end)
        pcall(function()
            if cfg.dropFloatPos and type(cfg.dropFloatPos) == "table" and #cfg.dropFloatPos == 4 then
                S.dropFloatPos = cfg.dropFloatPos
            end
            if cfg.dropFloatEnabled and not S.dropFloatEnabled then
                S.dropFloatEnabled = true
                createDropFloat()
                syncBtn(S.btnRefs.feDropFloatBtn, true, "Drop Float: ON", "Drop Float: OFF")
            end
        end)
        pcall(function()
            if cfg.equipAllFloatPos and type(cfg.equipAllFloatPos) == "table" and #cfg.equipAllFloatPos == 4 then
                S.equipAllFloatPos = cfg.equipAllFloatPos
            end
            if cfg.equipAllFloatEnabled and not S.equipAllFloatEnabled then
                S.equipAllFloatEnabled = true
                createEquipAllFloat()
                syncBtn(S.btnRefs.feEquipAllFloatToggleBtn, true, "Equip All Float: ON", "Equip All Float: OFF")
            end
        end)
        pcall(function()
            if cfg.useFloatPos and type(cfg.useFloatPos) == "table" and #cfg.useFloatPos == 4 then
                S.useFloatPos = cfg.useFloatPos
            end
            if cfg.useFloatEnabled and not S.useFloatEnabled then
                S.useFloatEnabled = true
                createUseFloat()
                syncBtn(S.btnRefs.feUseFloatToggleBtn, true, "Use Float: ON", "Use Float: OFF")
            end
        end)
        pcall(function()
            if S.feUseInput and cfg.feUseInputText then
                S.feUseInput.Text = tostring(cfg.feUseInputText)
            end
        end)
        pcall(function()
            if cfg.floatBtnSize then
                if S.updateFloatSizeVisuals then
                    S.updateFloatSizeVisuals(cfg.floatBtnSize)
                else
                    S.floatBtnSize = cfg.floatBtnSize
                end
            end
        end)
        pcall(function()
    if cfg.keybindsEnabled ~= nil then
        S.keybindsEnabled = cfg.keybindsEnabled
        if S.btnRefs.keybindsBtn then
            if S.keybindsEnabled then
                S.btnRefs.keybindsBtn.Text = "Disable Keybinds: OFF"
                setToggleState(S.btnRefs.keybindsBtn, false)
            else
                S.btnRefs.keybindsBtn.Text = "Disable Keybinds: ON"
                setToggleState(S.btnRefs.keybindsBtn, true)
            end
        end
    end
end)
pcall(function()
    if cfg.floatDragLocked ~= nil then
        S.floatDragLocked = cfg.floatDragLocked
        if S.btnRefs.floatDragLockBtn then
            if S.floatDragLocked then
                S.btnRefs.floatDragLockBtn.Text = "Undraggable Float Buttons: ON"
                setToggleState(S.btnRefs.floatDragLockBtn, true)
            else
                S.btnRefs.floatDragLockBtn.Text = "Undraggable Float Buttons: OFF"
                setToggleState(S.btnRefs.floatDragLockBtn, false)
            end
        end
    end
end)
pcall(function()
    if cfg.invisKeyName and Enum.KeyCode[cfg.invisKeyName] then
        S.invisKeyCode = Enum.KeyCode[cfg.invisKeyName]
        S.INVIS_CONFIG.TOGGLE_KEY = S.invisKeyCode
        if S.btnRefs.invisBindBtn then
            S.btnRefs.invisBindBtn.Text = cfg.invisKeyName
            S.btnRefs.invisBindBtn.BackgroundColor3 = Color3.fromRGB(38, 38, 38)
            S.btnRefs.invisBindBtn.BackgroundTransparency = 0.35
        end
    end
end)
pcall(function()
    if cfg.flyKeyName and Enum.KeyCode[cfg.flyKeyName] then
        S.flyKeyCode = Enum.KeyCode[cfg.flyKeyName]
        if S.btnRefs.flyBindBtn then
            S.btnRefs.flyBindBtn.Text = cfg.flyKeyName
            S.btnRefs.flyBindBtn.BackgroundColor3 = Color3.fromRGB(38, 38, 38)
            S.btnRefs.flyBindBtn.BackgroundTransparency = 0.35
        end
    end
end)
pcall(function()
    if cfg.noclipKeyName and Enum.KeyCode[cfg.noclipKeyName] then
        S.noclipKeyCode = Enum.KeyCode[cfg.noclipKeyName]
        if S.btnRefs.noclipBindBtn then
            S.btnRefs.noclipBindBtn.Text = cfg.noclipKeyName
            S.btnRefs.noclipBindBtn.BackgroundColor3 = Color3.fromRGB(38, 38, 38)
            S.btnRefs.noclipBindBtn.BackgroundTransparency = 0.35
        end
    end
end)
pcall(function()
    if cfg.menuKeyName and Enum.KeyCode[cfg.menuKeyName] then
        S.menuKeyCode = Enum.KeyCode[cfg.menuKeyName]
        if S.btnRefs.menuBindBtn then
            S.btnRefs.menuBindBtn.Text = cfg.menuKeyName
            S.btnRefs.menuBindBtn.BackgroundColor3 = Color3.fromRGB(38, 38, 38)
            S.btnRefs.menuBindBtn.BackgroundTransparency = 0.35
        end
    end
end)
end
local scriptStartTime = tick()
task.spawn(function()
    task.wait(1)
    showCustomNotif("Loading config...", 3)
    task.wait(2)
    local cfg = nil
    local ok, err = pcall(function()
        cfg = loadConfig()
    end)
    if ok and cfg then
        applyConfig(cfg)
        local loadTime = tick() - scriptStartTime
        showCustomNotif(string.format(
            "StarCrow Says:\n\n   Script has been successfully loaded in the game, Took %.2f seconds to load",
            loadTime
        ), 3.5, true)
    else
        showCustomNotif("No config found!", 3)
    end
end)
    do
    local _lbl = createTransparentLabel("Setting", panel10)
    _lbl.TextColor3 = Color3.fromRGB(255,255,255)
    _lbl.Font = Enum.Font.GothamBold
    _lbl.TextSize = 14
end
    local saveBtn = createTransparentButton("Save Settings", Color3.fromRGB(38, 38, 38), panel10)
    saveBtn.BackgroundColor3 = Color3.fromRGB(38, 38, 38)
saveBtn.BackgroundTransparency = 0.35
    saveBtn.TextSize = 13
    saveBtn.Size = UDim2.new(1, 0, 0, 36)
    saveBtn.MouseButton1Click:Connect(function()
        saveConfig()
    end)
    local deleteBtn = createTransparentButton("Reset Configurations", Color3.fromRGB(38, 38, 38), panel10)
    deleteBtn.BackgroundColor3 = Color3.fromRGB(38, 38, 38)
deleteBtn.BackgroundTransparency = 0.35
    deleteBtn.TextSize = 13
    deleteBtn.Size = UDim2.new(1, 0, 0, 36)
    deleteBtn.MouseButton1Click:Connect(function()
        if isfile(SAVE_FILE) then
            pcall(function() delfile(SAVE_FILE) end)
            showCustomNotif("StarCrow Says:\n\n   Settings Deleted", 3, true)
        else
            showCustomNotif("No config found", 3)
        end
    end)
    do
    local deleteDivider = Instance.new("Frame")
    deleteDivider.Parent = panel10
    deleteDivider.Size = UDim2.new(1, 0, 0, 2)
    deleteDivider.BackgroundColor3 = Color3.fromRGB(70, 70, 70)
    deleteDivider.BackgroundTransparency = 0.2
    deleteDivider.BorderSizePixel = 0
end
do
    local _lbl = createTransparentLabel("GUI", panel10)
    _lbl.TextColor3 = Color3.fromRGB(255,255,255)
    _lbl.Font = Enum.Font.GothamBold
    _lbl.TextSize = 14
end
local keybindsBtn = createTransparentButton("Disable Keybinds: OFF", Color3.fromRGB(38, 38, 38), panel10)
setToggleState(keybindsBtn, false)
keybindsBtn.TextSize = 13
keybindsBtn.Size = UDim2.new(1, 0, 0, 36)
S.btnRefs.keybindsBtn = keybindsBtn
local function updateKeybindsBtn()
    if not keybindsBtn then return end
    if S.keybindsEnabled then
        keybindsBtn.Text = "Disable Keybinds: OFF"
        setToggleState(keybindsBtn, false)
    else
        keybindsBtn.Text = "Disable Keybinds: ON"
       setToggleState(keybindsBtn, true)
    end
end
keybindsBtn.MouseButton1Click:Connect(function()
    S.keybindsEnabled = not S.keybindsEnabled
    updateKeybindsBtn()
end)
local function keyCodeToText(keyCode)
    if not keyCode then
        return "?"
    end
    local s = tostring(keyCode)
    return s:gsub("Enum.KeyCode.", "")
end
local function setBindButtonWaiting(btn)
    local s = Instance.new("Sound")
    s.SoundId = "rbxassetid://116394299154002"
    s.Volume = 0.5
    s.Parent = game:GetService("SoundService")
    s:Play()
    game:GetService("Debris"):AddItem(s, 2)
    btn.Text = "Press key..."
    btn.BackgroundColor3 = Color3.fromRGB(60, 60, 60)
    btn.BackgroundTransparency = 0.1
end
local function setBindButtonNormal(btn, keyCode)
    btn.Text = keyCodeToText(keyCode)
    btn.BackgroundColor3 = Color3.fromRGB(38, 38, 38)
    btn.BackgroundTransparency = 0.35
end
local floatDragLockBtn = createTransparentButton("Undraggable Float Buttons: OFF", Color3.fromRGB(38, 38, 38), panel10)
setToggleState(floatDragLockBtn, false)
floatDragLockBtn.TextSize = 13
floatDragLockBtn.TextScaled = false
floatDragLockBtn.TextWrapped = false
floatDragLockBtn.Size = UDim2.new(1, 0, 0, 36)
S.btnRefs.floatDragLockBtn = floatDragLockBtn
local function updateFloatDragLockBtn()
    if not floatDragLockBtn then return end
    if S.floatDragLocked then
        floatDragLockBtn.Text = "Undraggable Float Buttons: ON"
        setToggleState(floatDragLockBtn, true)
    else
        floatDragLockBtn.Text = "Undraggable Float Buttons: OFF"
        setToggleState(floatDragLockBtn, false)
    end
end
floatDragLockBtn.MouseButton1Click:Connect(function()
    S.floatDragLocked = not S.floatDragLocked
    updateFloatDragLockBtn()
end)
updateFloatDragLockBtn()
createFloatSizeSlider(panel10, S, UserInputService)
local function createUnifiedBind(labelTitle, currentKey, bindName)
    local row = Instance.new("Frame")
    row.Size = UDim2.new(1, 0, 0, 36)
    row.BackgroundColor3 = Color3.fromRGB(32, 32, 40)
    row.BackgroundTransparency = 0.4
    row.BorderSizePixel = 0
    row.Parent = panel10
    local rowCorner = Instance.new("UICorner")
    rowCorner.CornerRadius = UDim.new(0, 6)
    rowCorner.Parent = row
    local title = Instance.new("TextLabel")
    title.Size = UDim2.new(0.6, 0, 1, 0)
    title.Position = UDim2.new(0, 12, 0, 0)
    title.BackgroundTransparency = 1
    title.Text = labelTitle
    title.TextColor3 = Color3.fromRGB(230, 230, 230)
    title.Font = Enum.Font.GothamSemibold
    title.TextSize = 13
    title.TextXAlignment = Enum.TextXAlignment.Left
    title.Parent = row
    local btn = Instance.new("TextButton")
    btn.Size = UDim2.new(0, 70, 0, 20)
    btn.Position = UDim2.new(1, -6, 0.5, 0)
    btn.AnchorPoint = Vector2.new(1, 0.5)
    btn.BackgroundColor3 = Color3.fromRGB(20, 20, 25)
    btn.BackgroundTransparency = 0.2
    btn.BorderSizePixel = 0
    btn.Text = keyCodeToText(currentKey)
    btn.TextColor3 = Color3.fromRGB(230, 230, 230)
    btn.Font = Enum.Font.Gotham
    btn.TextSize = 13
    btn.Parent = row
    local btnCorner = Instance.new("UICorner")
    btnCorner.CornerRadius = UDim.new(0, 4)
    btnCorner.Parent = btn
    btn.MouseButton1Click:Connect(function()
        S.waitingForKeybind = bindName
        setBindButtonWaiting(btn)
    end)
    return btn
end
S.btnRefs.invisBindBtn = createUnifiedBind("Invisible", S.invisKeyCode, "Invisible")
S.btnRefs.flyBindBtn = createUnifiedBind("Fly", S.flyKeyCode, "Fly")
S.btnRefs.noclipBindBtn = createUnifiedBind("Noclip", S.noclipKeyCode or Enum.KeyCode.J, "Noclip")
S.btnRefs.menuBindBtn = createUnifiedBind("Menu", S.menuKeybind, "Menu")
end
local function CountFlingTargets()
    local n = 0
    for _ in pairs(S.SelectedFlingTargets) do n = n + 1 end
    return n
end
local function UpdateFlingStatus()
    local count = CountFlingTargets()
    if S.FlingActive then
        S.flingStatusLabel.Text = "Flinging " .. count .. " target(s)"
        S.flingStatusLabel.TextColor3 = Color3.fromRGB(255,80,80)
    else
        S.flingStatusLabel.Text = count .. " target(s) selected"
        S.flingStatusLabel.TextColor3 = Color3.fromRGB(255,255,255)
    end
end
local function RefreshFlingPlayerList()
    for _, child in pairs(S.flingPlayerList:GetChildren()) do
        if child:IsA("Frame") or child:IsA("TextButton") then child:Destroy() end
    end
    S.FlingPlayerCheckboxes = {}
    local PlayerList = Players:GetPlayers()
    table.sort(PlayerList, function(a, b) return a.Name:lower() < b.Name:lower() end)
    for _, player in ipairs(PlayerList) do
        if player ~= LocalPlayer then
            local entry = Instance.new("Frame")
            entry.Size = UDim2.new(1, -6, 0, 30)
            entry.BackgroundColor3 = Color3.fromRGB(50,50,50)
            entry.BackgroundTransparency = 0.5
            entry.BorderSizePixel = 0
            entry.Parent = S.flingPlayerList
            local ec = Instance.new("UICorner"); ec.CornerRadius = UDim.new(0,4); ec.Parent = entry
            local checkBtn = Instance.new("TextButton")
            checkBtn.Size = UDim2.new(0, 22, 0, 22)
            checkBtn.Position = UDim2.new(0, 4, 0.5, -11)
            checkBtn.BackgroundColor3 = Color3.fromRGB(70,70,70)
            checkBtn.BorderSizePixel = 0
            checkBtn.Text = ""
            checkBtn.Parent = entry
            _mkCorner(3, checkBtn)
            local checkmark = Instance.new("TextLabel")
            checkmark.Size = UDim2.new(1,0,1,0)
            checkmark.BackgroundTransparency = 1
            checkmark.Text = "✓"
            checkmark.TextColor3 = Color3.fromRGB(0,255,100)
            checkmark.TextSize = 16
            checkmark.Font = Enum.Font.GothamBold
            checkmark.Visible = S.SelectedFlingTargets[player.Name] ~= nil
            checkmark.Parent = checkBtn
            local nameLabel = Instance.new("TextLabel")
            nameLabel.Size = UDim2.new(1, -34, 1, 0)
            nameLabel.Position = UDim2.new(0, 30, 0, 0)
            nameLabel.BackgroundTransparency = 1
            nameLabel.Text = player.Name
            nameLabel.TextColor3 = Color3.fromRGB(255,255,255)
            nameLabel.TextSize = 13
            nameLabel.Font = Enum.Font.Gotham
            nameLabel.TextXAlignment = Enum.TextXAlignment.Left
            nameLabel.Parent = entry
            local clickArea = Instance.new("TextButton")
            clickArea.Size = UDim2.new(1, 0, 1, 0)
            clickArea.BackgroundTransparency = 1
            clickArea.Text = ""
            clickArea.ZIndex = 2
            clickArea.Parent = entry
            clickArea.MouseButton1Click:Connect(function()
                if S.SelectedFlingTargets[player.Name] then
                    S.SelectedFlingTargets[player.Name] = nil
                    checkmark.Visible = false
                else
                    S.SelectedFlingTargets[player.Name] = player
                    checkmark.Visible = true
                end
                UpdateFlingStatus()
            end)
            S.FlingPlayerCheckboxes[player.Name] = {Entry = entry, Checkmark = checkmark}
        end
    end
    UpdateFlingStatus()
end
local function ToggleAllFling(select)
    for _, player in ipairs(Players:GetPlayers()) do
        if player ~= LocalPlayer then
            local cb = S.FlingPlayerCheckboxes[player.Name]
            if cb then
                if select then
                    S.SelectedFlingTargets[player.Name] = player
                    cb.Checkmark.Visible = true
                else
                    S.SelectedFlingTargets[player.Name] = nil
                    cb.Checkmark.Visible = false
                end
            end
        end
    end
    UpdateFlingStatus()
end
local function StartFling()
    if S.FlingActive then return end
    if CountFlingTargets() == 0 then
        S.flingStatusLabel.Text = "No targets selected!"
        task.delay(1.5, function() UpdateFlingStatus() end)
        return
    end
    S.FlingActive = true
    UpdateFlingStatus()
    showCustomNotif("StarCrow Says:\n\n   Flinging " .. CountFlingTargets() .. " target(s)", 2, true)
    spawn(function()
        while S.FlingActive do
            local validTargets = {}
            for name, player in pairs(S.SelectedFlingTargets) do
                if player and player.Parent then
                    validTargets[name] = player
                else
                    S.SelectedFlingTargets[name] = nil
                    local cb = S.FlingPlayerCheckboxes[name]
                    if cb then cb.Checkmark.Visible = false end
                end
            end
            for _, player in pairs(validTargets) do
                if S.FlingActive then
                    SkidFling(player)
                    wait(0.1)
                else
                    break
                end
            end
            UpdateFlingStatus()
            wait(0.5)
        end
    end)
end
local function StopFling()
    if not S.FlingActive then return end
    S.FlingActive = false
    UpdateFlingStatus()
    if S.flingStartBtn then
        S.flingStartBtn.Text = "FLING PLAYER : OFF"
    end
    showCustomNotif("StarCrow Says:\n\n   Fling has been stopped", 2, true)
end
S.flingStartBtn.MouseButton1Click:Connect(function()
    if S.FlingActive then
        StopFling()
    else
        if CountFlingTargets() == 0 then
            showCustomNotif("StarCrow Says:\n\n   Select a player first!", 2, "fail")
            return
        end
        S.flingStartBtn.Text = "FLING PLAYER : ON"
        StartFling()
    end
end)
do
local bangTrack
S.bangBtn.MouseButton1Click:Connect(function()
    S.BangActive = not S.BangActive
    if S.BangActive then
        S.bangBtn.Text = "BANG PLAYER : ON"
    else
        S.bangBtn.Text = "BANG PLAYER : OFF"
        if bangTrack then
            bangTrack:Stop()
        end
        return
    end
local char = LocalPlayer.Character
if not char then return end
local humanoid = char:FindFirstChildOfClass("Humanoid")
local anim = Instance.new("Animation")
anim.AnimationId = "rbxassetid://5918726674"
bangTrack = humanoid:LoadAnimation(anim)
bangTrack:Play()
spawn(function()
    while S.BangActive do
        local char = LocalPlayer.Character
        if not char then task.wait() continue end
        local hrp = char:FindFirstChild("HumanoidRootPart")
        if not hrp then task.wait() continue end
        local humanoid = char:FindFirstChildOfClass("Humanoid")
        for _,player in pairs(S.SelectedFlingTargets) do
            if not S.BangActive then break end
            if player.Character then
                local targetHRP = player.Character:FindFirstChild("HumanoidRootPart")
                if targetHRP then
                    hrp.CFrame = targetHRP.CFrame * CFrame.new(0,0,1)
                    for i = 1,15 do
                        if not S.BangActive then break end
                        hrp.CFrame = targetHRP.CFrame * CFrame.new(0,0,1)
                        task.wait(0.05)
                        if not S.BangActive then break end
                        hrp.CFrame = targetHRP.CFrame * CFrame.new(0,0,1.5)
                        task.wait(0.05)
                    end
                end
            end
        end
        task.wait()
    end
end)
end)
end
S.walkFlingBtn.MouseButton1Click:Connect(function()
    local antiFlingWasManuallyEnabled = S.antiFlingEnabled and not walkFlingAutoEnabledAntiFling
    toggleWalkFling()
    if S.walkflinging then
        if not S.antiFlingEnabled then
            S.antiFlingEnabled = true
            walkFlingAutoEnabledAntiFling = true
            antiFlingBtn.Text = "Anti Fling: ON"
            setToggleState(antiFlingBtn, true)
            enableAntiFling()
        else
            if antiFlingWasManuallyEnabled then
                walkFlingAutoEnabledAntiFling = false
            end
        end
        S.walkFlingBtn.Text = "WALK FLING : ON"
    else
        if walkFlingAutoEnabledAntiFling and S.antiFlingEnabled then
            S.antiFlingEnabled = false
            walkFlingAutoEnabledAntiFling = false
            antiFlingBtn.Text = "Anti-Fling: OFF"
            setToggleState(antiFlingBtn, false)
            disableAntiFling()
        end
        S.walkFlingBtn.Text = "WALK FLING : OFF"
    end
end)
S.loopBringBtn.MouseButton1Click:Connect(function()
    S.loopBringActive = not S.loopBringActive
    if S.loopBringActive then
        if CountFlingTargets() == 0 then
            S.loopBringActive = false
            S.loopBringBtn.Text = "LOOP BRING PLAYER : OFF"
            S.flingStatusLabel.Text = "No targets selected for LoopBring!"
            task.delay(1.5, function()
                UpdateFlingStatus()
            end)
            return
        end
        S.loopBringBtn.Text = "LOOP BRING PLAYER : ON"
        task.spawn(function()
            while S.loopBringActive do
                local char = LocalPlayer.Character
                if not char then
                    task.wait(0.03)
                    continue
                end
                local hrp = char:FindFirstChild("HumanoidRootPart")
                if not hrp then
                    task.wait(0.03)
                    continue
                end
                local validTargets = {}
                for name, player in pairs(S.SelectedFlingTargets) do
                    if player and player.Parent then
                        validTargets[name] = player
                    else
                        S.SelectedFlingTargets[name] = nil
                        local cb = S.FlingPlayerCheckboxes[name]
                        if cb then
                            cb.Checkmark.Visible = false
                        end
                    end
                end
                for _, player in pairs(validTargets) do
                    if not S.loopBringActive then break end
                    if player.Character then
                        local targetHRP = player.Character:FindFirstChild("HumanoidRootPart")
                        local hum = player.Character:FindFirstChildOfClass("Humanoid")
                        if targetHRP and hum then
                            if not S.loopBringStates[player] then
                                S.loopBringStates[player] = {
                                    WalkSpeed = hum.WalkSpeed,
                                    JumpPower = hum.JumpPower,
                                    AutoRotate = hum.AutoRotate,
                                }
                            end
                            hum.WalkSpeed = 0
                            hum.JumpPower = 0
                            hum.AutoRotate = false
                            for _, track in ipairs(hum:GetPlayingAnimationTracks()) do
                                track:Stop(0.1)
                            end
                            for _, part in ipairs(player.Character:GetDescendants()) do
                                if part:IsA("BasePart") then
                                    part.CanCollide = false
                                    part.Massless = true
                                    part.AssemblyLinearVelocity = Vector3.zero
                                    part.AssemblyAngularVelocity = Vector3.zero
                                end
                            end
                            targetHRP.CFrame = hrp.CFrame * CFrame.new(0, 0, -4)
                        end
                    end
                end
                UpdateFlingStatus()
                task.wait(0.02)
            end
        end)
    else
        S.loopBringBtn.Text = "LOOP BRING PLAYER : OFF"
        S.loopBringBtn.BackgroundColor3 = Color3.fromRGB(38, 38, 38)
        S.loopBringBtn.BackgroundTransparency = 0.35
        for player, state in pairs(S.loopBringStates) do
            if player and player.Parent and player.Character then
                local hum = player.Character:FindFirstChildOfClass("Humanoid")
                if hum then
                    hum.WalkSpeed = state.WalkSpeed or 16
                    hum.JumpPower = state.JumpPower or 50
                    hum.AutoRotate = (state.AutoRotate ~= false)
                end
                for _, part in ipairs(player.Character:GetDescendants()) do
                    if part:IsA("BasePart") then
                        part.CanCollide = true
                        part.Massless = false
                    end
                end
            end
        end
        S.loopBringStates = {}
    end
end)
flingSelectAllBtn.MouseButton1Click:Connect(function()
    ToggleAllFling(true)
end)
flingDeselectAllBtn.MouseButton1Click:Connect(function()
    ToggleAllFling(false)
end)
antiFlingBtn.MouseButton1Click:Connect(function()
    S.antiFlingEnabled = not S.antiFlingEnabled
    if S.antiFlingEnabled then
        antiFlingBtn.Text = "Anti Fling: ON"
        setToggleState(antiFlingBtn, true)
        enableAntiFling()
    else
        antiFlingBtn.Text = "Anti Fling: OFF"
        setToggleState(antiFlingBtn, false)
        disableAntiFling()
    end
end)
do
    local loopFullBright = false
    local fbConnection = nil
    local function setLoopFB(state, silent)
        loopFullBright = state
        if loopFullBright then
            loopFBBtn.Text = "LoopFullBright: ON"
            setToggleState(loopFBBtn, true)
            if fbConnection then fbConnection:Disconnect() end
            fbConnection = RunService.RenderStepped:Connect(function()
                local lighting = game:GetService("Lighting")
                lighting.Brightness = 2
                lighting.ClockTime = 14
                lighting.FogEnd = 100000
                lighting.GlobalShadows = false
                lighting.OutdoorAmbient = Color3.fromRGB(128,128,128)
            end)
        else
            loopFBBtn.Text = "LoopFullBright: OFF"
            setToggleState(loopFBBtn, false)
            if fbConnection then fbConnection:Disconnect(); fbConnection = nil end
        end
    end
    S.setLoopFB = setLoopFB
    loopFBBtn.MouseButton1Click:Connect(function()
        setLoopFB(not loopFullBright)
    end)
end
noFogBtn.MouseButton1Click:Connect(function()
    local lighting = game:GetService("Lighting")
    lighting.FogStart = 0
    lighting.FogEnd = 1000000
    for _,v in pairs(lighting:GetChildren()) do
        if v:IsA("Atmosphere") then
            v:Destroy()
        end
    end
end)
do
    local instantBuyEnabled = false
    local instantBuyConnections = {}
    local instantBuySeen = {}
    local function clearInstantBuyConnections()
        for _, conn in ipairs(instantBuyConnections) do
            if conn then
                conn:Disconnect()
            end
        end
        table.clear(instantBuyConnections)
        table.clear(instantBuySeen)
    end
    local function setupPrompt(prompt)
        if not prompt or not prompt:IsA("ProximityPrompt") then
            return
        end
        if instantBuySeen[prompt] then
            return
        end
        instantBuySeen[prompt] = true
        if prompt.HoldDuration and prompt.HoldDuration > 0 then
            pcall(function()
                prompt.HoldDuration = 0
            end)
        end
        table.insert(instantBuyConnections, prompt:GetPropertyChangedSignal("HoldDuration"):Connect(function()
            if not instantBuyEnabled then return end
            if prompt.HoldDuration > 0 then
                pcall(function()
                    prompt.HoldDuration = 0
                end)
            end
        end))
    end
    local function enableInstantBuy()
        for _, v in ipairs(workspace:GetDescendants()) do
            if v:IsA("ProximityPrompt") then
                setupPrompt(v)
            end
        end
        table.insert(instantBuyConnections, workspace.DescendantAdded:Connect(function(v)
            if not instantBuyEnabled then return end
            if v:IsA("ProximityPrompt") then
                task.wait()
                setupPrompt(v)
            end
        end))
    end
    local function setInstantBuyEnabled(state)
        instantBuyEnabled = state
        instantBuyBtn.Text = instantBuyEnabled and "Instant Interact: ON" or "Instant Interact: OFF"
        setToggleState(instantBuyBtn, instantBuyEnabled)
        clearInstantBuyConnections()
        if instantBuyEnabled then
            enableInstantBuy()
        else
        end
    end
    S.setInstantBuyEnabled = setInstantBuyEnabled
    instantBuyBtn.MouseButton1Click:Connect(function()
        setInstantBuyEnabled(not instantBuyEnabled)
    end)
end
local RunService = game:GetService("RunService")
unlockCameraBtn.MouseButton1Click:Connect(function()
    local cam = workspace.CurrentCamera
    if cam then
        cam:Destroy()
    end
    task.wait(0.1)
    local char = LocalPlayer.Character
    if not char then
        repeat task.wait() until LocalPlayer.Character
        char = LocalPlayer.Character
    end
    cam = workspace.CurrentCamera
    if cam and char then
        local hum = char:FindFirstChildWhichIsA("Humanoid")
        if hum then
            cam.CameraSubject = hum
        end
        cam.CameraType = Enum.CameraType.Custom
    end
    LocalPlayer.CameraMinZoomDistance = 0.5
    LocalPlayer.CameraMaxZoomDistance = 100000
    LocalPlayer.CameraMode = Enum.CameraMode.Classic
    if char and char:FindFirstChild("Head") then
        char.Head.Anchored = false
    end
    RunService:BindToRenderStep("StarCrow_UnlockCam_Force", Enum.RenderPriority.Camera.Value + 99, function()
        pcall(function()
            if LocalPlayer.CameraMode ~= Enum.CameraMode.Classic then
                LocalPlayer.CameraMode = Enum.CameraMode.Classic
            end
            local currentCam = workspace.CurrentCamera
            if currentCam and currentCam.CameraType ~= Enum.CameraType.Custom then
                currentCam.CameraType = Enum.CameraType.Custom
            end
        end)
    end)
end)
do
    local mobileShiftLockEnabled = false
    local mobileShiftLockActive = false
    local mobileShiftLockButtonUI = nil
    local mobileCrosshair = nil
    local mobileShiftLockConnection = nil
    local function isMobileDevice()
        return UserInputService.TouchEnabled and not UserInputService.KeyboardEnabled
    end
    local function removeCrosshair()
        if mobileCrosshair then
            mobileCrosshair:Destroy()
            mobileCrosshair = nil
        end
        local playerGui = LocalPlayer:FindFirstChildOfClass("PlayerGui")
        if playerGui then
            local crosshairGui = playerGui:FindFirstChild("MobileShiftLockCrosshairGui")
            if crosshairGui then
                crosshairGui:Destroy()
            end
        end
    end
    local function createCrosshair()
        removeCrosshair()
        local playerGui = LocalPlayer:FindFirstChildOfClass("PlayerGui")
        if not playerGui then return end
        local crosshairGui = Instance.new("ScreenGui")
        crosshairGui.Name = "MobileShiftLockCrosshairGui"
        crosshairGui.ResetOnSpawn = false
        crosshairGui.IgnoreGuiInset = true
        crosshairGui.DisplayOrder = 999999
        crosshairGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
        crosshairGui.Parent = playerGui
        mobileCrosshair = Instance.new("ImageLabel")
mobileCrosshair.Name = "MobileShiftLockCrosshair"
mobileCrosshair.Size = UDim2.new(0, 24, 0, 24)
mobileCrosshair.AnchorPoint = Vector2.new(0.5, 0.5)
mobileCrosshair.Position = UDim2.new(0.5, 0, 0.5, 0)
mobileCrosshair.BackgroundTransparency = 1
mobileCrosshair.BorderSizePixel = 0
mobileCrosshair.ZIndex = 999999
mobileCrosshair.Image = "rbxthumb://type=Asset&id=11719595129&w=420&h=420"
mobileCrosshair.ScaleType = Enum.ScaleType.Fit
mobileCrosshair.Parent = crosshairGui
    end
    local function destroyMobileShiftLockButton()
        if mobileShiftLockButtonUI then
            mobileShiftLockButtonUI:Destroy()
            mobileShiftLockButtonUI = nil
        end
    end
    local function stopMobileShiftLock()
        mobileShiftLockActive = false
        if mobileShiftLockConnection then
            mobileShiftLockConnection:Disconnect()
            mobileShiftLockConnection = nil
        end
     local character = LocalPlayer.Character
if character then
    local humanoid = character:FindFirstChildOfClass("Humanoid")
    if humanoid then
        humanoid.AutoRotate = true
        humanoid.CameraOffset = Vector3.new(0, 0, 0)
    end
end
        removeCrosshair()
        if mobileShiftLockButtonUI then
            mobileShiftLockButtonUI.Image = "rbxassetid://105987953182009"
        end
    end
    local function startMobileShiftLock()
    if not mobileShiftLockEnabled or not isMobileDevice() then return end
    if mobileShiftLockActive then return end
    mobileShiftLockActive = true
    createCrosshair()
    if mobileShiftLockButtonUI then
        mobileShiftLockButtonUI.Image = "rbxassetid://139211296111194"
    end
    local character = LocalPlayer.Character
    if not character then return end
    local humanoid = character:FindFirstChildOfClass("Humanoid")
    local rootPart = character:FindFirstChild("HumanoidRootPart")
    local currentCamera = workspace.CurrentCamera
    if not humanoid or not rootPart or not currentCamera then return end
    humanoid.AutoRotate = false
    humanoid.CameraOffset = Vector3.new(1.75, 0, 0)
    mobileShiftLockConnection = RunService.RenderStepped:Connect(function()
        local char = LocalPlayer.Character
        if not char then return end
        local hum = char:FindFirstChildOfClass("Humanoid")
        local root = char:FindFirstChild("HumanoidRootPart")
        local cam = workspace.CurrentCamera
        if not hum or not root or not cam then return end
        local lookVector = cam.CFrame.LookVector
local flatLook = Vector3.new(lookVector.X, 0, lookVector.Z)
if flatLook.Magnitude > 0.01 then
    flatLook = flatLook.Unit
    root.CFrame = CFrame.new(root.Position, root.Position + flatLook)
end
    end)
end
    local function toggleMobileShiftLockActive()
        if not mobileShiftLockEnabled then return end
        if mobileShiftLockActive then
            stopMobileShiftLock()
        else
            startMobileShiftLock()
        end
    end
    local function createMobileShiftLockButton()
        if mobileShiftLockButtonUI or not isMobileDevice() then return end
        mobileShiftLockButtonUI = Instance.new("ImageButton")
        mobileShiftLockButtonUI.Name = "MobileShiftLockButton"
        mobileShiftLockButtonUI.Size = UDim2.new(0, 34, 0, 34)
        mobileShiftLockButtonUI.AnchorPoint = Vector2.new(1, 1)
        mobileShiftLockButtonUI.Position = UDim2.new(1, -70, 1, -130)
        mobileShiftLockButtonUI.BackgroundTransparency = 1
        mobileShiftLockButtonUI.BorderSizePixel = 0
        mobileShiftLockButtonUI.Image = "rbxassetid://105987953182009"
        mobileShiftLockButtonUI.ScaleType = Enum.ScaleType.Fit
        mobileShiftLockButtonUI.ZIndex = 999999
        mobileShiftLockButtonUI.Parent = gui
        mobileShiftLockButtonUI.MouseButton1Click:Connect(function()
            toggleMobileShiftLockActive()
        end)
    end
    local function setMobileShiftLockEnabled(state)
        mobileShiftLockEnabled = state
        mobileShiftLockBtn.Text = "Mobile ShiftLock: " .. (mobileShiftLockEnabled and "ON" or "OFF")
        setToggleState(mobileShiftLockBtn, mobileShiftLockEnabled)
        if mobileShiftLockEnabled and isMobileDevice() then
            createMobileShiftLockButton()
            stopMobileShiftLock()
        else
            stopMobileShiftLock()
            destroyMobileShiftLockButton()
        end
    end
    S.setMobileShiftLockEnabled = setMobileShiftLockEnabled
    mobileShiftLockBtn.MouseButton1Click:Connect(function()
        setMobileShiftLockEnabled(not mobileShiftLockEnabled)
    end)
    LocalPlayer.CharacterAdded:Connect(function()
        task.wait(1)
        if mobileShiftLockEnabled and isMobileDevice() then
            destroyMobileShiftLockButton()
            createMobileShiftLockButton()
            stopMobileShiftLock()
        end
    end)
end
do
    local loopspeedEnabled = false
    local loopspeedConnection = nil
    local function setLoopspeedEnabled(state)
        loopspeedEnabled = state
        S.loopspeedToggle.Text = loopspeedEnabled and "Loopspeed: ON" or "Loopspeed: OFF"
        setToggleState(S.loopspeedToggle, loopspeedEnabled)
        if loopspeedConnection then
            loopspeedConnection:Disconnect()
            loopspeedConnection = nil
        end
        if loopspeedEnabled then
            loopspeedConnection = RunService.Heartbeat:Connect(function()
                local char = LocalPlayer.Character
                if char then
                    local hum = char:FindFirstChildOfClass("Humanoid")
                    if hum then hum.WalkSpeed = tonumber(S.loopspeedInput.Text) or 16 end
                end
            end)
        else
            local char = LocalPlayer.Character
            if char then
                local hum = char:FindFirstChildOfClass("Humanoid")
                if hum then hum.WalkSpeed = 16 end
            end
        end
    end
    S.setLoopspeedEnabled = setLoopspeedEnabled
    S.loopspeedToggle.MouseButton1Click:Connect(function() setLoopspeedEnabled(not loopspeedEnabled) end)
end
mainFrame.Visible = false
local function openMenu()
    local TS = game:GetService("TweenService")
    mainFrame.Size               = UDim2.new(0, 260, 0, 310)
    mainFrame.Position           = UDim2.new(0.5, -130, 0.5, -175)
    mainFrame.BackgroundTransparency = 1
    mainFrame.Visible            = true
    toggleBtn.Visible            = false
    TS:Create(mainFrame, TweenInfo.new(0.3, Enum.EasingStyle.Back, Enum.EasingDirection.Out), {
        Size     = UDim2.new(0, 320, 0, 380),
        Position = UDim2.new(0.5, -160, 0.5, -190),
        BackgroundTransparency = 0.7,
    }):Play()
    updatePlayerList()
end
local function closeMenu()
    local TS = game:GetService("TweenService")
    local tw = TS:Create(mainFrame, TweenInfo.new(0.15, Enum.EasingStyle.Quad, Enum.EasingDirection.In), {
        Position = UDim2.new(0.5, -160, 0.5, -175),
        BackgroundTransparency = 0.95,
    })
    tw:Play()
    tw.Completed:Connect(function()
        mainFrame.Visible = false
        mainFrame.BackgroundTransparency = 0.7
        toggleBtn.Size = UDim2.new(0, 170, 0, 32)
        toggleBtn.BackgroundTransparency = 0.9
        toggleBtn.Visible = true
        TS:Create(toggleBtn, TweenInfo.new(0.2, Enum.EasingStyle.Back, Enum.EasingDirection.Out), {
            Size = UDim2.new(0, 190, 0, 38),
            BackgroundTransparency = 0.42,
        }):Play()
    end)
end
local function toggleMenu()
    _playClick()
    if mainFrame.Visible then
        closeMenu()
    else
        openMenu()
    end
end
toggleBtn.MouseButton1Click:Connect(function()
    if _tbMoved then _tbMoved = false; return end
    local TS = game:GetService("TweenService")
    local particleColors = {
        Color3.fromRGB(160, 60, 255),
        Color3.fromRGB(0, 180, 255),
        Color3.fromRGB(220, 100, 255),
        Color3.fromRGB(80, 220, 255),
    }
    for i = 1, 12 do
        local angle = (i / 12) * math.pi * 2
        local dist  = 38 + math.random(0, 24)
        local sz    = math.random(3, 7)
        local p = Instance.new("Frame")
        p.Size            = UDim2.new(0, sz, 0, sz)
        p.AnchorPoint     = Vector2.new(0.5, 0.5)
        p.Position        = UDim2.new(0.5, 0, 0.5, 0)
        p.BackgroundColor3= particleColors[(i % #particleColors) + 1]
        p.BackgroundTransparency = 0
        p.BorderSizePixel = 0
        p.ZIndex          = toggleBtn.ZIndex + 8
        p.Parent          = toggleBtn
        Instance.new("UICorner", p).CornerRadius = UDim.new(1, 0)
        local tx = math.cos(angle) * dist
        local ty = math.sin(angle) * dist
        TS:Create(p, TweenInfo.new(0.45, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {
            Position             = UDim2.new(0.5, tx, 0.5, ty),
            BackgroundTransparency = 1,
            Size                 = UDim2.new(0, sz * 0.4, 0, sz * 0.4),
        }):Play()
        game:GetService("Debris"):AddItem(p, 0.5)
    end
    local ring = Instance.new("Frame")
    ring.Size             = UDim2.new(0, 10, 0, 10)
    ring.AnchorPoint      = Vector2.new(0.5, 0.5)
    ring.Position         = UDim2.new(0.5, 0, 0.5, 0)
    ring.BackgroundTransparency = 1
    ring.BorderSizePixel  = 0
    ring.ZIndex           = toggleBtn.ZIndex + 7
    ring.Parent           = toggleBtn
    Instance.new("UICorner", ring).CornerRadius = UDim.new(1, 0)
    local ringStroke = Instance.new("UIStroke", ring)
    ringStroke.Thickness      = 2.5
    ringStroke.Transparency   = 0
    ringStroke.Color          = Color3.fromRGB(180, 80, 255)
    ringStroke.ApplyStrokeMode= Enum.ApplyStrokeMode.Border
    TS:Create(ring, TweenInfo.new(0.4, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {
        Size = UDim2.new(2.2, 0, 5, 0),
    }):Play()
    TS:Create(ringStroke, TweenInfo.new(0.4, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {
        Transparency = 1,
    }):Play()
    game:GetService("Debris"):AddItem(ring, 0.45)
    TS:Create(toggleBtn, TweenInfo.new(0.08, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {
        Size = UDim2.new(0, 210, 0, 32),
    }):Play()
    task.delay(0.08, function()
        TS:Create(toggleBtn, TweenInfo.new(0.22, Enum.EasingStyle.Elastic, Enum.EasingDirection.Out), {
            Size = UDim2.new(0, 190, 0, 38),
        }):Play()
    end)
    toggleMenu()
end)
closeButton.MouseButton1Click:Connect(function()
    _playClick()
    local TS = game:GetService("TweenService")
    TS:Create(closeButton, TweenInfo.new(0.07, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {
        Size = UDim2.new(0, 22, 0, 20),
    }):Play()
    local rip = Instance.new("Frame")
    rip.Size = UDim2.new(0, 0, 0, 0)
    rip.Position = UDim2.new(0.5, 0, 0.5, 0)
    rip.AnchorPoint = Vector2.new(0.5, 0.5)
    rip.BackgroundColor3 = Color3.fromRGB(255, 80, 80)
    rip.BackgroundTransparency = 0.35
    rip.BorderSizePixel = 0
    rip.ZIndex = closeButton.ZIndex + 5
    rip.ClipsDescendants = false
    rip.Parent = closeButton
    Instance.new("UICorner", rip).CornerRadius = UDim.new(1, 0)
    TS:Create(rip, TweenInfo.new(0.35, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {
        Size = UDim2.new(3, 0, 3, 0),
        BackgroundTransparency = 1,
    }):Play()
    game:GetService("Debris"):AddItem(rip, 0.4)
    task.delay(0.07, function()
        TS:Create(closeButton, TweenInfo.new(0.15, Enum.EasingStyle.Back, Enum.EasingDirection.Out), {
            Size = UDim2.new(0, 28, 0, 24),
        }):Play()
    end)
    closeMenu()
end)
autoToggle.MouseButton1Click:Connect(function()
if not S.autoHitboxEnabled then
    if S.autoBodyHitboxEnabled then
        S.autoBodyHitboxEnabled = false
        bodyToggle.Text = "Body Hit Box: OFF"
        setToggleState(bodyToggle, false)
        for _, player in pairs(Players:GetPlayers()) do
            if player ~= LocalPlayer and player.Character then
                resetBodyHitbox(player.Character)
            end
        end
    end
    if S.chamsEnabled then
        S.chamsEnabled = false
        chamsToggle.Text = "Esp Chams: OFF"
        setToggleState(chamsToggle, false)
        for _, player in pairs(Players:GetPlayers()) do
            if player ~= LocalPlayer and player.Character then
                removeChams(player.Character)
            end
        end
    end
end
    S.autoHitboxEnabled = not S.autoHitboxEnabled
    autoToggle.Text = S.autoHitboxEnabled and "Head Hit Box: ON" or "Head Hit Box: OFF"
    if S.autoHitboxEnabled then
        setToggleState(autoToggle, true)
    else
        setToggleState(autoToggle, false)
    end
    local size = tonumber(S.sizeInput.Text) or S.DEFAULT_HITBOX_SIZE
    if S.autoHitboxEnabled then
        if S.autoHitboxEnabled then
    for _, player in pairs(Players:GetPlayers()) do
        if player ~= LocalPlayer and player.Character and isAlive(player.Character) then
            applyHeadHitbox(player, player.Character, size)
        end
    end
else
    local charsToRemove = {}
    for char, _ in pairs(S.processedChars) do
        table.insert(charsToRemove, char)
    end
    for _, char in ipairs(charsToRemove) do
        removeHeadHitbox(char)
    end
end
        for _, player in pairs(Players:GetPlayers()) do
            if player ~= LocalPlayer and player.Character and isAlive(player.Character) then
                applyHeadHitbox(player, player.Character, size)
            end
        end
    else
        for char, _ in pairs(S.processedChars) do
            removeHeadHitbox(char)
        end
    end
    S.statusLabel.Text = "Processing: " .. tableCount(S.processedChars) .. " Players"
end)
bodyToggle.MouseButton1Click:Connect(function()
if not S.autoBodyHitboxEnabled then
    if S.autoHitboxEnabled then
        S.autoHitboxEnabled = false
        autoToggle.Text = "Head Hit Box: OFF"
        setToggleState(autoToggle, false)
        for _, player in pairs(Players:GetPlayers()) do
            if player ~= LocalPlayer and player.Character then
                resetHeadHitbox(player.Character)
            end
        end
    end
    if S.chamsEnabled then
        S.chamsEnabled = false
        chamsToggle.Text = "Esp Chams: OFF"
        setToggleState(chamsToggle, false)
        for _, player in pairs(Players:GetPlayers()) do
            if player ~= LocalPlayer and player.Character then
                removeChams(player.Character)
            end
        end
    end
end
    S.autoBodyHitboxEnabled = not S.autoBodyHitboxEnabled
    bodyToggle.Text = S.autoBodyHitboxEnabled and "Body Hit Box: ON" or "Body Hit Box: OFF"
    if S.autoBodyHitboxEnabled then
        setToggleState(bodyToggle, true)
    else
        setToggleState(bodyToggle, false)
    end
    local size = tonumber(S.sizeInput.Text) or S.DEFAULT_HITBOX_SIZE
    if S.autoBodyHitboxEnabled then
        for _, player in pairs(Players:GetPlayers()) do
            if player ~= LocalPlayer and player.Character and isAlive(player.Character) then
                applyBodyHitbox(player.Character, size)
            end
        end
    else
        for _, player in pairs(Players:GetPlayers()) do
            if player ~= LocalPlayer and player.Character then
                resetBodyHitbox(player.Character)
            end
        end
        S.statusLabel.Text = "Processing: 0 Players"
    end
end)
removeAllBtn.MouseButton1Click:Connect(function()
    for char, _ in pairs(S.processedChars) do
        removeHeadHitbox(char)
    end
    for _, player in pairs(Players:GetPlayers()) do
        if player ~= LocalPlayer and player.Character then
            resetBodyHitbox(player.Character)
        end
    end
    S.autoHitboxEnabled = false
    S.autoBodyHitboxEnabled = false
    autoToggle.Text = "Head Hit Box: OFF"
    setToggleState(autoToggle, false)
    bodyToggle.Text = "Body Hit Box: OFF"
    setToggleState(bodyToggle, false)
    S.statusLabel.Text = "Processing: 0 Players"
end)
local function updateESPVisibility()
    local anyEnabled = S.espShowName or S.espShowDist or S.espShowHp
    if anyEnabled then
        for _, player in pairs(Players:GetPlayers()) do
            if player ~= LocalPlayer and player.Character and isAlive(player.Character) then
                local head = player.Character:FindFirstChild("Head")
                if head and not head:FindFirstChild("NameESP") then
                    pcall(createNameESP, player, player.Character)
                end
            end
        end
    else
        for player, _ in pairs(S.nameESPObjects) do
            removeNameESP(player)
        end
    end
end
S.btnRefs.espShowNameBtn.MouseButton1Click:Connect(function()
    S.espShowName = not S.espShowName
    S.btnRefs.espShowNameBtn.Text = S.espShowName and "ESP Name: ON" or "ESP Name: OFF"
    setToggleState(S.btnRefs.espShowNameBtn, S.espShowName)
    updateESPVisibility()
end)
S.btnRefs.espShowDistBtn.MouseButton1Click:Connect(function()
    S.espShowDist = not S.espShowDist
    S.btnRefs.espShowDistBtn.Text = S.espShowDist and "ESP Distance: ON" or "ESP Distance: OFF"
    setToggleState(S.btnRefs.espShowDistBtn, S.espShowDist)
    updateESPVisibility()
end)
S.btnRefs.espShowHpBtn.MouseButton1Click:Connect(function()
    S.espShowHp = not S.espShowHp
    S.btnRefs.espShowHpBtn.Text = S.espShowHp and "ESP HP: ON" or "ESP HP: OFF"
    setToggleState(S.btnRefs.espShowHpBtn, S.espShowHp)
    updateESPVisibility()
end)
S.invisButton.MouseButton1Click:Connect(toggleInvisibility)
invisFloatToggleBtn.MouseButton1Click:Connect(function()
    S.invisFloatEnabled = not S.invisFloatEnabled
    if S.invisFloatEnabled then
        invisFloatToggleBtn.Text = "Invisible Float: ON"
        setToggleState(invisFloatToggleBtn, true)
        createInvisFloat()
    else
        invisFloatToggleBtn.Text = "Invisible Float: OFF"
        setToggleState(invisFloatToggleBtn, false)
        destroyInvisFloat()
    end
end)
infJumpToggle.MouseButton1Click:Connect(function()
    S.infJumpEnabled = not S.infJumpEnabled
    infJumpToggle.Text = S.infJumpEnabled and "Inf Jump: ON" or "Inf Jump: OFF"
    setToggleState(infJumpToggle, S.infJumpEnabled)
    if S.infJumpEnabled then
    startInfJump()
else
    stopInfJump()
end
end)
noclipToggle.MouseButton1Click:Connect(function()
    S.noclipEnabled = not S.noclipEnabled
    noclipToggle.Text = S.noclipEnabled and "Noclip: ON" or "Noclip: OFF"
    setToggleState(noclipToggle, S.noclipEnabled)
    if S.noclipEnabled then
        startNoclip()
    else
        stopNoclip()
    end
    updateNoclipFloatVisual()
end)
noclipFloatToggleBtn.MouseButton1Click:Connect(function()
    S.noclipFloatEnabled = not S.noclipFloatEnabled
    if S.noclipFloatEnabled then
        noclipFloatToggleBtn.Text = "Noclip Float: ON"
        setToggleState(noclipFloatToggleBtn, true)
        createNoclipFloat()
    else
        noclipFloatToggleBtn.Text = "Noclip Float: OFF"
        setToggleState(noclipFloatToggleBtn, false)
        destroyNoclipFloat()
    end
end)
freezeBodyBtn.MouseButton1Click:Connect(function()
    S.freezeBodyEnabled = not S.freezeBodyEnabled
    if S.freezeBodyEnabled then
        freezeBodyBtn.Text = "Freeze Body: ON"
        setToggleState(freezeBodyBtn, true)
        startFreezeBody()
    else
        freezeBodyBtn.Text = "Freeze Body: OFF"
        setToggleState(freezeBodyBtn, false)
        stopFreezeBody()
    end
end)
S.btnRefs.cameraNoclipBtn.MouseButton1Click:Connect(function()
    S.cameraNoclipEnabled = not S.cameraNoclipEnabled
    if S.cameraNoclipEnabled then
        S.btnRefs.cameraNoclipBtn.Text = "Camera Noclip: ON"
        setToggleState(S.btnRefs.cameraNoclipBtn, true)
        S._startCameraNoclip()
    else
        S.btnRefs.cameraNoclipBtn.Text = "Camera Noclip: OFF"
        setToggleState(S.btnRefs.cameraNoclipBtn, false)
        S._stopCameraNoclip()
    end
end)
S.btnRefs.floatBtn.MouseButton1Click:Connect(function()
    S.floatEnabled = not S.floatEnabled
    if S.floatEnabled then
        S.btnRefs.floatBtn.Text = "Float: ON"
        setToggleState(S.btnRefs.floatBtn, true)
        startFloat()
    else
        S.btnRefs.floatBtn.Text = "Float: OFF"
        setToggleState(S.btnRefs.floatBtn, false)
        stopFloat()
    end
end)
S.btnRefs.jumpPowerBtn.MouseButton1Click:Connect(function()
    S.jumpPowerEnabled = not S.jumpPowerEnabled
    if S.jumpPowerEnabled then
        S.btnRefs.jumpPowerBtn.Text = "Jump Power: ON"
        setToggleState(S.btnRefs.jumpPowerBtn, true)
        if S.jumpPowerConnection then S.jumpPowerConnection:Disconnect() end
        S.jumpPowerConnection = RunService.Heartbeat:Connect(function()
            if not S.jumpPowerEnabled then return end
            local val = tonumber(S.btnRefs.jumpPowerInput.Text) or 50
            local char = LocalPlayer.Character
            local hum = char and char:FindFirstChildOfClass("Humanoid")
            if hum then
                hum.UseJumpPower = true
                hum.JumpPower = val
            end
        end)
    else
        S.btnRefs.jumpPowerBtn.Text = "Jump Power: OFF"
        setToggleState(S.btnRefs.jumpPowerBtn, false)
        if S.jumpPowerConnection then
            S.jumpPowerConnection:Disconnect()
            S.jumpPowerConnection = nil
        end
        local char = LocalPlayer.Character
        local hum = char and char:FindFirstChildOfClass("Humanoid")
        if hum then
            hum.UseJumpPower = true
            hum.JumpPower = 50
        end
    end
end)
S.btnRefs.jumpPowerInput.FocusLost:Connect(function()
    local val = tonumber(S.btnRefs.jumpPowerInput.Text)
    if not val or val < 0 then
        S.btnRefs.jumpPowerInput.Text = "50"
    end
end)
S.flyToggle.MouseButton1Click:Connect(function()
    S.flySpeed = tonumber(flySpeedInput.Text) or S.flySpeed or 2
    S.flyEnabled = not S.flyEnabled
    S.flyToggle.Text = S.flyEnabled and "Fly: ON" or "Fly: OFF"
    setToggleState(S.flyToggle, S.flyEnabled)
    if S.flyEnabled then
        startFly()
    else
        stopFly()
    end
    updateFlyFloatVisual()
end)
flySpeedInput.FocusLost:Connect(function()
    local newSpeed = tonumber(flySpeedInput.Text)
    if not newSpeed or newSpeed <= 0 then
        flySpeedInput.Text = tostring(S.flySpeed or 2)
        return
    end
    S.flySpeed = newSpeed
end)
flyFloatToggleBtn.MouseButton1Click:Connect(function()
    S.flyFloatEnabled = not S.flyFloatEnabled
    if S.flyFloatEnabled then
        flyFloatToggleBtn.Text = "Fly Float: ON"
        setToggleState(flyFloatToggleBtn, true)
        createFlyFloat()
    else
        flyFloatToggleBtn.Text = "Fly Float: OFF"
        setToggleState(flyFloatToggleBtn, false)
        destroyFlyFloat()
    end
end)
rejoinBtn.MouseButton1Click:Connect(function()
    local TeleportService = game:GetService("TeleportService")
    local StarterGui = game:GetService("StarterGui")
    local queue = queue_on_teleport or queueonteleport
    showCustomNotif("StarCrow Says:\n\n   Rejoining...", 2, true)
    if queue then
        queue('loadstring(game:HttpGet("https://raw.githubusercontent.com/user-null-0/test-repo-08888/refs/heads/main/StarCrow"))()')
    end
    if #Players:GetPlayers() <= 1 then
        Players.LocalPlayer:Kick("\nRejoining...")
        task.wait(0.5)
        TeleportService:Teleport(game.PlaceId, Players.LocalPlayer)
    else
        TeleportService:TeleportToPlaceInstance(
            game.PlaceId,
            game.JobId,
            Players.LocalPlayer
        )
    end
end)
serverHopBtn.MouseButton1Click:Connect(function()
    local TeleportService = game:GetService("TeleportService")
    local HttpService = game:GetService("HttpService")
    local PlaceID = game.PlaceId
    local JobID = game.JobId
    showCustomNotif("StarCrow Says:\n\n   Searching for server...", 2, true)
    queue_on_teleport('loadstring(game:HttpGet("https://raw.githubusercontent.com/user-null-0/test-repo-08888/refs/heads/main/StarCrow"))()')
    task.spawn(function()
        local servers = {}
        local cursor = nil
        repeat
            local url = "https://games.roblox.com/v1/games/" .. PlaceID .. "/servers/Public?sortOrder=Asc&limit=100"
            if cursor then url = url .. "&cursor=" .. cursor end
            local ok, data = pcall(function()
                return HttpService:JSONDecode(game:HttpGet(url))
            end)
            if not ok or not data or not data.data then break end
            for _, server in pairs(data.data) do
                if server.id ~= JobID then
                    if server.playing < server.maxPlayers then
                        table.insert(servers, 1, server.id)
                    else
                        table.insert(servers, server.id)
                    end
                end
                if #servers >= 5 then break end
            end
            cursor = data.nextPageCursor
        until #servers >= 5 or not cursor
        if #servers == 0 then
            showCustomNotif("StarCrow Says:\n\n   No server found!", 3, "fail")
            return
        end
        for attempt = 1, math.min(#servers, 5) do
            local serverID = servers[attempt]
            showCustomNotif("StarCrow Says:\n\n   Trying server " .. attempt .. "/5...", 2, true)
            local failed = false
            local conn = TeleportService.TeleportInitFailed:Connect(function(player)
                if player == Players.LocalPlayer then
                    failed = true
                end
            end)
            local ok = pcall(TeleportService.TeleportToPlaceInstance, TeleportService, PlaceID, serverID, Players.LocalPlayer)
            if ok then
                task.wait(4)
            end
            conn:Disconnect()
            if ok and not failed then return end
            showCustomNotif("StarCrow Says:\n\n   Server " .. attempt .. " failed, trying next...", 1.5, "fail")
            task.wait(0.5)
        end
        showCustomNotif("StarCrow Says:\n\n   All 5 servers failed!", 3, "fail")
    end)
end)
joinJobBtn.MouseButton1Click:Connect(function()
    local jobId = jobIdInput.Text
    if jobId ~= "" then
        showCustomNotif("StarCrow Says:\n\n   Preparing to join server...", 3, true)
        task.wait(1)
        local queue = queue_on_teleport or queueonteleport
        if queue then
            queue('loadstring(game:HttpGet("https://raw.githubusercontent.com/user-null-0/test-repo-08888/refs/heads/main/StarCrow"))()')
        end
        local TeleportService = game:GetService("TeleportService")
        local failConn
        failConn = TeleportService.TeleportInitFailed:Connect(function(player, result, errMsg)
            if player == game.Players.LocalPlayer then
                failConn:Disconnect()
                showCustomNotif("StarCrow Says:\n\n   Join failed: " .. tostring(errMsg or result), 4, "fail")
            end
        end)
        local ok, err = pcall(function()
            TeleportService:TeleportToPlaceInstance(
                game.PlaceId,
                jobId,
                game.Players.LocalPlayer
            )
        end)
        if not ok then
            failConn:Disconnect()
            showCustomNotif("StarCrow Says:\n\n   Invalid Job ID!", 3, "fail")
        end
    else
        showCustomNotif("StarCrow Says:\n\n   Please enter a Job ID!", 3, "fail")
    end
end)
copyJobBtn.MouseButton1Click:Connect(function()
    if setclipboard then
        setclipboard(game.JobId)
    end
    showCustomNotif("StarCrow Says:\n\n   JobId copied!", 3, true)
end)
tpPosBtn.MouseButton1Click:Connect(function()
    local text = posInput.Text
    if text == "" then
        showCustomNotif("StarCrow Says:\n\n   Please enter a position first!", 3, "fail")
        return
    end
    local parts = string.split(text, ",")
    if #parts < 3 then
        showCustomNotif("StarCrow Says:\n\n   Invalid format! Use: X, Y, Z", 3, "fail")
        return
    end
    local x = tonumber(parts[1] and parts[1]:match("^%s*(.-)%s*$"))
    local y = tonumber(parts[2] and parts[2]:match("^%s*(.-)%s*$"))
    local z = tonumber(parts[3] and parts[3]:match("^%s*(.-)%s*$"))
    if not x or not y or not z then
        showCustomNotif("StarCrow Says:\n\n   Invalid numbers! Use: X, Y, Z", 3, "fail")
        return
    end
    S.manualPositionCFrame = CFrame.new(x, y, z)
    local char = LocalPlayer.Character
    local hrp = char and char:FindFirstChild("HumanoidRootPart")
    if hrp then
        hrp.CFrame = S.manualPositionCFrame
        showCustomNotif("StarCrow Says:\n\n   Teleported to position!", 3, true)
    else
        showCustomNotif("StarCrow Says:\n\n   HumanoidRootPart not found!", 3, "fail")
    end
end)
copyPosBtn.MouseButton1Click:Connect(function()
    local char = LocalPlayer.Character
    local hrp = char and char:FindFirstChild("HumanoidRootPart")
    if not hrp then
        showCustomNotif("StarCrow Says:\n\n   HumanoidRootPart not found!", 3, "fail")
        return
    end
    local pos = hrp.Position
    local posText = string.format("%.2f, %.2f, %.2f", pos.X, pos.Y, pos.Z)
    posInput.Text = posText
    if setclipboard then
        setclipboard(posText)
    end
    showCustomNotif("StarCrow Says:\n\n   Position copied!", 3, true)
end)
local SAVED_POS_FILE = "StarCrow/SavedPositions.json"
local function loadSavedPositions()
    if not (readfile and writefile) then return {} end
    local ok, data = pcall(readfile, SAVED_POS_FILE)
    if not ok or not data or data == "" then return {} end
    local decoded = {}
    for name, t in next, (game:GetService("HttpService"):JSONDecode(data) or {}) do
        decoded[name] = CFrame.new(
            t[1], t[2], t[3],
            t[4], t[5], t[6],
            t[7], t[8], t[9],
            t[10], t[11], t[12]
        )
    end
    return decoded
end
local function saveSavedPositions(tbl)
    if not (readfile and writefile) then return end
    local raw = {}
    for name, cf in next, tbl do
        raw[name] = {
            cf.X, cf.Y, cf.Z,
            cf.XVector.X, cf.XVector.Y, cf.XVector.Z,
            cf.YVector.X, cf.YVector.Y, cf.YVector.Z,
            cf.ZVector.X, cf.ZVector.Y, cf.ZVector.Z
        }
    end
    pcall(writefile, SAVED_POS_FILE, game:GetService("HttpService"):JSONEncode(raw))
end
local savedPositions = loadSavedPositions()
savePosBtn.MouseButton1Click:Connect(function()
    local name = savePosNameInput.Text:match("^%s*(.-)%s*$")
    if name == "" then
        showCustomNotif("StarCrow Says:\n\n   Enter a location name first!", 3, "fail")
        return
    end
    local char = LocalPlayer.Character
    local hrp = char and char:FindFirstChild("HumanoidRootPart")
    if not hrp then
        showCustomNotif("StarCrow Says:\n\n   HumanoidRootPart not found!", 3, "fail")
        return
    end
    savedPositions[name] = hrp.CFrame
    saveSavedPositions(savedPositions)
    showCustomNotif("StarCrow Says:\n\n   Position saved: " .. name, 3, true)
end)
tpSavedPosBtn.MouseButton1Click:Connect(function()
    local name = savePosNameInput.Text:match("^%s*(.-)%s*$")
    if name == "" then
        showCustomNotif("StarCrow Says:\n\n   Enter a location name first!", 3, "fail")
        return
    end
    local cf = savedPositions[name]
    if not cf then
        showCustomNotif("StarCrow Says:\n\n   Location not found: " .. name, 3, "fail")
        return
    end
    local char = LocalPlayer.Character
    local hrp = char and char:FindFirstChild("HumanoidRootPart")
    if not hrp then
        showCustomNotif("StarCrow Says:\n\n   HumanoidRootPart not found!", 3, "fail")
        return
    end
    hrp.CFrame = cf
    showCustomNotif("StarCrow Says:\n\n   Teleported to: " .. name, 3, true)
end)
local spawnHereCF = nil
local spawnHereConn = nil
spawnHereBtn.MouseButton1Click:Connect(function()
    local char = LocalPlayer.Character
    local hrp = char and char:FindFirstChild("HumanoidRootPart")
    if not hrp then
        showCustomNotif("StarCrow Says:\n\n   HumanoidRootPart not found!", 3, "fail")
        return
    end
    spawnHereCF = hrp.CFrame
    if spawnHereConn then spawnHereConn:Disconnect() end
    spawnHereConn = LocalPlayer.CharacterAdded:Connect(function(newChar)
        if not spawnHereCF then return end
        local hrp2 = newChar:WaitForChild("HumanoidRootPart", 5)
        if hrp2 then
            task.wait(0.1)
            hrp2.CFrame = spawnHereCF
        end
    end)
    showCustomNotif("StarCrow Says:\n\n   Spawn point set!", 3, true)
end)
S.autoTPBtn.MouseButton1Click:Connect(function()
    S.autoTPEnabled = not S.autoTPEnabled
    if S.autoTPEnabled then
        S.autoTPBtn.Text = "Auto TP: ON"
        setToggleState(S.autoTPBtn, true)
    else
        S.autoTPBtn.Text = "Auto TP: OFF"
        setToggleState(S.autoTPBtn, false)
    end
    if S.autoTPConnection then
        S.autoTPConnection:Disconnect()
        S.autoTPConnection = nil
    end
    if S.autoTPEnabled then
        S.autoTPConnection = RunService.Heartbeat:Connect(function()
            local myChar = LocalPlayer.Character
            if not myChar then return end
            local myHRP = myChar:FindFirstChild("HumanoidRootPart")
            if not myHRP then return end
            for player,_ in pairs(S.selectedTPPlayers) do
                local targetChar = player.Character
                if targetChar then
                    local targetHRP = targetChar:FindFirstChild("HumanoidRootPart")
                    if targetHRP then
                        myHRP.CFrame = targetHRP.CFrame * CFrame.new(0, 0, 2.5)
                    end
                end
            end
        end)
    end
end)
teleportBtn.MouseButton1Click:Connect(function()
    local myChar = LocalPlayer.Character
    if not myChar then return end
    local myHRP = myChar:FindFirstChild("HumanoidRootPart")
    if not myHRP then return end
    for player,_ in pairs(S.selectedTPPlayers) do
        local targetChar = player.Character
        if targetChar then
            local targetHRP = targetChar:FindFirstChild("HumanoidRootPart")
            if targetHRP then
                myHRP.CFrame = targetHRP.CFrame + Vector3.new(0,3,0)
                break
            end
        end
    end
end)
tpToolBtn.MouseButton1Click:Connect(function()
    local backpack = LocalPlayer:FindFirstChild("Backpack")
    local char = LocalPlayer.Character
    if not backpack then return end
    if backpack:FindFirstChild("Tp tool") or (char and char:FindFirstChild("Tp tool")) then
        return
    end
    local tool = Instance.new("Tool")
    tool.RequiresHandle = false
    tool.Name = "Tp tool"
    tool.Activated:Connect(function()
        local currentChar = LocalPlayer.Character
        if currentChar and currentChar:FindFirstChild("HumanoidRootPart") then
            local pos = mouse.Hit.Position + Vector3.new(0, 2.5, 0)
            currentChar.HumanoidRootPart.CFrame = CFrame.new(pos)
        end
    end)
    tool.Parent = backpack
end)
ctrlClickTPBtn.MouseButton1Click:Connect(function()
    S.ctrlClickTPEnabled = not S.ctrlClickTPEnabled
    if S.ctrlClickTPEnabled then
        ctrlClickTPBtn.Text = "Ctrl Click TP: ON"
        setToggleState(ctrlClickTPBtn, true)
    else
        ctrlClickTPBtn.Text = "Ctrl Click TP: OFF"
        setToggleState(ctrlClickTPBtn, false)
    end
end)
UserInputService.InputBegan:Connect(function(input, gameProcessed)
    if input.UserInputType ~= Enum.UserInputType.Keyboard then
        return
    end
    if UserInputService:GetFocusedTextBox() then
        return
    end
    if input.KeyCode == (S.menuKeyCode or Enum.KeyCode.RightControl) then
        if not S.waitingForKeybind then
            toggleMenu()
            return
        end
    end
    if S.waitingForKeybind then
        local newKey = input.KeyCode
        if newKey ~= Enum.KeyCode.Unknown then
            if S.waitingForKeybind ~= "Menu" and newKey == (S.menuKeyCode or Enum.KeyCode.RightControl) then
            else
                if S.waitingForKeybind == "Invisible" then
                    S.invisKeyCode = newKey
                    S.INVIS_CONFIG.TOGGLE_KEY = newKey
                    if S.btnRefs.invisBindBtn then
                        S.btnRefs.invisBindBtn.Text = tostring(newKey):gsub("Enum.KeyCode.", "")
                        S.btnRefs.invisBindBtn.BackgroundColor3 = Color3.fromRGB(38, 38, 38)
                        S.btnRefs.invisBindBtn.BackgroundTransparency = 0.35
                    end
                                elseif S.waitingForKeybind == "Fly" then
                    S.flyKeyCode = newKey
                    if S.btnRefs.flyBindBtn then
                        S.btnRefs.flyBindBtn.Text = tostring(newKey):gsub("Enum.KeyCode.", "")
                        S.btnRefs.flyBindBtn.BackgroundColor3 = Color3.fromRGB(38, 38, 38)
                        S.btnRefs.flyBindBtn.BackgroundTransparency = 0.35
                    end
                elseif S.waitingForKeybind == "Noclip" then
                    S.noclipKeyCode = newKey
                    if S.btnRefs.noclipBindBtn then
                        S.btnRefs.noclipBindBtn.Text = tostring(newKey):gsub("Enum.KeyCode.", "")
                        S.btnRefs.noclipBindBtn.BackgroundColor3 = Color3.fromRGB(38, 38, 38)
                        S.btnRefs.noclipBindBtn.BackgroundTransparency = 0.35
                    end
                elseif S.waitingForKeybind == "Menu" then
                    S.menuKeyCode = newKey
                    if S.btnRefs.menuBindBtn then
                        S.btnRefs.menuBindBtn.Text = tostring(newKey):gsub("Enum.KeyCode.", "")
                        S.btnRefs.menuBindBtn.BackgroundColor3 = Color3.fromRGB(38, 38, 38)
                        S.btnRefs.menuBindBtn.BackgroundTransparency = 0.35
                    end
                end
            end
        end
        S.waitingForKeybind = nil
        return
    end
    if not S.keybindsEnabled then
        return
    end
    if gameProcessed then
        return
    end
    if input.KeyCode == (S.invisKeyCode or S.INVIS_CONFIG.TOGGLE_KEY) then
        _playClick()
        toggleInvisibility()
        return
    end
    if input.KeyCode == (S.flyKeyCode or Enum.KeyCode.F) then
        _playClick()
        S.flySpeed = tonumber(S.btnRefs.flySpeedInput and S.btnRefs.flySpeedInput.Text) or S.flySpeed or 2
        S.flyEnabled = not S.flyEnabled
        if S.flyToggle then
            S.flyToggle.Text = S.flyEnabled and "Fly: ON" or "Fly: OFF"
            setToggleState(S.flyToggle, S.flyEnabled)
        end
        if S.flyEnabled then
    startFly()
else
    stopFly()
end
        updateFlyFloatVisual()
        return
    end
    if input.KeyCode == (S.noclipKeyCode or Enum.KeyCode.J) then
        _playClick()
        S.noclipEnabled = not S.noclipEnabled
        if S.btnRefs.noclipToggle then
            S.btnRefs.noclipToggle.Text = S.noclipEnabled and "Noclip: ON" or "Noclip: OFF"
            setToggleState(S.btnRefs.noclipToggle, S.noclipEnabled)
        end
        if S.noclipEnabled then
    startNoclip()
else
    stopNoclip()
end
        updateNoclipFloatVisual()
        return
    end
end)
UserInputService.InputBegan:Connect(function(input, gameProcessed)
    if not S.ctrlClickTPEnabled then
        return
    end
    if gameProcessed then
        return
    end
    if input.UserInputType ~= Enum.UserInputType.MouseButton1 then
        return
    end
    if not (UserInputService:IsKeyDown(Enum.KeyCode.LeftControl) or UserInputService:IsKeyDown(Enum.KeyCode.RightControl)) then
        return
    end
    local char = LocalPlayer.Character
    if not char then return end
    local hrp = char:FindFirstChild("HumanoidRootPart")
    if not hrp then return end
    local hit = mouse.Hit
    if not hit then return end
    local pos = hit.Position + Vector3.new(0, 2.5, 0)
    hrp.CFrame = CFrame.new(pos)
end)
LocalPlayer.CharacterAdded:Connect(function(character)
    S.flyEnabled = false
    stopFly()
   if S.flyToggle then
    S.flyToggle.Text = "Fly: OFF"
    setToggleState(S.flyToggle, false)
end
if S.flyFloatEnabled then
    if not S.flyFloatBtn then
        createFlyFloat()
    else
        updateFlyFloatVisual()
    end
end
    if S.isInvisible then
        disableInvisibility()
    end
end)
Players.PlayerAdded:Connect(function(player)
    registerPlayer(player)
    RefreshFlingPlayerList()
end)
Players.PlayerRemoving:Connect(function(player)
    if player == LocalPlayer then
        if S.isInvisible then
            disableInvisibility()
        end
        return
    end
    unregisterPlayer(player)
    if S.SelectedFlingTargets[player.Name] then
        S.SelectedFlingTargets[player.Name] = nil
    end
    RefreshFlingPlayerList()
end)
local function updateTPList()
    for _,v in pairs(S.tpPlayerList:GetChildren()) do
        if v:IsA("TextButton") then v:Destroy() end
    end
    for _,player in pairs(Players:GetPlayers()) do
        if player ~= LocalPlayer then
            local btn = Instance.new("TextButton")
            btn.Size = UDim2.new(1,-6,0,28)
            btn.Text = player.Name
            btn.BackgroundColor3 = S.selectedTPPlayers[player] and Color3.fromRGB(50, 200, 120) or Color3.fromRGB(38, 38, 38)
btn.BackgroundTransparency = S.selectedTPPlayers[player] and 0 or 0.35
            btn.TextColor3 = Color3.new(1,1,1)
            btn.Font = Enum.Font.Gotham
            btn.TextSize = 14
            btn.BorderSizePixel = 0
            btn.Parent = S.tpPlayerList
            local c = Instance.new("UICorner"); c.CornerRadius = UDim.new(0,4); c.Parent = btn
            btn.MouseButton1Click:Connect(function()
                S.selectedTPPlayers = {}
                S.selectedTPPlayers[player] = true
                updateTPList()
            end)
        end
    end
end
for _, player in pairs(Players:GetPlayers()) do
    registerPlayer(player)
end
task.spawn(function()
    while true do
        task.wait(2)
    end
end)
task.spawn(function()
    while true do
        task.wait(2)
        if S.autoHitboxEnabled then
            local size = tonumber(S.sizeInput.Text) or S.DEFAULT_HITBOX_SIZE
            for _, player in pairs(Players:GetPlayers()) do
                if player ~= LocalPlayer then
                    local char = player.Character
                    if char and isAlive(char) then
                        if not S.playerConnections[player] then registerPlayer(player) end
                        applyHeadHitbox(player, char, size)
                    end
                end
            end
            S.statusLabel.Text = "Processing: " .. tableCount(S.processedChars) .. " Players"
        end
    end
end)
task.spawn(function()
    while true do
        task.wait(1.1)
        if S.autoBodyHitboxEnabled then
            local size = tonumber(S.sizeInput.Text) or S.DEFAULT_HITBOX_SIZE
            local count = 0
            for _, player in pairs(Players:GetPlayers()) do
                if player ~= LocalPlayer and player.Character and isAlive(player.Character) then
                    applyBodyHitbox(player.Character, size)
                    count = count + 1
                end
            end
            for char, _ in pairs(S.bodyHitboxChars) do
                local humanoid = char:FindFirstChildOfClass("Humanoid")
                if not char.Parent or not humanoid or humanoid.Health <= 0 then
                    pcall(resetBodyHitbox, char)
                end
            end
            S.statusLabel.Text = "Processing: " .. count .. " Players"
        end
    end
end)
task.spawn(function()
    while true do
        task.wait(1)
        updateTPList()
    end
end)
task.spawn(function()
    while true do
        task.wait(1.5)
        for _, player in pairs(Players:GetPlayers()) do
            if player ~= LocalPlayer then
                local char = player.Character
                if char and isAlive(char) then
                    if not S.playerConnections[player] then
                        registerPlayer(player)
                    end
                    if S.chamsEnabled and not char:FindFirstChild("StarCrowChams") then
                        pcall(addChams, char, getTeamColor(player))
                    end
                    if S.autoHitboxEnabled then
                        local hSize = tonumber(S.sizeInput and S.sizeInput.Text) or S.DEFAULT_HITBOX_SIZE
                        if not S.processedChars[char] then
                            pcall(applyHeadHitbox, player, char, hSize)
                        elseif not char:FindFirstChild("StarCrowOutline") then
                        end
                    end
                    if S.espShowName or S.espShowDist or S.espShowHp or (S.nameESPEnabled and S.selectedPlayers[player]) then
                        local head = char:FindFirstChild("Head")
                        if head and not head:FindFirstChild("NameESP") then
                            pcall(createNameESP, player, char)
                        end
                    end
                end
            end
        end
    end
end)
S.statusLabel.Text = "Processing: 0 Players"
RefreshFlingPlayerList()
